# 《YES, BOSS!》后端任务型提示词库 v1.0

> **使用对象**：后端工程师（Python / FastAPI / asyncio / LLM 编排）
> **使用方式**：把对应任务下的「可直接复制给 AI 编程工具的完整提示词」整段复制到 Cursor / Claude Code / Windsurf / Augment / Copilot Agent 执行
> **配套文档**：`DESIGN(3).md`、`AI-Agent提示词库.md`、`前端提示词库.md`
> **版本范围**：v1 原型 = **4 季度 / 4 指标**（`CASH / MORALE / BOARD / FACE`），与现有前端 `js/state.js` 对齐；v2（8 季度 / 6 指标 / 摄像头分析 / 多 Agent 拆分）是路线图，本库**不实现**
> **更新日期**：2026-05-23

---

## 序章 · 你必须先理解的红线

### 0.1 项目一句话

你正在为一款"濒死公司 CEO 求生模拟器"做**后端**。前端会用浏览器直连 LLM 的纯文字版方式并行开发（`js/llm.js` 走 OpenAI 兼容协议）；你的任务是搭一套**协议化、可测试、可演进**的服务端，最终通过 WebSocket / REST 让前端从"裸 LLM 客户端"演进到"前端 ⇄ 后端 ⇄ LLM"。

### 0.2 v1 范围铁律

- **4 季度 / 4 指标**：`CASH / MORALE / BOARD / FACE`（**严禁** `SALES / MKT`）
- **总导演（Director）单次调用**模式（**不**拆 5+ Agent）
- **发布会只在 Q3 触发**（不做 Q1/Q5/Q7 多类型）
- **文字 transcript**模式（不接 ASR，但保留 ASR 后处理接口位置）
- **单局 + 元进度**（不做跨局 leaderboard / cloud sync）
- **公司业务 / 决策卡池 / 员工模板** v1 各取 6–12 条样本，可扩

### 0.3 实时期 / 异步期分离（核心架构红线）

```
┌──────────────── 实时期（玩家在线交互，毫秒级响应）────────────────┐
│  选决策卡  →  收八卦  →  发布会输入  →  提交本季                  │
│      │           │            │             │                    │
│      ▼           ▼            ▼             ▼                    │
│  只校验·只记录·只 ACK  —— 严禁调用 LLM!                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────── 异步期（季度结算，1–10 秒可接受）─────────────────┐
│  输入聚合  →  PromptBuilder  →  LLMClient  →  Parser            │
│                                                  │              │
│                                                  ▼              │
│   写入持久化  ←  Outbound Bundle  ←  RuleResolver               │
└─────────────────────────────────────────────────────────────────┘
```

违反这条红线，整套成本控制 / 可重放 / 可测试性全崩。**每条任务的提示词都会强制重申。**

### 0.4 分层架构图

```
┌────────────────────────────────────────────────────────────────┐
│                       API Layer                                 │
│  ┌────────────────────────┐  ┌──────────────────────────────┐  │
│  │  WebSocket Gateway     │  │  REST 调试接口 + Health      │  │
│  │  (连接/路由/ACK/重连)  │  │  (Postman/CI Probe)          │  │
│  └────────────────────────┘  └──────────────────────────────┘  │
└─────────────────────┬──────────────────────────────────────────┘
                      │
┌─────────────────────▼──────────────────────────────────────────┐
│                  Protocol Layer                                 │
│   Envelope · Inbound · Outbound · DTO · Serializer             │
└─────────────────────┬──────────────────────────────────────────┘
                      │
┌─────────────────────▼──────────────────────────────────────────┐
│                  Service Layer                                  │
│  CompanyService · QuarterStateMachine · DecisionService        │
│  GossipService · PressInputService                             │
│  SettlementInputAggregator · SettlementOrchestrator            │
│  DeathReportService                                            │
└──────┬───────────────────────────────┬─────────────────────────┘
       │                               │
┌──────▼──────────┐    ┌───────────────▼──────────────────────┐
│ Domain Layer    │    │             LLM Layer                │
│ Company/Stats   │    │  PromptBuilder · LLMClient (Mock /   │
│ Employee/Quarter│    │  OpenAI-compat 占位) · OutputParser ·│
│ Decision/Press  │    │  RetryChain · FallbackStub           │
│ Promise/Memory  │    └──────────────────────┬───────────────┘
└──────┬──────────┘                           │
       │                            ┌─────────▼────────────┐
┌──────▼──────────────────────┐     │   Rule Resolver      │
│      Repository Layer       │     │  DirectorResolver /  │
│  Protocol + InMemory + DB   │     │  PressEvalResolver   │
│  占位 (PostgreSQL/Redis)    │     └──────────────────────┘
└─────────────────────────────┘
       │
┌──────▼──────────────────────────────────────────────────────┐
│              Replay / Archive Layer                          │
│  PressArchive · DeathLog · PromiseLog · AgentMemory          │
└──────────────────────────────────────────────────────────────┘
```

### 0.5 异步结算阶段调用时序（每条相关任务都引用）

```
[实时期结束 — 玩家提交 SettleQuarter 消息]
        │
        ▼
1. SettlementInputAggregator 聚合:
   · 当前 GameSession 快照（4 指标 / quarter / company / employees）
   · 本季 selectedDecision + immediateEffect（已即时应用）
   · 本季 gossip 采集结果
   · 本季 pressInput（若有 transcript）
   · 跨季 promiseLog（最近 6 条）+ agentMemory 摘要
   · 已计划的延迟反噬 scheduledEvents
        │
        ▼
2. PromptBuilder:
   · build_director_prompt(aggregate)
   · build_press_eval_prompt(transcript) — 若本季有发布会
   · build_death_report_prompt(history)  — 若本季死亡触发
        │
        ▼
3. LLMClient.chat_complete(prompt) — 异步调用 mock 或真实模型
   · timeout / retry / circuit-breaker
        │
        ▼
4. OutputParser.parse_*(raw_text)
   · 严格 schema 校验 / 容错切片 / 字段钳制
        │
        ▼
5. RuleResolver.resolve(llm_suggestion, state, rules)
   · 把 LLM 建议折成后端权威 ΔStats
   · patience / metricsDelta 区间钳制
   · 违禁字段拒绝
        │
        ▼
6. 持久化:
   · GameSessionRepo.save(updated_session)
   · PressArchive.append(press_record)
   · PromiseLog.update_fulfilled_flags()
   · AgentMemory.append(entries)
        │
        ▼
7. 生成 Outbound Bundle:
   · SettlementBundle (quarterReport / mediaHeadlines / boardReaction /
                       employeeGossip / rivalAction / marketSignal /
                       metricsDelta)
   · PressBundle（若发布会）
   · DeathReportBundle（若死亡）
        │
        ▼
8. OutboundDispatcher 通过 WebSocket / REST 推给前端
```

### 0.6 共用规则（每条任务的"完整提示词"都内置；这里给你一次过看到）

1. 这是《YES, BOSS!》后端，v1 用 4 指标 `CASH / MORALE / BOARD / FACE`。
2. 前端会并行开发（纯 DOM + 浏览器直连 LLM），最终通过协议接入你的后端。
3. **当前任务只做后端**，不写前端文件，不动 `index.html / js/* / styles.css`。
4. **不启动开发服务器**（`uvicorn` / `docker` / 数据库 / Redis 一律不启动）。
5. **不调用真实 LLM**（除任务 16 的 LLMClient 抽象占位外，所有任务用 `MockLLMClient`）。
6. **不连接真实数据库**（in-memory 仓储 + DB 占位接口）。
7. **实时期只记录不判断**（决策选择 / 八卦采集 / 发布会输入均不调 LLM）。
8. **只有异步结算期才允许通过 `LLMClient` 抽象调用模型**。
9. MVP 不重点做 prompt injection 防护（保留：字段类型校验、文本长度限制、空消息拒绝、日志记录、基础异常处理）。
10. 所有模块必须可测试（每条任务都要写 pytest 用例 + 断言关键架构红线）。
11. 业务逻辑禁止写在 API 路由 / WebSocket handler 里——路由只做反序列化 + 调 service + 序列化。
12. mock / in-memory 实现必须可以无痛替换为真实 DB / 真实 LLM（必须用 `Protocol` 或抽象基类）。
13. 单文件 > 300 行就拆；避免巨石模块。
14. **输出顺序**：先说明计划 → 列出修改的文件清单 → 给出验证命令（`pytest` / `ruff` / `mypy`）→ 不启动服务。

### 0.7 技术栈固定

```
Python:        3.11+
Framework:     FastAPI (含 WebSocket)
Schema:        Pydantic v2（strict 模式）
Async:         asyncio
Test:          pytest + pytest-asyncio
Lint:          ruff
Typecheck:     mypy（strict 关键模块）
HTTP:          httpx（异步）
LLM SDK:       不绑定供应商，通过 LLMClient 抽象层封装
Persistence:   v1 = in-memory；DB 占位 = SQLAlchemy 2.x（不连接）
Protocol:      JSON over WebSocket / REST，预留 MessagePack adapter
```

如与 DESIGN / 前端提示词库已有约定冲突，**优先对齐已有约定**。

### 0.8 目录约定（任务 1 创建后所有任务沿用）

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI app 工厂（不启动）
│   ├── config.py               # Settings（pydantic-settings）
│   ├── api/
│   │   ├── __init__.py
│   │   ├── rest.py             # REST 调试接口
│   │   ├── ws.py               # WebSocket Gateway
│   │   └── health.py
│   ├── protocol/
│   │   ├── __init__.py
│   │   ├── envelope.py
│   │   ├── inbound.py
│   │   └── outbound.py
│   ├── domain/
│   │   ├── __init__.py
│   │   ├── company.py
│   │   ├── stats.py
│   │   ├── employee.py
│   │   ├── quarter.py
│   │   ├── decision.py
│   │   ├── press.py
│   │   ├── promise.py
│   │   ├── memory.py
│   │   └── meta_progress.py
│   ├── repo/
│   │   ├── __init__.py
│   │   ├── protocols.py        # Repository Protocols
│   │   ├── memory_impl.py      # in-memory 实现
│   │   ├── sql_stub.py         # SQLAlchemy 模型占位
│   │   └── migration_plan.md   # v2 迁移文档
│   ├── content/
│   │   ├── __init__.py
│   │   ├── companies.py
│   │   ├── decisions.py
│   │   ├── employees.py
│   │   ├── press_types.py
│   │   └── gossip_pool.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── company_service.py
│   │   ├── quarter_state_machine.py
│   │   ├── decision_service.py
│   │   ├── gossip_service.py
│   │   ├── press_input_service.py
│   │   ├── settlement_aggregator.py
│   │   ├── settlement_orchestrator.py
│   │   └── death_report_service.py
│   ├── llm/
│   │   ├── __init__.py
│   │   ├── prompt_builder.py
│   │   ├── client.py           # LLMClient 抽象 + Mock + OpenAI-compat 占位
│   │   ├── schema.py           # 输出 JSON schema
│   │   ├── parser.py
│   │   └── fallback.py
│   ├── rules/
│   │   ├── __init__.py
│   │   ├── director_resolver.py
│   │   └── press_resolver.py
│   └── safety/
│       ├── __init__.py
│       ├── brand_blacklist.py
│       └── transcript_cleaner.py
├── tests/
│   ├── __init__.py
│   ├── conftest.py
│   ├── factories.py
│   ├── unit/
│   ├── integration/
│   └── architecture/           # 关键架构断言（实时期不触达 LLM 等）
├── docs/
│   ├── BACKEND_FRONTEND_INTEGRATION.md
│   └── brand_blacklist.txt
├── pyproject.toml
├── pytest.ini
├── ruff.toml
├── mypy.ini
└── README.md
```

---

## 任务总览（22 条）

| # | 任务 | 涉及层 |
|---|---|---|
| 1 | 初始化后端工程骨架 | 全局 |
| 2 | 领域模型 — 公司 / 指标 / 死因 | Domain |
| 3 | 领域模型 — 员工生态 | Domain |
| 4 | 领域模型 — 季度流程 | Domain |
| 5 | 领域模型 — 跨季度记忆与元进度 | Domain |
| 6 | 协议层 — Envelope / Inbound / Outbound | Protocol |
| 7 | 仓储层 — in-memory + DB 占位 + 迁移计划 | Repo |
| 8 | 内容注册中心 — 业务/决策/员工/发布会池 | Content |
| 9 | CompanyService — 开局生成 | Service |
| 10 | QuarterStateMachine — 阶段流转与死亡判定 | Service |
| 11 | DecisionService — 抽卡选卡即时应用 | Service |
| 12 | GossipService — 信息收集（实时期只记录） | Service |
| 13 | PressInputService — 发布会录入 + ASR 安全过滤 | Service / Safety |
| 14 | SettlementInputAggregator — 异步结算前聚合 | Service |
| 15 | PromptBuilder — Director / PressEval / DeathReport | LLM |
| 16 | LLMClient — 抽象 + Mock + OpenAI 兼容占位 | LLM |
| 17 | OutputParser — Schema + 兜底链 | LLM |
| 18 | RuleResolver — 裁决器 | Rules |
| 19 | SettlementOrchestrator — 异步结算总控 | Service |
| 20 | DeathReportService — 死亡复盘 + 遗产解锁 | Service |
| 21 | API 层 — WebSocket Gateway + REST 调试 + Health | API |
| 22 | 测试套件 + 数据工厂 + 集成文档 | Test / Docs |

---

## 任务 1：初始化后端工程骨架

### 使用场景

仓库根目录已存在前端文件（`index.html / js/* / styles.css`），现在要新建独立 `backend/` 子目录承载 Python 后端。**这条任务是后续所有任务的前置**——其他任务都假设这个骨架已存在。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》游戏初始化后端工程骨架。

# 项目背景
《YES, BOSS!》是一款"濒死公司 CEO 求生模拟器"，玩家做决策、开发布会、和 AI 员工 / 董事会 / 媒体周旋。
v1 范围：4 季度 / 4 指标（CASH / MORALE / BOARD / FACE），单局 + 跨局元进度。
前端已存在（纯 DOM + ES Module，浏览器直连 LLM），现在并行做后端。最终前端通过 WebSocket / REST 接入后端。

# 共用规则（务必遵守）
1. 当前任务只做后端，不写前端，不动 index.html / js/* / styles.css。
2. 不启动开发服务器（uvicorn / docker / 数据库 / Redis 一律不启动）。
3. 不调用真实 LLM。
4. 不连接真实数据库。
5. 实时期只记录不判断；只有异步结算期才允许调 LLM。
6. MVP 不重点做 prompt injection 防护。
7. 业务逻辑禁止写在 API 路由里。
8. mock 实现必须可未来无痛替换（用 Protocol / 抽象基类）。
9. 单文件 > 300 行就拆。
10. 输出顺序：先说明计划 → 列出修改文件 → 给验证命令。

# 技术栈
- Python 3.11+
- FastAPI（含 WebSocket）
- Pydantic v2（strict）
- pydantic-settings（环境变量）
- asyncio
- pytest + pytest-asyncio
- ruff（lint）
- mypy（strict 关键模块）
- httpx（异步 HTTP）
- SQLAlchemy 2.x（占位，不连接）

# 本任务必须做的事
1. 在仓库根目录创建 `backend/` 子目录。
2. 创建以下完整目录树（空 __init__.py 也要建）：
   backend/
   ├── app/
   │   ├── api/{rest.py, ws.py, health.py}
   │   ├── protocol/{envelope.py, inbound.py, outbound.py}
   │   ├── domain/{company.py, stats.py, employee.py, quarter.py, decision.py, press.py, promise.py, memory.py, meta_progress.py}
   │   ├── repo/{protocols.py, memory_impl.py, sql_stub.py, migration_plan.md}
   │   ├── content/{companies.py, decisions.py, employees.py, press_types.py, gossip_pool.py}
   │   ├── services/{company_service.py, quarter_state_machine.py, decision_service.py, gossip_service.py, press_input_service.py, settlement_aggregator.py, settlement_orchestrator.py, death_report_service.py}
   │   ├── llm/{prompt_builder.py, client.py, schema.py, parser.py, fallback.py}
   │   ├── rules/{director_resolver.py, press_resolver.py}
   │   ├── safety/{brand_blacklist.py, transcript_cleaner.py}
   │   ├── main.py
   │   └── config.py
   ├── tests/{conftest.py, factories.py, unit/, integration/, architecture/}
   ├── docs/{BACKEND_FRONTEND_INTEGRATION.md, brand_blacklist.txt}
   ├── pyproject.toml
   ├── pytest.ini
   ├── ruff.toml
   ├── mypy.ini
   └── README.md

   所有 .py 文件除明确实现的（config.py / main.py / health.py / conftest.py）外，先建空文件 + 单行 docstring 占位 + `__all__ = []`，后续任务填充。

3. 实现 `backend/app/config.py`：
   - 用 pydantic-settings BaseSettings
   - 字段：app_name="yes-boss-backend" / env: Literal["dev","test","prod"]="dev" / log_level="INFO" / llm_mode: Literal["mock","openai_compat"]="mock" / llm_endpoint: Optional[str]=None / llm_api_key: Optional[SecretStr]=None / llm_model: Optional[str]=None / llm_timeout_ms=15000 / settlement_max_concurrency=4
   - 提供 `get_settings()` lru_cache 单例
   - 不读取真实环境变量值；测试可注入

4. 实现 `backend/app/main.py`：
   - `create_app() -> FastAPI`：注册路由、中间件、startup/shutdown hook 占位
   - **严禁** `if __name__ == "__main__": uvicorn.run(...)`
   - 注册一个最简 root GET / 返回 {"app": settings.app_name, "version": "0.1.0"}

5. 实现 `backend/app/api/health.py`：
   - GET /healthz → {"status":"ok","ts":<iso>,"components":{...}}
   - GET /readyz → 检查 in-memory repo / mock llm client 占位，返回 ready/not_ready
   - 不依赖任何真实外部服务

6. `pyproject.toml`：
   - name=yes-boss-backend, version=0.1.0
   - dependencies: fastapi, uvicorn[standard], pydantic>=2, pydantic-settings, httpx, sqlalchemy>=2, typing-extensions
   - dev-dependencies: pytest, pytest-asyncio, pytest-cov, ruff, mypy
   - tool.ruff: line-length=100, target-version=py311, select=["E","F","W","I","B","UP","SIM","TID"]
   - tool.mypy: strict=true 对 app/，tests/ 允许宽松

7. `pytest.ini`：testpaths=tests, asyncio_mode=auto, addopts="-q --strict-markers"

8. `ruff.toml` / `mypy.ini`：与 pyproject 同步

9. `tests/conftest.py`：
   - `test_settings` fixture 注入 dev settings
   - `app_factory` fixture 返回 create_app()
   - `client` fixture 用 httpx.AsyncClient + ASGITransport

10. 两条最简单测试：
    - `tests/unit/test_health.py`：测 /healthz 返回 200 + 字段完整
    - `tests/unit/test_app_factory.py`：测 create_app() 不抛 + 路由表非空

11. `backend/README.md` ≤ 80 行：项目简介 / 目录结构 / 本地验证命令（仅 pytest/ruff/mypy）/ 明确写"不要 uvicorn run，等任务 21"

# 禁止事项
- 禁止运行 uvicorn / docker / 任何长进程。
- 禁止 pip install —— 只产出 pyproject.toml 声明。
- 禁止填充其他 21 条任务的代码（domain / service / llm 等）。
- 禁止写前端文件。
- 禁止在 main.py 末尾写 if __name__ == "__main__": uvicorn.run(...)。
- 禁止假装连接真实 LLM / 数据库。
- 禁止依赖 Node.js / npm。

# 验收标准
1. 目录结构与树形完全一致。
2. pytest -q 在 backend/ 下 2 条测试通过。
3. ruff check app tests 零错误。
4. mypy app 零错误（空文件 + docstring 不触发错误）。
5. /healthz 与 /readyz 返回 200。
6. 没有任何文件调用 uvicorn.run。
7. config.py 的 SecretStr 字段 repr 中不暴露 key。
8. 所有 __init__.py 存在（即使空）。

# 输出
1. 计划摘要（5 行内）
2. 修改/新建文件清单
3. 关键文件内容（main.py / config.py / health.py / conftest.py / pyproject.toml / README.md）
4. 验证命令（pytest -q / ruff check / mypy app）
5. 明确声明"未启动任何服务"
````

### 预期产物

- `backend/` 完整目录树（约 60 个空文件占位 + 上述需实现文件）
- 可被 pytest 发现并通过的最小测试集
- `pyproject.toml` / `pytest.ini` / `ruff.toml` / `mypy.ini` 齐全
- `backend/README.md` 说明本地验证方式
- `backend/app/main.py` 暴露 `create_app()`
- `backend/app/config.py` 暴露 `get_settings()`
- `/healthz` 和 `/readyz` 可响应

### 验收标准

1. 目录结构与提示词树形 1:1 对齐。
2. `pytest -q` 通过 ≥ 2 条测试。
3. `ruff check backend/app backend/tests` 零错误。
4. `mypy backend/app` 关键模块零错误。
5. `main.py` 不含 `uvicorn.run()`。
6. `SecretStr` 字段在 `repr(settings)` 中不暴露 key。
7. `/healthz` / `/readyz` 返回 JSON 且字段完整。
8. 所有子目录均有 `__init__.py`。
9. `README.md` 明确"不要启动 uvicorn"。
10. 没有任何前端文件被修改。

### 禁止事项

- 禁止启动 uvicorn / docker / npm。
- 禁止填充本任务范围外的模块（domain / service / llm / repo 实际逻辑）。
- 禁止 `pip install` 实际执行；只声明 `pyproject.toml`。
- 禁止修改前端 `index.html / js/* / styles.css`。
- 禁止在 `main.py` 写 `if __name__ == "__main__"` 启动块。
- 禁止假装连接 LLM / 数据库。

---

## 任务 2：领域模型 — 公司 / 指标 / 死因

### 使用场景

任务 1 已建好骨架；现在要把 DESIGN §四（公司生命指标）和 §十三（开局公司业务）落地为 Pydantic v2 领域模型。**这是后续 Service / LLM / Repo 所有任务依赖的基础类型**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》后端实现"公司 + 4 指标 + 死因"领域模型。

# 项目背景
《YES, BOSS!》是濒死公司 CEO 求生模拟器。v1 范围：4 季度 / 4 指标（CASH / MORALE / BOARD / FACE），玩家撑过 Q4 = 暂时活下来。任一指标触底 = 公司死亡，死法不同。

# 共用规则
1. 只做后端，不写前端。
2. 不启动 uvicorn / docker / 数据库。
3. 不调真实 LLM。
4. 不连真实数据库。
5. 实时期只记录不判断；异步结算期才调 LLM。
6. MVP 不重点做 prompt injection 防护。
7. 业务逻辑不写在路由。
8. mock 可替换（用 Protocol）。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 代码 → 验证命令。

# 技术栈
- Python 3.11+
- Pydantic v2（strict 模式）
- mypy strict

# 本任务范围（只动这些文件）
- backend/app/domain/stats.py
- backend/app/domain/company.py
- backend/app/domain/__init__.py（导出公共类型）
- backend/tests/unit/test_domain_stats.py
- backend/tests/unit/test_domain_company.py
- backend/tests/factories.py（追加 build_stats / build_company）

# 必须实现的类型

## stats.py
1. class Stats(BaseModel)（ConfigDict frozen=True, strict=True）：
   - CASH/MORALE/BOARD/FACE: int = Field(ge=0, le=100)
   - 类方法 Stats.starting(rng_seed: int | None = None) -> Stats：
     · CASH ∈ [30, 60], MORALE ∈ [40, 60], BOARD ∈ [40, 60], FACE ∈ [50, 70]
     · rng_seed 可复现
   - apply_delta(delta: StatsDelta) -> Stats（返回新对象，钳 [0,100]）
   - is_dead() -> DeathReason | None：
     · CASH ≤ 0 → BANKRUPTCY
     · MORALE ≤ 10 → MORALE_COLLAPSE
     · BOARD ≤ 5 → OUSTED
     · FACE ≤ 0 → DISGRACE
     · 否则 None
   - 多个死亡同时触发时优先级：CASH > FACE > BOARD > MORALE（按 DESIGN 视觉冲击力）

2. class StatsDelta(BaseModel)：
   - 字段全部 Optional[int]，无 ge/le
   - merge(other: StatsDelta) -> StatsDelta 累加合并
   - as_dict() -> dict[str, int] 跳过 None

3. class DeathReason(StrEnum)：BANKRUPTCY / MORALE_COLLAPSE / OUSTED / DISGRACE
   每个 @property title 中文一句话（破产清算 / 集体崩盘 / CEO 被罢免 / 身败名裂）

## company.py
1. class DeathCause(BaseModel)：
   - category: Literal["financial","product","org","market","capital","trust","absurd"]
   - description: str（≤ 80 字中文）

2. class Company(BaseModel, frozen=True)：
   - id: str（uuid4）
   - name: str（≤ 20 字中文公司名）
   - business: str（≤ 30 字荒诞业务）
   - absurdity: int = Field(ge=1, le=5)
   - founding_motto: str（≤ 30 字 slogan）
   - death_causes: list[DeathCause]（2–3 条）
   - starting_promises: list[str]（前任已破承诺，0–3 条，≤ 30 字 each）
   - founded_year: int（2020–2029）

3. 类方法 Company.from_template(template: dict, rng_seed: int | None = None) -> Company

# 测试 (test_domain_stats.py)
- Stats.starting() 在合法区间
- Stats.starting(seed=42) 两次调用结果一致
- apply_delta 正/负/越界（钳 [0,100]）
- is_dead 四种死法各一条 + 健康返回 None
- 多重死亡触发时优先级正确
- StatsDelta.merge() 累加正确

# 测试 (test_domain_company.py)
- Company.from_template 字段长度约束生效
- 非法 absurdity（0 / 6）被 ValidationError 拒绝
- id 不重复（生成 100 次 set 长度 == 100）

# factories.py 追加
- build_stats(**overrides) -> Stats
- build_company(**overrides) -> Company

# 禁止事项
- 禁止引入 enum 之外的全局可变常量。
- 禁止在 Stats 内塞 apply_decision 之类业务方法（属 service 层）。
- 禁止把指标公式硬编码进 Stats（属 SettlementOrchestrator）。
- 禁止用 dict 字典代替 Pydantic 模型。
- 禁止 import app.services / app.api / app.llm（domain 是底层）。
- 禁止扩成 6 指标（v1 只 4 指标）。

# 验收标准
1. pytest backend/tests/unit/test_domain_stats.py test_domain_company.py -q 全过
2. ruff check backend/app/domain 零错误
3. mypy backend/app/domain --strict 零错误
4. Stats / Company / StatsDelta / DeathReason / DeathCause 从 app.domain 顶层可 import
5. Stats / Company frozen=True；apply_delta 返回新对象
6. is_dead 阈值与 DESIGN §12.5 完全一致
7. DeathReason.title 全部中文
8. Company.id 是 uuid4

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动服务、未调 LLM、未连库"
````

### 预期产物

- `backend/app/domain/stats.py`（`Stats / StatsDelta / DeathReason`）
- `backend/app/domain/company.py`（`Company / DeathCause`）
- `backend/app/domain/__init__.py` 导出
- 2 个测试文件
- `factories.py` 追加 2 个工厂

### 验收标准

1. 单测全过。
2. ruff / mypy 零错误。
3. 死亡阈值与 DESIGN §12.5 一致。
4. `Stats` `frozen=True`；`apply_delta` 返回新对象。
5. 类型从 `app.domain` 顶层可 import。
6. 没有 service / api / llm 反向依赖。
7. `Stats.starting(seed=42)` 结果可复现。
8. 多重死亡触发时优先级 CASH > FACE > BOARD > MORALE。

### 禁止事项

- 禁止在 Stats 内塞业务方法（结算公式归 orchestrator）。
- 禁止 domain → service / api / llm 反向 import。
- 禁止把 4 指标扩成 6 指标。
- 禁止用 dict 代替 Pydantic 模型。
- 禁止启动服务 / 调 LLM / 连库。

---

## 任务 3：领域模型 — 员工生态

### 使用场景

DESIGN §九 描述了 12 名员工 + 性格 tag + 关系网 + 派系 + 隐藏秘密 + 心情。本任务把这些落地为 Pydantic 模型，供 GossipService、SettlementOrchestrator（员工反应）、DeathReportService（最后留下的员工）使用。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》后端实现"员工 + 关系 + 八卦线索"领域模型。

# 项目背景
《YES, BOSS!》员工不是数值，是"办公室小社会"。每名员工有性格、忠诚、压力、关系网、秘密。玩家通过茶水间/电梯间等场景收集八卦提前发现危机。

# 共用规则
1. 只做后端，不写前端。
2. 不启动服务 / docker / 数据库。
3. 不调真实 LLM。
4. 不连真实数据库。
5. 实时期只记录不判断（八卦采集只生成线索，不调 LLM）。
6. MVP 不做 prompt injection 防护。
7. 业务逻辑不写在路由。
8. mock 可替换。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 代码 → 验证命令。

# 技术栈
- Python 3.11+, Pydantic v2 strict, mypy strict

# 本任务范围（只动这些文件）
- backend/app/domain/employee.py
- backend/app/domain/__init__.py（追加导出）
- backend/tests/unit/test_domain_employee.py
- backend/tests/factories.py（追加 build_employee / build_gossip_lead）

# 必须实现的类型

## employee.py
1. class PersonalityTag(StrEnum)：
   GOOD_PERSON / SLACKER / GO_GETTER / VETERAN / TROUBLEMAKER / DEFECTOR / IN_LOVE / RADICAL / CAUTIOUS
   每个有 .label_zh（中文：老好人/摸鱼/卷王/老臣/刺头/叛逃种子/恋爱中/革命派/谨慎）

2. class Mood(StrEnum)：
   HOPEFUL / NEUTRAL / ANXIOUS / ANGRY / NUMB / IN_LOVE
   每个有 .icon (emoji) 和 .label_zh

3. class Faction(StrEnum)：TECH / SALES / FINANCE / OPS / NEUTRAL

4. class Relationship(BaseModel)：
   - target_id: str
   - type: Literal["close_friend","crush","faction_ally","faction_enemy","mentor","rival"]
   - strength: int = Field(ge=0, le=100)

5. class Employee(BaseModel, frozen=True)：
   - id: str（"E-"+uuid4hex[:8] 形式）
   - name: str（≤ 6 字中文姓名）
   - role: str（≤ 12 字岗位）
   - competence: int = Field(ge=0, le=100)
   - loyalty: int = Field(ge=0, le=100)
   - stress: int = Field(ge=0, le=100)
   - personality_tag: PersonalityTag
   - faction: Faction
   - relationships: list[Relationship] = []
   - hidden_secrets: list[str] = []（每条 ≤ 40 字，字段 docstring 写"WARN: 严禁出现在 outbound 协议中"）
   - attitude_to_player: int = Field(ge=0, le=100)
   - current_goal: str | None = None
   - mood: Mood = Mood.NEUTRAL

   实例方法：
   - with_loyalty(delta: int) -> Employee（返回新实例，钳 [0,100]）
   - with_stress(delta: int) -> Employee
   - derive_mood() -> Mood：纯函数
     · loyalty > 70 → HOPEFUL
     · loyalty < 30 and stress > 60 → ANGRY
     · stress > 80 and loyalty < 30 → NUMB
     · stress > 60 → ANXIOUS
     · relationships 含 type=crush 且 strength>50 → IN_LOVE
     · 否则 NEUTRAL
   - is_quitting_risk() -> bool：loyalty < 20 或 attitude_to_player < 15

   类方法：
   - Employee.from_template(template: dict, rng_seed: int | None = None) -> Employee

6. class GossipReliability(StrEnum)：RUMOR / LIKELY / CONFIRMED（对应 30%/50%/90% 真实度桶）

7. class GossipLead(BaseModel, frozen=True)：
   - id: str
   - quarter: int = Field(ge=1, le=4)
   - scene: Literal["tearoom","elevator","meeting_room","workstation","rooftop","smoking_area"]
   - speaker_id: str | None
   - text: str（≤ 60 字中文）
   - reliability: GossipReliability
   - is_truth: bool（后端独有，字段 docstring 标 WARN: 禁止 outbound）
   - linked_employee_ids: list[str] = []
   - ap_cost: int = Field(ge=0, le=3, default=1)

# 测试 (test_domain_employee.py)
- derive_mood 6 种 mood 至少各一条 case
- with_loyalty 正/负/越界（钳 [0,100]）
- is_quitting_risk 边界（loyalty=19/20/21；attitude=14/15/16）
- PersonalityTag.label_zh 9 个标签全部断言
- Employee 不可变（赋值新字段抛 ValidationError）
- GossipLead.is_truth 与 reliability 解耦（CONFIRMED 但 is_truth=False 合法）

# factories.py 追加
- build_employee(**overrides) -> Employee（默认 personality=GOOD_PERSON, faction=NEUTRAL）
- build_gossip_lead(**overrides) -> GossipLead

# 禁止事项
- 禁止把"心情触发动画"逻辑放在 domain（前端事）。
- 禁止 Employee 反向依赖 service / api / llm。
- 禁止把 hidden_secrets / is_truth 暴露在任何 outbound 字段——本任务不实现 Outbound DTO，但字段 docstring 必须标注 WARN。
- 禁止在 GossipLead 调用 LLM 来生成 text。
- 禁止把"线索池"硬编码在 employee.py（属于 content/gossip_pool.py，任务 8）。

# 验收标准
1. 单测全过。
2. ruff / mypy 零错误。
3. Employee / GossipLead frozen=True。
4. with_loyalty / with_stress 钳制正确。
5. derive_mood 决策树与 DESIGN §9.6 一致。
6. PersonalityTag 9 个 tag 覆盖 DESIGN §9.3 全部。
7. hidden_secrets / is_truth 字段有 WARN docstring。
8. 从 app.domain 顶层可 import 所有公开类型。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动服务、未调 LLM、未连库"
````

### 预期产物

- `backend/app/domain/employee.py`（`Employee / Relationship / GossipLead / PersonalityTag / Mood / Faction / GossipReliability`）
- `backend/app/domain/__init__.py` 追加导出
- `test_domain_employee.py`
- `factories.py` 追加

### 验收标准

1. 单测全过。
2. `Employee.frozen=True`，`with_*` 返回新实例。
3. `derive_mood` 决策树覆盖 6 种 mood。
4. `is_quitting_risk` 边界精确。
5. `GossipLead.is_truth` 字段 docstring 写 "WARN: 禁止 outbound"。
6. ruff / mypy 零错误。
7. domain 不反向依赖 service / api / llm。

### 禁止事项

- 禁止把 `hidden_secrets / is_truth` 暴露在任何 outbound 协议字段。
- 禁止在 domain 调 LLM 生成文本。
- 禁止 domain 反向 import service / api / llm。
- 禁止把"线索池数据"内嵌（归 content/gossip_pool.py）。
- 禁止启动服务 / 调 LLM / 连库。

---

## 任务 4：领域模型 — 季度流程实体

### 使用场景

把 DESIGN §三（季度循环）+ §五（决策卡）+ §八（发布会）落地为领域模型：季度、决策卡、发布会、历史条目、结算包。**任务 10–14 全部依赖这套类型**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》后端实现"季度 + 决策卡 + 发布会 + 历史 + 结算"领域模型。

# 项目背景
《YES, BOSS!》单局结构：1 家公司 × 4 季度（v1）。每季度阶段：briefing → gossip → decision → press(仅 Q3) → settlement → next。
每季度抽 3 张决策卡（都是坏选项），玩家选 1；选完立即应用 immediateEffect；季度结束触发异步结算。
发布会只在 Q3 触发；玩家提交文字 transcript，后端在结算时调 LLM 评估 + 生成媒体头条。

# 共用规则
1. 只做后端，不写前端。
2. 不启动服务 / docker / 数据库。
3. 不调真实 LLM。
4. 不连真实数据库。
5. 实时期只记录不判断（决策选择/发布会输入只标记，不调 LLM）。
6. MVP 不做 prompt injection 防护。
7. 业务逻辑不写在路由。
8. mock 可替换。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 代码 → 验证命令。

# 技术栈
- Python 3.11+, Pydantic v2 strict, mypy strict

# 本任务范围
- backend/app/domain/quarter.py
- backend/app/domain/decision.py
- backend/app/domain/press.py
- backend/app/domain/__init__.py（追加导出）
- backend/tests/unit/test_domain_quarter.py
- backend/tests/unit/test_domain_decision.py
- backend/tests/unit/test_domain_press.py
- backend/tests/factories.py（追加 build_decision_card / build_quarter / build_press_input）

# 必须实现的类型

## decision.py
1. class DecisionCategory(StrEnum)：
   FUNDING / LAYOFF / SELL_ASSET / PIVOT / KILL_PRODUCT / PRICE_WAR / SOOTHE / SWAP_EXEC / HIDE_BAD_NEWS / DELAY_BOARD / NEGOTIATE_RIVAL / HUMILIATING_TERM
   每个 .label_zh（融资/裁员/卖资产/产品转型/砍产品线/价格战/安抚员工/换高管/隐瞒坏消息/争取董事会时间/与对手谈判/接受屈辱条款）

2. class DecisionCard(BaseModel, frozen=True)：
   - id: str（D-uuid4hex[:8] 或 D_LAYOFF_01 等内容池 ID）
   - category: DecisionCategory
   - title: str（≤ 14 字中文）
   - description: str（≤ 40 字中文）
   - immediate_effect: StatsDelta（注：每项 -25 ~ +25）
   - flavor: str（≤ 30 字 italic flavor text）
   - long_term_hint: str | None = None（≤ 30 字，给前端 hover 显示，玩家可见）
   - boomerang_seeds: list[BoomerangSeed] = []（异步结算后转 ScheduledEvent）

3. class BoomerangSeed(BaseModel)：
   - delay_quarters: Literal[1, 2, 3]
   - probability: float = Field(ge=0, le=1)
   - description: str（≤ 40 字）
   - effect: StatsDelta

## press.py
1. class PressType(StrEnum)：
   INAUGURATION / CRISIS / PRODUCT / FINANCIAL / ROADSHOW / LAYOFF_EXPLAIN / REGULATOR / COUNTER
   每个 .label_zh

2. class PressInput(BaseModel, frozen=True)：
   - quarter: int = Field(ge=1, le=4)
   - press_type: PressType
   - must_answer_topics: list[str] = Field(min_length=1, max_length=4)
   - transcript: str = Field(min_length=30, max_length=600)
   - duration_s: float | None = None
   - word_count: int = Field(ge=0)
   - flags: list[str] = []  # ASR 安全过滤标记，如 ["brand_replaced","profanity_filtered"]
   - submitted_at: datetime

3. class PressEvaluation(BaseModel)：（异步结算后填充，禁止前端实时构造）
   - scores: dict[Literal["contentCompleteness","issueResponse","overpromise","logicClarity","confidence","riskAvoidance","memorableQuote","weaknessExposed","authenticity"], int]
   - memorable_quote: str
   - biggest_flaw: str
   - media_angle: Literal["金句传播","漏洞放大","模糊带过"]
   - stat_impact: StatsDelta

4. class MediaHeadline(BaseModel, frozen=True)：
   - outlet: Literal["36 氪","彭博体","晚点 LatePost","虎嗅","钛媒体","脉脉自媒体"]
   - headline: str (≤ 22 字)
   - tone: Literal["positive","neutral","negative","mocking"]
   - summary: str | None = None (≤ 150 字)

5. class PressBundle(BaseModel)：（结算 outbound 用）
   - input: PressInput
   - evaluation: PressEvaluation
   - headlines: list[MediaHeadline] = Field(min_length=1, max_length=4)

## quarter.py
1. class QuarterPhase(StrEnum)：
   BRIEFING / GOSSIP / DECISION / PRESS / SETTLEMENT / DONE

2. class Briefing(BaseModel, frozen=True)：
   - quarter: int = Field(ge=1, le=4)
   - market_mood: Literal["bull","neutral","bear","crisis"]
   - headline_hint: str（≤ 60 字，AI 秘书播报）
   - hidden_risks: list[str] = []（后端独有，docstring 标 WARN: 不 outbound）

3. class Settlement(BaseModel)：
   - quarter: int
   - quarter_report: str（80–150 字，黑色幽默 + 财经体，末尾匿名员工评价）
   - board_reaction: BoardReaction
   - employee_gossip: EmployeeGossip
   - rival_action: RivalAction
   - market_signal: Literal["bull","neutral","bear","crisis"]
   - metrics_delta: StatsDelta（结算最终权威 delta，不含 immediate）
   - scheduled_events_added: list[str] = []  # 本季新增的延迟反噬 id

4. class BoardReaction(BaseModel, frozen=True)：
   - speech: str (≤ 60 字)
   - patience_delta: int = Field(ge=-2, le=1)
   - vote: Literal["approve","oppose","abstain"]

5. class EmployeeGossip(BaseModel, frozen=True)：
   - speaker: str (≤ 6 字)
   - line: str (≤ 35 字)
   - mood: Literal["anxious","angry","tired","hopeful","numb","excited","in_love"]

6. class RivalAction(BaseModel, frozen=True)：
   - rival_name: str
   - action: Literal["price_war","poach","launch","pr_attack","wait","acquisition_rumor"]
   - description: str (≤ 60 字)
   - expected_damage: StatsDelta

7. class Quarter(BaseModel)：（可变，用 model_copy(update=...) 更新）
   - number: int = Field(ge=1, le=4)
   - phase: QuarterPhase = QuarterPhase.BRIEFING
   - briefing: Briefing | None = None
   - decision_cards: list[DecisionCard] = []  # 抽到的 3 张
   - selected_decision_id: str | None = None
   - gossip_collected: list[str] = []  # GossipLead.id
   - press_input: PressInput | None = None
   - press_bundle: PressBundle | None = None  # 结算后填
   - settlement: Settlement | None = None
   - ap_remaining: int = Field(ge=0, default=10)  # 行动点

8. class HistoryEntry(BaseModel, frozen=True)：
   - quarter: int
   - decision_id: str
   - press_bundle_id: str | None = None
   - stats_before: Stats
   - stats_after: Stats
   - settlement_summary: str (≤ 60 字)

# 测试
- test_domain_decision.py:
  · DecisionCard 字段长度边界
  · BoomerangSeed probability 越界拒绝（-0.1 / 1.1）
  · DecisionCategory 12 类 label_zh 全断言
- test_domain_press.py:
  · PressInput transcript 长度边界（29 拒绝、30 接受、600 接受、601 拒绝）
  · MediaHeadline.outlet 不在 6 个白名单中拒绝
  · PressEvaluation.scores 必须包含 9 个键，任意缺失 ValidationError
- test_domain_quarter.py:
  · Quarter 默认 phase=BRIEFING
  · Quarter.ap_remaining 不能负
  · Briefing.hidden_risks 字段有 WARN docstring
  · HistoryEntry frozen，stats_before/after 用任务 2 的 Stats

# factories.py 追加
- build_decision_card(**overrides)
- build_quarter(**overrides)
- build_press_input(**overrides)
- build_press_evaluation(**overrides)
- build_settlement(**overrides)

# 禁止事项
- 禁止把"抽 3 张卡"逻辑放进 domain（属 DecisionService，任务 11）。
- 禁止 PressEvaluation 由前端构造——它只能由 SettlementOrchestrator 在异步阶段写入。
- 禁止在 PressInput 字段中包含 evaluation（解耦实时录入与异步评估）。
- 禁止 Briefing.hidden_risks 出现在 outbound（字段 docstring 必须标 WARN）。
- 禁止把 Quarter.phase 流转逻辑放在 domain（属 QuarterStateMachine，任务 10）。
- 禁止 import service / api / llm。

# 验收标准
1. 所有单测全过。
2. ruff / mypy 零错误。
3. DecisionCategory 12 类对齐 DESIGN §5.2。
4. PressType 8 类对齐 DESIGN §8.1。
5. MediaHeadline.outlet 白名单与 AI-Agent提示词库 §3.1 一致。
6. PressEvaluation.scores 9 个键名对齐 §7.8 / §4.1。
7. Quarter.number ∈ [1, 4]（v1 4 季度）。
8. 从 app.domain 顶层可 import 所有公开类型。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动服务、未调 LLM、未连库"
````

### 预期产物

- `backend/app/domain/quarter.py`
- `backend/app/domain/decision.py`
- `backend/app/domain/press.py`
- 3 个测试文件
- `factories.py` 追加 5 个工厂

### 验收标准

1. 单测全过；ruff / mypy 零错误。
2. `DecisionCategory` 12 类对齐 DESIGN §5.2。
3. `PressType` 8 类对齐 DESIGN §8.1。
4. `PressEvaluation.scores` 9 个键名对齐 §7.8。
5. `MediaHeadline.outlet` 白名单与 AI-Agent提示词库 §3.1 一致。
6. `Quarter.number ∈ [1, 4]`。
7. `Briefing.hidden_risks` 与 `PressEvaluation.internal_eval`（若有）字段 docstring 标 WARN。
8. domain 不反向依赖 service / api / llm。

### 禁止事项

- 禁止把"抽 3 张卡 / 选卡 / 应用 immediate" 逻辑放进 domain。
- 禁止 PressEvaluation 在实时期构造（必须 SettlementOrchestrator 写入）。
- 禁止 Quarter.phase 流转逻辑写进 domain（属 QuarterStateMachine）。
- 禁止把 hidden_risks 暴露在 outbound。
- 禁止启动服务 / 调 LLM / 连库。

---

## 任务 5：领域模型 — 跨季度记忆与元进度

### 使用场景

DESIGN §十 + §十三 + AI-Agent提示词库 §7 涉及 4 个跨季度持久概念：玩家承诺（promise_log）、Agent 记忆（agent_memory）、计划反噬（scheduled_events）、跨局元进度（meta_progress：解锁遗产/管理风格/死亡日志）。**这是 SettlementOrchestrator、DeathReportService、LegacyService 的基础类型**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》后端实现"跨季度记忆 + 跨局元进度"领域模型。

# 项目背景
《YES, BOSS!》记忆系统分两层：
- **单局内**：promise_log（玩家在发布会/质询/决策中的承诺）+ agent_memory（董事会/员工对玩家行为的记忆）+ scheduled_events（决策延迟反噬）
- **跨局**：meta_progress（解锁遗产/管理风格/死亡日志/发布会金句存档）

单局结束（死亡或撑过 Q4）→ 提取 legacy 写入 meta_progress；下一局开局从 meta_progress 加载初始 buff。

# 共用规则
1. 只做后端，不写前端。
2. 不启动服务 / docker / 数据库。
3. 不调真实 LLM。
4. 不连真实数据库。
5. 实时期只记录不判断（promise 抽取是纯正则，不调 LLM）。
6. MVP 不做 prompt injection 防护。
7. 业务逻辑不写在路由。
8. mock 可替换。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 代码 → 验证命令。

# 技术栈
- Python 3.11+, Pydantic v2 strict, mypy strict

# 本任务范围
- backend/app/domain/promise.py
- backend/app/domain/memory.py
- backend/app/domain/meta_progress.py
- backend/app/domain/__init__.py（追加导出）
- backend/tests/unit/test_domain_promise.py
- backend/tests/unit/test_domain_memory.py
- backend/tests/unit/test_domain_meta_progress.py
- backend/tests/factories.py（追加 build_promise / build_agent_memory / build_meta_progress / build_legacy_unlock）

# 必须实现的类型

## promise.py
1. class PromiseSource(StrEnum)：
   PRESS / BOARD_QA / DECISION_FLAVOR / FREE_TEXT

2. class PromiseTarget(BaseModel)：
   - metric: Literal["CASH","MORALE","BOARD","FACE","SALES_HINT"]（SALES_HINT 是发布会画饼专用）
   - target_expr: str  # 例 "+50%" / "翻倍" / ">= 70"
   - deadline_quarter: int | None = None

3. class Promise(BaseModel)：
   - id: str
   - quarter_made: int = Field(ge=1, le=4)
   - source: PromiseSource
   - text: str（≤ 80 字）
   - parsed: PromiseTarget | None = None  # 若正则无法解析则 None
   - fulfilled: bool | None = None  # None=未判定 / True=兑现 / False=失约
   - judged_at_quarter: int | None = None

## memory.py
1. class MemoryActor(StrEnum)：
   BOARD / EMPLOYEE / MEDIA / RIVAL

2. class MemoryEntry(BaseModel, frozen=True)：
   - id: str
   - actor: MemoryActor
   - actor_id: str | None = None  # 员工 id / 媒体 outlet / 董事 name；BOARD/MEDIA 总体时可为 None
   - quarter: int = Field(ge=1, le=4)
   - event_type: Literal["decision_seen","promise_made","promise_broken","layoff","press_quote","betrayal","favor"]
   - summary: str（≤ 80 字）
   - weight: int = Field(ge=1, le=5, default=3)  # 召回时排序用

3. class MemoryWindow(BaseModel)：（异步结算 prompt 构造时取最近 N 条）
   - entries: list[MemoryEntry]
   - max_size: int = 6
   - 方法 append(entry: MemoryEntry) -> MemoryWindow（返回新实例，按 quarter desc + weight desc 排序，截断到 max_size）

4. class ScheduledEvent(BaseModel, frozen=True)：
   - id: str
   - source_decision_id: str
   - fire_quarter: int  # 触发季
   - description: str (≤ 60 字)
   - effect_on_fire: StatsDelta
   - resolved: bool = False

## meta_progress.py
1. class LegacyType(StrEnum)：
   PR_EXPERIENCE / FUNDING_PITCH / ORG_KNOWHOW / PRODUCT_TASTE / MEDIA_NERVE / EMPLOYEE_TRUST / INDUSTRY_INTEL

2. class LegacyUnlock(BaseModel, frozen=True)：
   - type: LegacyType
   - label_zh: str (≤ 12 字)
   - description: str (≤ 60 字)
   - effect_summary: str (≤ 40 字，如"下轮发布会基础分 +5"，仅用于展示)
   - earned_at_run_id: str
   - earned_at_quarter: int

3. class ManagementStyle(StrEnum)：
   IRON_LAYOFF / STORY_MASTER / DATA_FREAK / FACE_KEEPER / SURVIVAL_PRO

4. class DeathLogEntry(BaseModel, frozen=True)：
   - run_id: str
   - company_name: str
   - business: str
   - died_at_quarter: int  # 1 表示 Q1 死，4 表示 Q4 活下来
   - death_reason: DeathReason | None  # None = 撑过 Q4
   - obituary: str (≤ 500 字)
   - biggest_mistake_decision_id: str | None
   - last_employee_quote: str | None
   - headlines: list[str] = Field(max_length=3)

5. class MetaProgress(BaseModel)：
   - player_id: str (uuid4)
   - schema_version: int = 1
   - unlocked_legacies: list[LegacyUnlock] = []
   - unlocked_styles: list[ManagementStyle] = []
   - death_log: list[DeathLogEntry] = []
   - press_archive: list[str] = []  # PressBundle.id
   - total_runs: int = 0
   - 方法 register_run_end(entry: DeathLogEntry, new_legacies: list[LegacyUnlock], new_styles: list[ManagementStyle]) -> MetaProgress（返回新实例，去重 legacies / styles）

# 测试
- test_domain_promise.py:
  · Promise.parsed 可为 None 合法
  · fulfilled 三态（None/True/False）
  · PromiseSource 4 类
- test_domain_memory.py:
  · MemoryWindow.append 排序 + 截断（构造 10 条 weight/quarter 混乱，append 后取出验证顺序与长度）
  · ScheduledEvent.resolved 默认 False
  · MemoryActor 4 类
- test_domain_meta_progress.py:
  · MetaProgress.register_run_end 去重（同 LegacyType 不重复）
  · total_runs 自增
  · DeathLogEntry.headlines max 3 条
  · LegacyType / ManagementStyle / LegacyUnlock 覆盖 DESIGN §13.4 / §13.5

# factories.py 追加
- build_promise(**overrides)
- build_agent_memory(**overrides)  # MemoryEntry
- build_scheduled_event(**overrides)
- build_legacy_unlock(**overrides)
- build_meta_progress(**overrides)

# 禁止事项
- 禁止把"promise 抽取正则"放进 domain（属 SettlementInputAggregator 或独立 promise_extractor，任务 14）。
- 禁止把"legacy 解锁规则"放进 domain（属 DeathReportService / LegacyService，任务 20）。
- 禁止 MemoryEntry 包含原始 LLM 输出（应该是摘要 ≤ 80 字）。
- 禁止 MetaProgress 反向依赖 service / api。
- 禁止把 schema_version 写死成 2 或更高（v1 是 1）。

# 验收标准
1. 单测全过。
2. ruff / mypy 零错误。
3. LegacyType 7 类对齐 DESIGN §13.4。
4. ManagementStyle 5 类对齐 DESIGN §13.5。
5. MemoryWindow.append 排序规则 quarter desc + weight desc。
6. MetaProgress.register_run_end 不重复添加同 LegacyType / 同 Style。
7. DeathLogEntry.headlines max 3。
8. Promise.fulfilled 三态合法。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动服务、未调 LLM、未连库"
````

### 预期产物

- `backend/app/domain/promise.py`
- `backend/app/domain/memory.py`
- `backend/app/domain/meta_progress.py`
- 3 个测试文件
- `factories.py` 追加 5 个工厂

### 验收标准

1. 单测全过；ruff / mypy 零错误。
2. `LegacyType` / `ManagementStyle` 对齐 DESIGN §13.4 / §13.5。
3. `MemoryWindow.append` 排序 quarter desc + weight desc，截断 max_size。
4. `MetaProgress.register_run_end` 去重。
5. `Promise.fulfilled` 三态合法。
6. `DeathLogEntry.headlines` ≤ 3。
7. domain 不反向依赖 service / api / llm。

### 禁止事项

- 禁止把 promise 抽取正则写进 domain。
- 禁止把 legacy 解锁规则写进 domain。
- 禁止 MemoryEntry 含原始 LLM 输出。
- 禁止 schema_version > 1。
- 禁止启动服务 / 调 LLM / 连库。

---

## 任务 6：协议层 — Envelope / Inbound / Outbound

### 使用场景

前端提示词库 §8 / §9 给了一套 ui.* 调用接口的形态；本任务把它落成正式的 WebSocket / REST 协议层（Pydantic 序列化），让前端在保留"纯 DOM"风格的同时可以从"裸 LLM 客户端"切换到"后端调用"。**任务 21 的 WS / REST 路由直接消费这套 schema**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》后端实现"前后端协议层"——Envelope + Inbound 消息 + Outbound 消息。

# 项目背景
前端是纯 DOM + ES Module，目前用 js/llm.js 直连 LLM，js/director.js / js/press.js 已实现 stub fallback。
现在要让前端可以**通过你的协议**与后端通信，最终替代裸 LLM。协议必须 1:1 覆盖前端 §8 main.js 编排里所有"await"边界：startNewGame / onCardPick / openPressPanel / runDirector / evaluatePress / settlement / death。
v1 4 季度 4 指标；只 Q3 触发发布会；transcript 文字模式。

# 共用规则
1. 只做后端，不写前端。
2. 不启动服务 / docker / 数据库。
3. 不调真实 LLM。
4. 不连真实数据库。
5. 实时期只记录不判断。
6. MVP 不做 prompt injection 防护。
7. 业务逻辑不写在路由（路由只做反序列化 + 调 service + 序列化）。
8. mock 可替换。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 代码 → 验证命令。

# 技术栈
- Python 3.11+, Pydantic v2 strict, FastAPI WebSocket 兼容

# 本任务范围
- backend/app/protocol/envelope.py
- backend/app/protocol/inbound.py
- backend/app/protocol/outbound.py
- backend/app/protocol/__init__.py（统一导出）
- backend/tests/unit/test_protocol_envelope.py
- backend/tests/unit/test_protocol_inbound.py
- backend/tests/unit/test_protocol_outbound.py

# 必须实现的类型

## envelope.py
1. class MessageDirection(StrEnum)：INBOUND / OUTBOUND

2. class Envelope[T](BaseModel)（泛型）：
   - v: int = 1  # 协议版本
   - id: str  # uuid4
   - ts: datetime  # 发送时间
   - direction: MessageDirection
   - type: str  # 消息类型，如 "create_game" / "settlement_bundle"
   - ack_for: str | None = None  # 若是 ack/响应，则填 inbound id
   - payload: T  # 具体消息体
   - 类方法 wrap(direction, msg) -> Envelope，自动填 type=msg.__class__.__name__ snake_case

3. class AckPayload(BaseModel)：
   - ok: bool
   - reason: str | None = None
   - retry_after_ms: int | None = None

## inbound.py（所有前端 → 后端消息）
1. class CreateGame(BaseModel)：
   - player_id: str | None = None  # None = 新匿名玩家
   - request_legacies: bool = True  # 是否应用 meta_progress 加成

2. class SelectDecision(BaseModel)：
   - session_id: str
   - quarter_number: int = Field(ge=1, le=4)
   - card_id: str

3. class CollectGossip(BaseModel)：
   - session_id: str
   - quarter_number: int
   - scene: Literal["tearoom","elevator","meeting_room","workstation","rooftop","smoking_area"]

4. class SubmitPress(BaseModel)：
   - session_id: str
   - quarter_number: int = 3  # v1 只 Q3
   - press_type: Literal["INAUGURATION","CRISIS","PRODUCT","FINANCIAL","ROADSHOW","LAYOFF_EXPLAIN","REGULATOR","COUNTER"]
   - transcript: str = Field(min_length=30, max_length=600)
   - duration_s: float | None = None

5. class SettleQuarter(BaseModel)：
   - session_id: str
   - quarter_number: int = Field(ge=1, le=4)

6. class RequestSnapshot(BaseModel)：（断线重连）
   - session_id: str

7. class Ping(BaseModel)：（心跳）
   - sent_at: datetime

# inbound.py 末尾导出 InboundMessage = Union[...]（discriminated union 用 type 字段）

## outbound.py（所有后端 → 前端消息）
1. class GameSnapshot(BaseModel)：
   - session_id: str
   - player_id: str
   - company: CompanyDTO  # 见下
   - stats: StatsDTO
   - quarter: QuarterDTO
   - history: list[HistoryEntryDTO] = []
   - meta_summary: MetaSummaryDTO  # 元进度精简版

2. class DecisionAck(BaseModel)：
   - session_id: str
   - quarter_number: int
   - card_id: str
   - immediate_stats: StatsDTO  # 应用 immediateEffect 之后的快照
   - next_phase: Literal["GOSSIP","DECISION","PRESS","SETTLEMENT","DONE"]

3. class GossipResult(BaseModel)：
   - session_id: str
   - quarter_number: int
   - lead: GossipLeadDTO  # 注意：is_truth/hidden_secrets 不出现
   - ap_remaining: int

4. class PressAck(BaseModel)：
   - session_id: str
   - quarter_number: int
   - accepted: bool
   - flags: list[str] = []  # ["brand_replaced","profanity_filtered"]
   - replaced_count: int = 0

5. class SettlementBundle(BaseModel)：
   - session_id: str
   - quarter_number: int
   - settlement: SettlementDTO  # 见下；不暴露 hidden_risks
   - press_bundle: PressBundleDTO | None = None
   - new_stats: StatsDTO
   - history_added: HistoryEntryDTO
   - death: DeathReasonDTO | None = None  # 若死亡

6. class DeathReportBundle(BaseModel)：
   - session_id: str
   - obituary: str
   - biggest_mistake_decision_id: str | None
   - last_employee: dict | None  # {"name":..., "quote":...}
   - headlines: list[str]
   - legacy_unlocks: list[LegacyUnlockDTO]
   - styles_unlocked: list[str]

7. class Toast(BaseModel)：（非阻塞提示）
   - level: Literal["info","warn","error"]
   - message: str
   - hint: str | None = None

8. class ErrorOutbound(BaseModel)：
   - code: str  # e.g. SESSION_NOT_FOUND / RATE_LIMIT / LLM_DEGRADED
   - message: str
   - retryable: bool

# DTO 设计原则（关键！）
- 每个 Outbound DTO 必须从 domain 模型转换而来（用 model_validator 或独立 to_dto() 函数）
- DTO 必须**显式过滤** WARN 字段：
  · Employee.hidden_secrets 永远不出现
  · GossipLead.is_truth 永远不出现
  · Briefing.hidden_risks 永远不出现
  · PressEvaluation.internal_eval 若存在永远不出现
- DTO 字段命名使用 camelCase（与前端 JS 习惯对齐），用 Pydantic alias_generator
- 但 Python 内部仍 snake_case；序列化时 by_alias=True

# 协议版本管理
- envelope.v = 1
- 新增 inbound/outbound 类型不递增 v
- 字段语义变更或字段删除递增 v
- 同时维持 v=1 路由直到 v=2 上线两个版本周期

# 测试
- test_protocol_envelope.py:
  · Envelope.wrap 自动填 type
  · 泛型序列化往返
  · ack_for 字段可选
- test_protocol_inbound.py:
  · SubmitPress transcript < 30 字拒绝
  · SubmitPress transcript > 600 字拒绝
  · CollectGossip.scene 非白名单拒绝
  · InboundMessage discriminated union 用 type 字段正确路由
- test_protocol_outbound.py:
  · GameSnapshot 序列化产物中不含 hidden_secrets / is_truth / hidden_risks / internal_eval
  · DTO 序列化后字段名 camelCase
  · SettlementBundle 在死亡时 death 字段非空，未死亡时为 None
  · 用 factory 构造 Domain 模型 → 转 DTO → 序列化 → 断言 WARN 字段不出现（这是最关键的一组测试）

# 禁止事项
- 禁止把业务逻辑放进 envelope / inbound / outbound（它们只是 schema）。
- 禁止 outbound DTO 直接 inherit domain Model（避免泄露 WARN 字段）。
- 禁止 inbound 包含 evaluation / settlement 等本应由后端产出的字段。
- 禁止协议层 import service / api / llm。
- 禁止使用 dict[str, Any]（必须强类型）。
- 禁止 transcript 字段长度超过 600（v1 上限）。

# 验收标准
1. 所有单测全过；ruff / mypy 零错误。
2. Outbound DTO 序列化产物用 set 校验不含任何 WARN 字段（写专门 architecture 级断言）。
3. Envelope 泛型在 mypy strict 通过。
4. InboundMessage discriminated union 可被 FastAPI 直接消费。
5. 字段命名外部 camelCase / 内部 snake_case。
6. SubmitPress.transcript 长度边界严格。
7. 协议层只 import domain，不 import service / api / llm。
8. v=1 写在 envelope 默认值。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动服务、未调 LLM、未连库"
````

### 预期产物

- `backend/app/protocol/envelope.py`
- `backend/app/protocol/inbound.py`
- `backend/app/protocol/outbound.py`
- `backend/app/protocol/__init__.py`
- 3 个单测 + 1 个 architecture-级 WARN 字段断言

### 验收标准

1. 单测全过；ruff / mypy 零错误。
2. Outbound DTO 序列化产物零 WARN 字段（专项断言）。
3. Envelope 泛型 mypy 通过。
4. `InboundMessage` discriminated union 可被 FastAPI 直接消费。
5. 外部 camelCase / 内部 snake_case。
6. `SubmitPress.transcript ∈ [30, 600]`。
7. 协议层只 import domain。
8. `envelope.v = 1`。

### 禁止事项

- 禁止把业务逻辑放进协议层。
- 禁止 outbound DTO 直接 inherit domain Model。
- 禁止 inbound 包含 evaluation / settlement 等后端产出字段。
- 禁止协议层 import service / api / llm。
- 禁止 `dict[str, Any]` 替代强类型。
- 禁止 transcript > 600。
- 禁止启动服务 / 调 LLM / 连库。

---

## 任务 7：仓储层 — in-memory + DB 占位 + 迁移计划

### 使用场景

v1 用纯内存仓储跑通，但必须用 `Protocol` / 抽象类把接口冻结，让任务 19 SettlementOrchestrator 和任务 20 DeathReportService 不依赖具体实现。同时为 v2 留 PostgreSQL（GameSession / MetaProgress） + Redis（短期缓存）占位与迁移文档。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》后端实现"仓储抽象 + 内存实现 + 数据库占位 + 迁移计划"。

# 项目背景
v1 用内存跑通，所有 service 都从 Repository Protocol 拿数据；v2 切 PostgreSQL（持久化 GameSession / MetaProgress）+ Redis（短期 session 缓存 + 限流）。本任务确保切换时不改 service 代码。

# 共用规则
1. 只做后端，不写前端。
2. 不启动服务 / docker / 数据库。
3. 不调真实 LLM。
4. 不连接真实数据库（SQLAlchemy 模型只声明，不执行迁移）。
5. 实时期只记录不判断。
6. MVP 不做 prompt injection 防护。
7. 业务逻辑不写在路由。
8. mock 可替换（这正是本任务的核心目标）。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 代码 → 验证命令。

# 技术栈
- Python 3.11+, Pydantic v2, typing.Protocol, asyncio
- SQLAlchemy 2.x（占位，不连接）

# 本任务范围
- backend/app/repo/protocols.py
- backend/app/repo/memory_impl.py
- backend/app/repo/sql_stub.py
- backend/app/repo/migration_plan.md
- backend/app/repo/__init__.py
- backend/tests/unit/test_repo_memory_session.py
- backend/tests/unit/test_repo_memory_meta.py
- backend/tests/unit/test_repo_memory_archive.py
- backend/tests/architecture/test_repo_protocol_compliance.py

# 必须实现的内容

## protocols.py（接口冻结）
所有方法 async；id 用 str。

1. class GameSession(BaseModel)：（聚合根，由后续 service 消费）
   - id: str
   - player_id: str
   - company: Company
   - stats: Stats
   - quarter: Quarter
   - employees: list[Employee]
   - history: list[HistoryEntry] = []
   - promise_log: list[Promise] = []
   - agent_memory: MemoryWindow = MemoryWindow(entries=[])
   - scheduled_events: list[ScheduledEvent] = []
   - status: Literal["active","dead","won"] = "active"
   - created_at: datetime
   - updated_at: datetime

2. class GameSessionRepo(Protocol)：
   - async create(session: GameSession) -> GameSession
   - async get(session_id: str) -> GameSession | None
   - async save(session: GameSession) -> GameSession  # 整体替换，返回 updated_at 已刷新
   - async list_by_player(player_id: str, limit: int = 20) -> list[GameSession]
   - async delete(session_id: str) -> bool

3. class MetaProgressRepo(Protocol)：
   - async get(player_id: str) -> MetaProgress  # 不存在时返回默认实例（不抛）
   - async save(meta: MetaProgress) -> MetaProgress
   - async list_recent_deaths(player_id: str, limit: int = 10) -> list[DeathLogEntry]

4. class PressArchiveRepo(Protocol)：
   - async append(session_id: str, bundle: PressBundle) -> str  # 返回 archive_id
   - async get(archive_id: str) -> PressBundle | None
   - async list_by_session(session_id: str) -> list[PressBundle]

5. class AgentMemoryRepo(Protocol)：（虽然 GameSession 自带 agent_memory，但跨 session 查询走这里——v2 用）
   - async append(player_id: str, session_id: str, entry: MemoryEntry) -> None
   - async fetch_window(player_id: str, max_size: int = 6) -> MemoryWindow

6. 工厂函数：
   - def get_session_repo() -> GameSessionRepo
   - def get_meta_repo() -> MetaProgressRepo
   - def get_press_archive_repo() -> PressArchiveRepo
   - def get_agent_memory_repo() -> AgentMemoryRepo
   通过 app.config.settings.env 切换（dev/test → memory_impl；prod → sql_stub 占位，目前抛 NotImplementedError 并 log warning）

## memory_impl.py
1. class InMemoryGameSessionRepo(GameSessionRepo)：
   - 内部 `_data: dict[str, GameSession]` + asyncio.Lock 保护写
   - save 自动更新 updated_at = datetime.utcnow()
   - delete 返回是否真删

2. class InMemoryMetaProgressRepo(MetaProgressRepo)：
   - get 不存在时 lazy-create 默认 MetaProgress(player_id=player_id)
   - 注意 list_recent_deaths 按 quarter desc 排

3. class InMemoryPressArchiveRepo(PressArchiveRepo)：
   - archive_id = "P-" + uuid4hex[:10]

4. class InMemoryAgentMemoryRepo(AgentMemoryRepo)：
   - 内部按 player_id 分桶
   - fetch_window 调 MemoryWindow.append 排序逻辑

5. 模块级单例（仅 dev/test 用）：
   - _session_singleton = InMemoryGameSessionRepo()
   - 工厂函数返回单例

## sql_stub.py
1. 声明 SQLAlchemy 2.x ORM 模型骨架（继承 DeclarativeBase）：
   - GameSessionORM（表 game_sessions：id PK / player_id / company_json JSONB / stats_json JSONB / quarter_json JSONB / status / created_at / updated_at）
   - MetaProgressORM（meta_progress：player_id PK / data_json JSONB / schema_version / total_runs）
   - PressArchiveORM（press_archive：id PK / session_id FK / bundle_json JSONB / created_at）
   - AgentMemoryORM（agent_memory：id PK / player_id / session_id / entry_json JSONB / quarter / actor / created_at）
2. 文件末尾 class SqlGameSessionRepo / 等占位 class，所有方法体：`raise NotImplementedError("SQL repo not implemented in v1; see migration_plan.md")`
3. 不创建任何 engine / session_maker / connection。

## migration_plan.md
≤ 200 行，分章节：
1. 现状（v1 in-memory）
2. v2 目标（PostgreSQL + Redis）
3. 数据模型映射（每个 ORM 对应哪个 Pydantic 聚合根）
4. 迁移步骤（Alembic 初始化 / 三张表创建 / 数据导入策略 / 灰度切换 / 回滚预案）
5. Redis 用途（限流 / WS 在线会话索引 / press transcript 限流）
6. 性能预算（v2 单 session save P99 < 50ms / meta_progress.get P99 < 20ms）
7. 测试策略（testcontainers / docker-compose 仅 CI 用）
8. 不引入 ORM 迁移代码的明确声明

## 测试
- test_repo_memory_session.py:
  · create + get 往返
  · save 后 updated_at 刷新
  · list_by_player 过滤正确
  · delete 返回 True/False
  · 并发 100 个 save 同 session 无丢失（asyncio.gather）
- test_repo_memory_meta.py:
  · get 不存在 → 返回默认 MetaProgress
  · save + get 往返
  · list_recent_deaths 按 quarter desc 排
- test_repo_memory_archive.py:
  · append 返回 archive_id 形如 P-...
  · list_by_session 顺序按 append 时间
- test_repo_protocol_compliance.py（架构断言）：
  · 用 typing.get_type_hints 检查 InMemory* 类签名完全实现 Protocol
  · 检查 sql_stub.* 类继承 Protocol（即便方法是 raise NotImplementedError）

# 禁止事项
- 禁止在 memory_impl 写任何业务逻辑（例如不能在 save 里调用 derive_mood）。
- 禁止 sql_stub 真的连接数据库 / 调用 create_engine。
- 禁止 protocols 反向 import memory_impl / sql_stub。
- 禁止 GameSession 包含 outbound DTO 字段（只用 domain 类型）。
- 禁止跨进程持久化（v1 是单进程内存）。
- 禁止把测试用的 _session_singleton 暴露给非 dev/test 环境（用 settings.env 检查）。

# 验收标准
1. 所有单测全过；ruff / mypy 零错误。
2. InMemory* 类 100% 实现 Protocol（架构断言通过）。
3. SqlStub* 类全部 raise NotImplementedError。
4. concurrent save 测试无数据丢失。
5. MetaProgressRepo.get 不存在时返回默认实例不抛。
6. migration_plan.md 包含 8 个章节。
7. protocols / memory_impl / sql_stub 都不 import service / api / llm。
8. settings.env != dev/test 时单例工厂返回 sql_stub 实例（即便后续会 raise NotImplementedError）。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码 + migration_plan.md
4. 验证命令
5. 声明"未启动服务、未调 LLM、未连库"
````

### 预期产物

- `backend/app/repo/protocols.py`（含 `GameSession` 聚合根 + 4 个 Repo Protocol）
- `backend/app/repo/memory_impl.py`（4 个内存实现）
- `backend/app/repo/sql_stub.py`（4 个 ORM 骨架 + 4 个占位 Repo class）
- `backend/app/repo/migration_plan.md`（8 章节）
- 3 个单测 + 1 个架构断言

### 验收标准

1. 单测全过；ruff / mypy 零错误。
2. InMemory* 100% 实现 Protocol（架构断言）。
3. SqlStub* 全部 `NotImplementedError`。
4. concurrent save 无数据丢失。
5. `MetaProgressRepo.get` 不存在返回默认实例。
6. `migration_plan.md` 含 8 章节。
7. `protocols` / `memory_impl` / `sql_stub` 都不 import service / api / llm。
8. 工厂函数按 `settings.env` 切换。

### 禁止事项

- 禁止在 `memory_impl` 写业务逻辑（如 `save` 内调 `derive_mood`）。
- 禁止 `sql_stub` 真连数据库 / 调 `create_engine`。
- 禁止 `protocols` 反向 import `memory_impl` / `sql_stub`。
- 禁止 `GameSession` 包含 outbound DTO 字段。
- 禁止跨进程持久化。
- 禁止启动服务 / 调 LLM / 连库。

---

## 任务 8：内容注册中心 — 业务/决策/员工/发布会池

### 使用场景

DESIGN §13.2（10 类公司业务）+ §5.2（12 类决策卡）+ §9.3（12 名员工模板）+ §8.1（8 类发布会类型，v1 只用 inauguration / crisis）+ §9.5（6 个八卦场景）的"内容数据"必须从代码逻辑里剥离，让策划/QA 可以独立维护。**任务 9 CompanyService 抽公司、任务 11 DecisionService 抽卡、任务 12 GossipService 选线索池都从这里取**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》后端实现"内容注册中心"——把公司业务/决策卡/员工模板/发布会类型/八卦池等可拓展数据集中管理。

# 项目背景
策划会频繁调整这些数据。要避免散在 service 里，必须建一个 content registry，每类资源都暴露 get_all / get_by_id / sample(n, rng_seed) 三个接口。
v1 各类先填一组样本：公司 6 个、决策卡 12 个、员工模板 12 个、发布会类型 8 个、八卦场景 6 个 × 每场景 4 条线索模板。

# 共用规则
1. 只做后端，不写前端。
2. 不启动服务 / docker / 数据库。
3. 不调真实 LLM。
4. 不连真实数据库。
5. 实时期只记录不判断（八卦线索是模板，不在此调 LLM）。
6. MVP 不做 prompt injection 防护。
7. 业务逻辑不写在路由。
8. mock 可替换。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 代码 → 验证命令。

# 技术栈
- Python 3.11+, Pydantic v2 strict

# 本任务范围
- backend/app/content/companies.py
- backend/app/content/decisions.py
- backend/app/content/employees.py
- backend/app/content/press_types.py
- backend/app/content/gossip_pool.py
- backend/app/content/__init__.py
- backend/tests/unit/test_content_*.py（每个模块一个测试）

# 必须实现的内容

## companies.py
1. COMPANY_TEMPLATES: list[dict]（6 条）——基于 DESIGN §13.2 抽 6 条，每条字段：
   - template_id（C-01 到 C-06）
   - name_pool: list[str]（每个公司 2–3 个候选名字，开局随机选 1）
   - business（如 "AI 情绪冰箱"）
   - absurdity（1–5）
   - founding_motto
   - death_causes（2–3 条 dict {category, description}）
   - starting_promises（前任 CEO 已破承诺，0–3 条字符串）
   - stats_modifiers（dict CASH/MORALE/BOARD/FACE → 偏移量，叠加到 Stats.starting() 上，[-10,+10]）

2. def get_company_templates() -> list[dict]
3. def get_company_template_by_id(template_id: str) -> dict | None
4. def sample_company_template(rng_seed: int | None = None) -> dict

## decisions.py
1. DECISION_CARDS: list[dict]（12 条，对齐 DESIGN §5.2 12 类）每条：
   - id（D_FUNDING_01 等）
   - category（任务 4 的 DecisionCategory enum 值）
   - title / description / flavor / immediate_effect / long_term_hint
   - boomerang_seeds（0–2 条）
   - applicable_quarters: list[int]（哪些季度可抽到，v1 默认 [1,2,3,4]）
   - applicable_when: list[str]（前置条件标签，如 "CASH<30"，本任务先字符串存，DecisionService 任务 11 解析）

2. def get_all_decision_cards() -> list[dict]
3. def get_decision_card_by_id(card_id: str) -> dict | None
4. def sample_decision_cards(n: int = 3, quarter: int = 1, current_stats: Stats | None = None, rng_seed: int | None = None) -> list[dict]
   - 过滤 applicable_quarters 包含 quarter
   - 若 current_stats 非空，过滤 applicable_when 不满足的卡（简单字符串解析：支持 "CASH<X" "MORALE>X" 等）
   - 从剩余池随机抽 n（不重复）；若候选不足 n，重复抽并打 warn log

## employees.py
1. EMPLOYEE_TEMPLATES: list[dict]（12 条对齐 DESIGN §9.3）每条：
   - template_id（E-T-01 到 E-T-12）
   - role（CEO 助理 / CFO / CTO / 产品负责人 / HR 总监 / 销售总监 / 核心工程师 A / 核心工程师 B / 摸鱼员工 / 激进员工 / 老臣员工 / 叛逃种子员工）
   - personality_tag / faction
   - competence_range / loyalty_range / stress_range（[min, max] int 区间）
   - name_pool（每条 3–5 个候选中文姓名）
   - relationships_seed（list[dict]，描述与其他 template_id 的关系类型；具体 target_id 由 CompanyService 任务 9 在生成员工后回填）
   - hidden_secrets_pool: list[str]（0–3 条候选，开局随机抽 0–2 条赋给该员工）
   - quitting_risk_template: str | None  # ≤ 30 字提示后端 service 是否高离职风险，仅文档用

2. def get_employee_templates() -> list[dict]
3. def get_employee_template_by_id(id) -> dict | None
4. def sample_employee_set(rng_seed: int | None = None) -> list[dict]  # 返回 12 个员工模板（v1 固定 12 人，将来可配置）

## press_types.py
1. PRESS_TYPES: dict[str, dict]（8 类对齐 DESIGN §8.1）每个值：
   - title_zh
   - trigger_condition_text（≤ 60 字描述，仅文档；不是代码触发逻辑）
   - must_answer_topics: list[str]（3 条）
   - difficulty: int = Field(ge=1, le=5)
   - press_keywords: list[str]（命中检查用）

2. def get_press_type_by_id(press_id: str) -> dict | None
3. def list_press_types() -> list[dict]
4. def get_default_press_for_quarter(q: int) -> str | None
   v1 规则：q==1 → INAUGURATION / q==3 → CRISIS / 其他 → None
   （v1 严格遵守：只 Q3 触发，但 Q1 教程性发布会暂时禁用，留接口）

## gossip_pool.py
1. GOSSIP_TEMPLATES: dict[scene → list[dict]] 6 场景每场景 4–6 条线索模板，每条：
   - template_id（G-tearoom-01 等）
   - scene
   - text_template（≤ 60 字，含 {speaker} {role} 等 placeholder）
   - reliability_distribution: dict（{"RUMOR": 0.3, "LIKELY": 0.5, "CONFIRMED": 0.2}）——决定抽到的可信度分布
   - truth_probability: float（[0,1]，决定 is_truth 概率）
   - ap_cost: int = 1
   - linked_role_tags: list[str]（如 ["CFO","HR"]，service 选员工时按 role 匹配）

2. def get_gossip_templates_by_scene(scene: str) -> list[dict]
3. def sample_gossip_lead(scene: str, employees: list[Employee], rng_seed: int | None = None) -> dict
   - 按场景模板池抽 1 条
   - 按 reliability_distribution 抽 reliability
   - 按 truth_probability 决定 is_truth
   - 按 linked_role_tags 选 speaker（无匹配则随机选一个）
   - 渲染 text_template 占位
   - 返回 dict（不返回 GossipLead 实例，let GossipService 任务 12 包装）

## __init__.py
统一导出 4 个 sample_* + 3 个 get_* + 模板常量

## 测试
- test_content_companies.py: 6 条模板字段完整；sample 可复现
- test_content_decisions.py:
  · 12 类 category 全覆盖
  · sample_decision_cards(n=3) 返回 3 张不重复
  · current_stats={CASH:25} 时不返回 applicable_when="CASH>30" 的卡
- test_content_employees.py: 12 条覆盖 12 个 role；name_pool 至少 3 个
- test_content_press_types.py: 8 类齐全；get_default_press_for_quarter(3)==CRISIS；q==2 返回 None
- test_content_gossip_pool.py:
  · 6 场景齐全
  · sample_gossip_lead reliability 按分布偏差 < 5% (跑 1000 次统计)
  · speaker_id 落在传入的 employees 范围

# 禁止事项
- 禁止把"开局抽取" service 逻辑（如随机决定首季公司）塞进 content（属 CompanyService）。
- 禁止 sample_decision_cards 调用 LLM。
- 禁止 GOSSIP_TEMPLATES.text_template 在此模板中生成完整中文（必须保留 placeholder）。
- 禁止用 Python pickle / json 文件读取（v1 直接 Python literal；策划改了走 PR）。
- 禁止 content 反向 import service / api / llm。
- 禁止把 hidden_secrets 暴露在 GOSSIP 输出（hidden_secrets 是 Employee 独有，gossip text 可以暗示但不能照搬）。

# 验收标准
1. 所有单测全过；ruff / mypy 零错误。
2. 内容池数量与上述完全一致（公司 6 / 决策 12 / 员工 12 / 发布会 8 / 八卦 24+）。
3. sample_* 接受 rng_seed 可复现。
4. v1 PressType 触发规则严格：仅 Q3 返回 CRISIS。
5. sample_decision_cards 过滤 applicable_when 正确。
6. content 模块零反向依赖 service / api / llm。
7. text_template 含 placeholder（{speaker} / {role}）。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码（每个模板字段填齐）
4. 验证命令
5. 声明"未启动服务、未调 LLM、未连库"
````

### 预期产物

- `companies.py / decisions.py / employees.py / press_types.py / gossip_pool.py` 各含模板常量 + sampler 函数
- `content/__init__.py` 统一导出
- 5 个单测文件

### 验收标准

1. 单测全过；ruff / mypy 零错误。
2. 模板数量精确：公司 6 / 决策 12 / 员工 12 / 发布会 8 / 八卦 24+。
3. `sample_*` 接受 `rng_seed` 可复现。
4. v1 仅 Q3 返回 CRISIS。
5. `sample_decision_cards` 过滤 `applicable_when` 正确。
6. content 模块零反向依赖。
7. text_template 保留 placeholder。

### 禁止事项

- 禁止 sampler 调 LLM。
- 禁止把"开局生成" service 逻辑塞进 content。
- 禁止用文件读取（v1 Python literal 即可）。
- 禁止 content 反向 import service / api / llm。
- 禁止把 hidden_secrets 直接拼进 gossip text。
- 禁止启动服务 / 调 LLM / 连库。

---

## 任务 9：CompanyService — 开局生成

### 使用场景

前端发 `CreateGame` 后端创建一个新的 `GameSession`：从 `content.companies` 抽公司模板 → 实例化 `Company` → 从 `content.employees` 抽员工集合 → 回填员工关系 → 计算初始 `Stats`（叠加模板 modifier + meta_progress legacy buff） → 写入 `GameSessionRepo`。**这是玩家进入游戏的第一个后端动作**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是 Python 后端工程师。要为《YES, BOSS!》后端实现 CompanyService —— 处理"创建新一局"的全部领域逻辑。

# 项目背景
玩家点"开始游戏" → 前端发 Inbound.CreateGame → 后端 CompanyService.create_new_run(player_id, apply_legacies=True)：
1. 从 content.companies 抽公司模板（rng）
2. 实例化 Company（id=uuid4，name 从 name_pool 抽）
3. 从 content.employees 抽 12 名员工模板 → 实例化 Employee（id 自动）
4. 回填 employee.relationships：把 relationships_seed 里的 template_id 替换成实际 employee.id
5. 调 Stats.starting(seed) + 叠加 company_template.stats_modifiers
6. 若 apply_legacies=True 且 meta_progress 已有相关 legacy，再叠加 legacy buff（DESIGN §13.4）
7. 构造 Quarter(number=1, phase=BRIEFING) + 一份 Briefing（market_mood 抽样）
8. 构造 GameSession 写入 GameSessionRepo
9. 返回 GameSession（供任务 21 包装成 GameSnapshot DTO）

# 共用规则
1. 只做后端，不写前端。
2. 不启动服务 / docker / 数据库。
3. 不调真实 LLM（整个 CompanyService 实时期，绝对不调）。
4. 不连真实数据库（走 InMemoryRepo）。
5. 实时期只记录不判断。
6. MVP 不做 prompt injection 防护。
7. 业务逻辑写在 service，不要写进 api / domain / content。
8. mock 可替换（接受 repo 注入）。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 代码 → 验证命令。

# 技术栈
- Python 3.11+, asyncio, Pydantic v2

# 本任务范围
- backend/app/services/company_service.py
- backend/app/services/__init__.py（导出）
- backend/tests/unit/test_service_company.py
- backend/tests/integration/test_create_new_run.py

# 必须实现的类型

## company_service.py
1. class CompanyService：
   - 构造函数：
     def __init__(
         self,
         session_repo: GameSessionRepo,
         meta_repo: MetaProgressRepo,
         rng_factory: Callable[[int | None], random.Random] = lambda s: random.Random(s),
     )

   - async def create_new_run(
         self,
         player_id: str | None = None,
         apply_legacies: bool = True,
         rng_seed: int | None = None,
     ) -> GameSession:
     1. 若 player_id 是 None → 新生成 uuid4
     2. 抽公司模板 + Company 实例化（name 从 name_pool 随机抽）
     3. 抽员工模板列表 + 实例化 Employee 列表（先无 relationships）
     4. 回填 relationships（用 _resolve_relationships 私有方法把 template_id 转 employee.id）
     5. 初始 Stats（_compute_initial_stats，叠加 modifier + legacy buff）
     6. 初始 Quarter(number=1, phase=BRIEFING, briefing=_make_briefing(...))
     7. 构造 GameSession 实例
     8. session_repo.create(session)
     9. return session

   - 私有方法（同模块下定义）：
     - _resolve_relationships(employees: list[Employee], templates: list[dict]) -> list[Employee]
     - _compute_initial_stats(company_template, meta: MetaProgress, apply_legacies, rng) -> Stats
       · base = Stats.starting(seed)
       · base = base.apply_delta(StatsDelta(**company_template["stats_modifiers"]))
       · if apply_legacies: 遍历 meta.unlocked_legacies → 按 LegacyType 加 buff（PR_EXPERIENCE → FACE+5 等；与 DESIGN §13.4 一致）
     - _make_briefing(quarter: int, company: Company, rng) -> Briefing
       · market_mood 抽样（v1 概率：bull 0.25 / neutral 0.4 / bear 0.25 / crisis 0.1）
       · headline_hint 模板（≤ 60 字，引用公司业务名）
       · hidden_risks 抽样（基于 death_causes 投射 1–2 条；本季玩家可见提示是 headline_hint，hidden_risks 仅后端用）

2. 异常：
   - if 模板抽样失败抛 ContentRegistryError
   - 不要 catch 后默默吞掉

# 测试

## test_service_company.py（纯单元）
- create_new_run 返回的 GameSession.status=="active"
- quarter.number==1 且 phase=BRIEFING
- 员工数 == 12
- 所有员工 id 唯一
- relationships.target_id 均落在 employees.id 集合内（不会有悬空 id）
- stats 在 [0,100] 区间
- apply_legacies=True 且 meta 含 PR_EXPERIENCE → FACE 高于无 legacy 版本至少 5
- 同一 rng_seed 连续两次调用产物相同（公司名/员工集/stats 全等）
- 不同 rng_seed 产物不同（10 次抽样不重复率 > 80%）
- player_id=None 自动生成 uuid4 且持久化（meta_repo.get(player_id) 不再返回默认）

## test_create_new_run.py（集成）
- 用真实 InMemoryGameSessionRepo + InMemoryMetaProgressRepo
- create_new_run → session_repo.get(session.id) 能取出 == 原对象
- 同 player_id 连续 create 3 次 → list_by_player 返回 3 条

# 关键架构断言（写在测试里）
- 整个 CompanyService 执行过程 0 次 LLM 调用（mypy + 静态扫描：grep 任何 llm_client / chat_complete 字样应为 0）
- CompanyService 不 import app.llm.* / app.api.* / app.protocol.*

# 禁止事项
- 禁止在 CompanyService 里调用 LLM 生成公司名、员工名、slogan（v1 全部走 content registry 模板）。
- 禁止在 service 里访问 content 数据时跨过 sampler 直接抓 dict（必须用 content.* sample_* 函数）。
- 禁止把 Briefing.hidden_risks 暴露到 outbound（任务 21 路由层负责过滤；本任务只确保字段存在）。
- 禁止 CompanyService 反向 import api / protocol / llm。
- 禁止把 rng 全局实例化（必须通过 rng_factory 注入，方便测试）。
- 禁止把 12 员工硬编码为变量（必须从 content.employees.sample_employee_set 抽）。

# 验收标准
1. 所有单测 + 集成测全过；ruff / mypy 零错误。
2. 0 次 LLM 调用断言通过。
3. 同 rng_seed 完全可复现。
4. relationships.target_id 无悬空。
5. legacy 加成数值与 DESIGN §13.4 对齐。
6. 异常路径（content 抽样失败）抛 ContentRegistryError。
7. CompanyService 不 import app.llm / app.api / app.protocol。
8. session 写入后能 get 回来。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动服务、未调 LLM、未连库"
````

### 预期产物

- `backend/app/services/company_service.py`
- `services/__init__.py` 导出
- 单测 + 集成测

### 验收标准

1. 单测 / 集成测全过；ruff / mypy 零错误。
2. 0 次 LLM 调用断言通过。
3. 同 rng_seed 完全可复现。
4. `relationships.target_id` 无悬空。
5. legacy 加成与 DESIGN §13.4 对齐。
6. `ContentRegistryError` 在抽样失败时抛出。
7. CompanyService 不 import `app.llm / app.api / app.protocol`。
8. session 写入后能 get 回来。

### 禁止事项

- 禁止在 CompanyService 调 LLM 生成任何文本。
- 禁止跨过 sampler 直接读 content dict。
- 禁止 rng 全局单例（必须 inject）。
- 禁止 12 员工硬编码。
- 禁止暴露 hidden_risks。
- 禁止启动服务 / 连库。

---
