# 《YES, BOSS!》前端任务型提示词库 v2.0（像素风版）

> **使用对象**：前端工程师（React / TypeScript / Tailwind / 像素风视觉）
> **使用方式**：把对应任务下「可直接复制给 AI 编程工具的完整提示词」整段复制到 Cursor / Claude Code / Windsurf / Augment / Copilot Agent 执行
> **配套文档**：`DESIGN(3).md`、`AI-Agent提示词库.md`、`后端提示词库.md`、11 张原型图
> **版本范围**：v2 像素风原型 = **4 季度核心循环**（v1 数据流 4 指标 `CASH/MORALE/BOARD/FACE`；UI 预留 6 指标槽位以兼容原型图视觉），与后端 22 条任务**协议对齐**
> **更新日期**：2026-05-24

---

## 序章 · 红线与铁律

### 0.1 项目一句话

你正在为《YES, BOSS!》—— "濒死公司 CEO 求生模拟器"——做**前端**。
后端会并行开发（FastAPI + WebSocket，见后端提示词库 22 条任务）。你**先用 mock 数据跑通完整体验**，最后通过 WebSocket Envelope 协议接入后端，**零业务逻辑变更**。

### 0.2 视觉风格定义（铁律）

**风格**：8-bit / 16-bit **像素风**（Pixel Art），整体参考：

- 《精灵宝可梦》红白机的对话框 / 文字气泡
- 《星露谷物语》的室内俯视瓦片 + 角色对话立绘
- 《Stardew Valley》的菜单 UI 网格感

**核心视觉元素**：

| 元素 | 规则 |
|---|---|
| 网格 | 4px / 8px / 16px 三档对齐；所有间距 / padding / border 为 4 的倍数 |
| 边框 | 实心 2px 像素描边（无圆角，或仅 1px 像素圆角阶梯） |
| 阴影 | 4px 实色硬阴影（无渐变 / 无 blur），方向统一右下 |
| 字体 | 中文 = **Fusion Pixel 12px**（开源像素体）；英文 / 数字 = **Press Start 2P** |
| 渲染 | 所有像素图标 / 角色立绘 / 瓦片必须设 `image-rendering: pixelated`；缩放仅用整数倍 |
| 颜色 | 限制调色板（≤ 32 色）；不使用 hex 渐变；危机色用 alarm-red 闪烁 |
| 动画 | 整数像素步进；无 ease-out 曲线；打字机 30ms/字；数字滚动 400ms 阶梯 |

**严禁**：

- 圆形头像 / 圆角按钮（即便组件库默认有，也要 override）
- 任何高斯模糊 / 渐变背景 / 半透明蒙版
- Material Design / iOS 风格的弹性动效
- emoji 充当 UI 主图标（emoji 仅可作为对话气泡内的情绪表达）

### 0.3 技术栈固定

```
Vite + React 18 + TypeScript (strict)
Tailwind CSS 3.4+
8bitcn-ui            通过 shadcn CLI 注册表添加（非 npm 包）
                     pnpm dlx shadcn@latest add @8bitcn/<component>
                     导入：import { Button } from "@/components/ui/8bit"
                     已知 50 组件 + 4 游戏化（health-bar / mana-bar / xp-bar /
                     enemy-health-display）
pixelarticons        npm install pixelarticons（800 图标，24×24，currentColor）
                     用法：import { Heart } from "pixelarticons/react/Heart"
Zustand 4            状态管理
Framer Motion        仅用作 enter/exit 简单淡入；禁止用作复杂物理动画
WebSocket 原生        与后端 /api/v1/ws/{player_id} 通信
vitest               单元测试
playwright           最关键 3 个 happy path 端到端
```

### 0.4 与后端的协议对齐（铁律）

- 协议层 schema 与后端任务 6 一致：`Envelope<T> { v, id, ts, direction, type, ack_for?, payload }`
- **外部 camelCase**（与后端 `by_alias=True` 一致）；内部 TS 类型 camelCase
- **WARN 字段前端永远收不到**（`hidden_secrets / is_truth / hidden_risks / internalEval`）
- 协议版本：v=1；新增字段不递增

### 0.5 实时期 / 异步期分离（铁律）

```
┌──────────── 实时期（前端 → 后端，毫秒级） ────────────┐
│ create_game / select_decision / collect_gossip /     │
│ submit_press → 收到 ACK envelope 立即继续             │
│                                                      │
│ 前端永远不调用 LLM（v2 不再像 v1 让浏览器直连 LLM）  │
└─────────────────────────────────────────────────────┘
                          ↓
┌──────────── 异步期（后端结算，1–10s） ────────────────┐
│ settle_quarter → 前端立即收 PressAck → 显示          │
│   "AI 正在结算..." 骨架屏 → 之后异步推送             │
│   SettlementBundle envelope                          │
└─────────────────────────────────────────────────────┘
```

- 前端绝不允许直连 LLM
- 前端绝不在实时期阻塞 UI 等异步结果
- `llm_degraded=true` 时 UI 顶部标 "AI 失联（兜底文案）"

### 0.6 v1 数据 vs 原型图视觉的取舍

原型图展示了**最多 6 指标**（含 SALES / MKT），但 v1 后端只回 4 指标 `CASH/MORALE/BOARD/FACE`。

**前端策略**：

```ts
const METRIC_KEYS_V1 = ["CASH", "MORALE", "BOARD", "FACE"] as const;
const METRIC_KEYS_V2_PREVIEW = ["SALES", "MKT"] as const;  // 灰显占位
```

- HUD / Dashboard 渲染 6 个卡槽，但 v1 阶段后两个标记 "v2 即将开放" 灰显
- Settlement / DeathReport 只展示 v1 收到的字段
- 任何"渲染未收到的指标"的代码都要写 `if (env.NEXT_PUBLIC_METRIC_V2 === "on")` 保护

### 0.7 屏幕清单（11 个原型图对应）

| # | 屏幕 ID | 原型图 | 类型 | 入口 |
|---|---|---|---|---|
| 1 | `OnboardingScreen` | Image 1 / 2 | 一次性 | 应用启动（无存档时） |
| 2 | `CompanySelectScreen` | Image 9 / 11 | 一次性 | "开始新一局" 触发 |
| 3 | `OfficeScene` | Image 3 | 主场景 | 每季度 BRIEFING / GOSSIP 阶段 |
| 4 | `GossipScene` | Image 4 | 子场景 | 在 OfficeScene 点击场景图块进入 |
| 5 | `StatsDashboard` | Image 5 | 子屏 | 顶部 HUD "更多" 或 主按钮 "公司管理" |
| 6 | `DecisionScreen` | Image 8 / 10 | 阶段屏 | 每季度 DECISION 阶段 |
| 7 | `PressScreen` | (DESIGN §8) | 阶段屏 | Q3 PRESS 阶段（原型图未提供，按 DESIGN 实现） |
| 8 | `SettlementScreen` | Image 12 | 阶段屏 | 每季度 SETTLEMENT 阶段 |
| 9 | `OverviewScreen` | Image 7 | 子屏 | "公司管理" → 经营总览 |
| 10 | `DeathReportScreen` | Image 6 | 终结屏 | session.status 转 dead/won |
| 11 | `LegacyVaultScreen` | (DESIGN §13.4) | 跨局元进度 | DeathReport "带遗产再来一轮" |

### 0.8 全局 Layout（每个屏幕都遵守）

```
┌─────────────────────────────────────────────────────────────┐
│  [Logo] [日期]   [💰 CASH] [😊 MORALE] [🛡 BOARD] [😎 FACE] │  ← HUDBar (高 64px)
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                   <屏幕主体>                                  │
│                                                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [公司管理] [员工沟通] [项目推进] [财务决策]                  │  ← MainActionBar (高 72px)
└─────────────────────────────────────────────────────────────┘
```

- HUDBar / MainActionBar 全屏幕共用（除 Onboarding / DeathReport 外可隐藏 MainActionBar）
- 屏幕主体区域：1280×640（在 1280×720 / 1366×768 / 1920×1080 都用整数缩放）

### 0.9 共用规则（每条任务的"完整提示词"都内置）

1. 这是《YES, BOSS!》前端 v2 像素风原型。
2. 后端 22 条任务并行开发，最终通过 WebSocket Envelope 协议接入。
3. 当前任务只做前端，不动后端文件。
4. 不启动后端服务（前端启 `pnpm dev` 跑 mock 模式即可）。
5. **前端永远不调真实 LLM**（v1 直连模式废弃）。
6. 实时期 UI 操作不阻塞等待异步结果；异步结果到来时 push 更新。
7. 视觉严格遵守像素风（§0.2 铁律）。
8. WARN 字段从协议层就不会收到，**禁止前端展示任何 `hidden_*` / `internal_*` 字段**。
9. 业务状态全部在 Zustand store；组件只负责渲染 + 派发 action。
10. mock 与 ws 两种数据源切换由环境变量 `VITE_DATA_SOURCE=mock|ws` 控制。
11. 8bitcn-ui 组件优先；找不到再手写（保持像素风一致）。
12. pixelarticons 优先；缺图标用 SVG 手绘 24×24。
13. 单文件 > 300 行就拆。
14. 输出顺序：先说明计划 → 列出修改文件 → 给验证命令（`pnpm dev` / `vitest` / `tsc --noEmit` / `pnpm lint`）。

### 0.10 调色板（开局即固定，禁止扩展）

```
背景层
  bg-canvas        #F4F2EB    主背景（米黄）
  bg-panel         #FFFFFF    卡片底
  bg-panel-dim     #ECE9DF    禁用 / 灰显
  bg-floor         #DCD7C7    办公室地板

文字层
  ink-1            #1B1B1B    主文字
  ink-2            #4A4A4A    次文字
  ink-3            #8C8C8C    弱文字 / 提示

主操作色（4 大按钮 + 状态卡）
  pixel-blue       #2E6FE6    管理 / 中性正向
  pixel-green      #3DAE5C    沟通 / 健康
  pixel-orange     #F39A2B    推进 / 警告
  pixel-red        #D9412C    决策 / 危险

危机色
  alarm-red        #E63946    死亡阈值
  caution-yellow   #F5C518    黄色预警

游戏化
  exp-gold         #FFD23F    经验 / 声望
  legacy-purple    #7B5BE6    遗产

像素描边
  stroke-ink       #1B1B1B    所有 2px 描边
  shadow-hard      #1B1B1B    4px 实色硬阴影
```

### 0.11 字体加载

- 中文：**Fusion Pixel 12px** （https://github.com/TakWolf/fusion-pixel-font）
  - 用 `@font-face` 加载本地 `public/fonts/fusion-pixel-12px-monospaced-zh_hans.woff2`
  - `font-family: "Fusion Pixel", monospace;`
- 英文 / 数字：**Press Start 2P**（Google Fonts，8bitcn-ui 已自动 @import）
- 全站 base font-size = 12px；标题级 16 / 20 / 24 / 32（必须 4 的倍数）

### 0.12 目录约定

```
frontend/
├── public/
│   ├── fonts/                         # Fusion Pixel
│   ├── sprites/                       # 角色立绘 + 瓦片 + 静物
│   └── audio/                         # 可选音效
├── src/
│   ├── main.tsx
│   ├── App.tsx                        # ScreenRouter
│   ├── components/
│   │   ├── ui/8bit/                   # 8bitcn-ui 添加产物
│   │   ├── pixel/                     # 自定义像素组件
│   │   │   ├── PixelButton.tsx
│   │   │   ├── PixelCard.tsx
│   │   │   ├── PixelIcon.tsx
│   │   │   ├── PixelSpeechBubble.tsx
│   │   │   ├── PixelPortrait.tsx
│   │   │   ├── PixelLineChart.tsx
│   │   │   ├── PixelBarChart.tsx
│   │   │   ├── PixelMetricCard.tsx
│   │   │   └── PixelDialog.tsx
│   │   ├── layout/
│   │   │   ├── AppShell.tsx
│   │   │   ├── HUDBar.tsx
│   │   │   └── MainActionBar.tsx
│   │   └── screens/
│   │       ├── OnboardingScreen.tsx
│   │       ├── CompanySelectScreen.tsx
│   │       ├── OfficeScene.tsx
│   │       ├── GossipScene.tsx
│   │       ├── StatsDashboard.tsx
│   │       ├── DecisionScreen.tsx
│   │       ├── PressScreen.tsx
│   │       ├── SettlementScreen.tsx
│   │       ├── OverviewScreen.tsx
│   │       ├── DeathReportScreen.tsx
│   │       └── LegacyVaultScreen.tsx
│   ├── store/
│   │   ├── gameStore.ts               # Zustand
│   │   ├── screenStore.ts             # ScreenRouter 枚举切换
│   │   └── networkStore.ts            # 连接状态
│   ├── protocol/
│   │   ├── envelope.ts                # 与后端任务 6 对齐
│   │   ├── inbound.ts
│   │   ├── outbound.ts
│   │   └── types.ts                   # GameSnapshot/StatsDTO 等
│   ├── net/
│   │   ├── wsClient.ts                # WebSocket 客户端
│   │   ├── mockAdapter.ts             # 与 ws 同接口
│   │   └── dataSource.ts              # 工厂：ws 或 mock
│   ├── mock/
│   │   ├── fixtures/                  # 完整 4 季度 mock 数据
│   │   └── scenarios/                 # happy / die-q1 / press-fail
│   ├── theme/
│   │   ├── palette.ts                 # §0.10 调色板
│   │   ├── tokens.ts                  # 间距/字号常量
│   │   └── tailwind.config.ts
│   ├── styles/
│   │   ├── pixel.css                  # @font-face + image-rendering
│   │   └── globals.css
│   ├── anim/
│   │   ├── typewriter.ts
│   │   ├── numberRoll.ts
│   │   └── pulse.ts
│   └── utils/
│       ├── format.ts
│       └── audio.ts
├── tests/
│   ├── unit/
│   ├── component/
│   └── e2e/                           # playwright
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── vite.config.ts
├── vitest.config.ts
├── playwright.config.ts
├── .eslintrc.cjs
└── README.md
```

### 0.13 任务总览（22 条）

| # | 任务 | 涉及 |
|---|---|---|
| 1 | 项目初始化（Vite + React + 8bitcn-ui CLI + pixelarticons + Zustand） | Infra |
| 2 | 像素设计 token + Tailwind theme + 字体加载 | Theme |
| 3 | AppShell + ScreenRouter + 屏幕枚举 | Layout |
| 4 | Zustand store + 屏幕状态 + localStorage 持久化 | Store |
| 5 | 协议适配层（Envelope / Inbound / Outbound + wsClient + mockAdapter） | Net |
| 6 | PixelButton + PixelCard + PixelDialog 基础组件 | UI |
| 7 | PixelIcon wrapper（pixelarticons + 像素感缩放） | UI |
| 8 | PixelSpeechBubble + PixelPortrait（对话气泡 + 立绘） | UI |
| 9 | PixelMetricCard + PixelLineChart / PixelBarChart（像素图表） | UI |
| 10 | HUDBar（顶部 - Logo + 日期 + 4–6 指标） | Layout |
| 11 | MainActionBar（底部 - 4 大彩色按钮 + 上下文徽标） | Layout |
| 12 | OnboardingScreen（开场任命书 + 角色立绘） | Screen |
| 13 | CompanySelectScreen（空降 CEO 3 选 1 + 复仇进度） | Screen |
| 14 | OfficeScene（办公室俯视 + 像素瓦片 + 场景图块触发） | Screen |
| 15 | GossipScene（茶水间对话 + AP 消耗 + 信任度） | Screen |
| 16 | StatsDashboard（六大核心数据 + 风险预警） | Screen |
| 17 | DecisionScreen + PromiseLedger（决策卡 + 画饼总账） | Screen |
| 18 | PressScreen（发布会输入 + 必答主题 + 字数校验） | Screen |
| 19 | SettlementScreen（季度结算 - 指标对比 + 承诺结果 + 董事评论） | Screen |
| 20 | OverviewScreen（经营总览 - 趋势图 + 部门状态 + 建议动作） | Screen |
| 21 | DeathReportScreen + LegacyVaultScreen（死亡报告 + 遗产解锁） | Screen |
| 22 | mock fixtures + 关键 e2e + FRONTEND_BACKEND_INTEGRATION.md | Test / Doc |

---

## 任务 1：项目初始化（Vite + React + 8bitcn-ui CLI + pixelarticons + Zustand）

### 使用场景

仓库根目录已有前端 v1（纯 DOM，`index.html / js/* / styles.css`）。本任务在 `frontend/` 子目录建 v2 项目，**不删 v1**（v1 保留作为"无后端兜底"双击可跑版本）。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》初始化 v2 像素风前端工程骨架。

# 项目背景
《YES, BOSS!》是濒死公司 CEO 求生模拟器。v1 是纯 DOM 文字版（已存在 index.html / js/*），v2 升级为像素风可视化版本，并接后端 WebSocket。
v2 视觉：8-bit / 16-bit 像素风（Pokemon / Stardew Valley），用 8bitcn-ui + pixelarticons。

# 共用规则（务必遵守）
1. 只做前端，不动后端文件。
2. 不启动后端服务（前端 pnpm dev 跑 mock 模式即可）。
3. 前端永远不调真实 LLM（v1 直连模式废弃）。
4. 不删除 v1 文件（index.html / js/* / styles.css 保留在仓库根目录）。
5. 业务状态全部在 Zustand store；组件只负责渲染。
6. 8bitcn-ui 组件优先；找不到再手写。
7. pixelarticons 优先；缺图标用 SVG 手绘 24×24。
8. 视觉严格遵守像素风（无圆角 / 无渐变 / 无模糊 / image-rendering: pixelated）。
9. 单文件 > 300 行就拆。
10. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- Vite 5 + React 18 + TypeScript strict
- Tailwind CSS 3.4+
- 8bitcn-ui（通过 shadcn CLI 注册表，不是 npm 包）
- pixelarticons（npm，800 图标，24×24，currentColor）
- Zustand 4
- vitest + @testing-library/react
- playwright（仅 e2e）
- pnpm 包管理

# 本任务必须做的事

1. 在仓库根目录创建 frontend/ 子目录。

2. 初始化 Vite：
   - `pnpm create vite@latest frontend --template react-ts`（不真实执行，写在 README 即可）
   - 但本任务直接产出最终 package.json / vite.config.ts / tsconfig.json，不用 vite 模板默认值

3. 安装依赖（写进 package.json，不实际 install）：
   dependencies:
     react ^18.3
     react-dom ^18.3
     zustand ^4.5
     pixelarticons ^2.1
     framer-motion ^11 (仅用作 enter/exit 简单淡入)
     clsx ^2
   devDependencies:
     typescript ^5.5
     @types/react / @types/react-dom
     vite ^5.4
     @vitejs/plugin-react
     tailwindcss ^3.4
     postcss / autoprefixer
     vitest ^2 / @testing-library/react / @testing-library/jest-dom / jsdom
     @playwright/test ^1.45
     eslint / @typescript-eslint/eslint-plugin / eslint-plugin-react-hooks
     prettier
   注：不引入 shadcn 作为 npm 包；8bitcn-ui 通过 CLI 添加（产物落在 src/components/ui/8bit/）。

4. 创建目录树：
   frontend/
   ├── public/
   │   ├── fonts/.gitkeep
   │   ├── sprites/.gitkeep
   │   └── audio/.gitkeep
   ├── src/
   │   ├── main.tsx
   │   ├── App.tsx
   │   ├── components/{ui/8bit/.gitkeep, pixel/.gitkeep, layout/.gitkeep, screens/.gitkeep}
   │   ├── store/.gitkeep
   │   ├── protocol/.gitkeep
   │   ├── net/.gitkeep
   │   ├── mock/{fixtures/.gitkeep, scenarios/.gitkeep}
   │   ├── theme/.gitkeep
   │   ├── styles/{globals.css, pixel.css}
   │   ├── anim/.gitkeep
   │   └── utils/.gitkeep
   ├── tests/{unit/.gitkeep, component/.gitkeep, e2e/.gitkeep}
   ├── package.json
   ├── tsconfig.json
   ├── tsconfig.node.json
   ├── vite.config.ts
   ├── vitest.config.ts
   ├── playwright.config.ts
   ├── tailwind.config.ts
   ├── postcss.config.js
   ├── .eslintrc.cjs
   ├── .prettierrc
   ├── components.json (shadcn 配置，让 8bitcn-ui CLI 能找到 src/components/ui/8bit 路径)
   └── README.md

5. 实现 main.tsx：
   - 挂载 <App /> 到 #root
   - import "./styles/globals.css"
   - import "./styles/pixel.css"
   - 启用 React.StrictMode

6. 实现 App.tsx：
   - 占位：返回 <div className="bg-canvas min-h-screen text-ink-1 font-pixel">YES, BOSS! v2 (pixel)</div>
   - 真正的 ScreenRouter 由任务 3 实现，本任务只占位

7. tailwind.config.ts:
   - content: ["./index.html","./src/**/*.{ts,tsx}"]
   - theme.extend.colors: 任务 2 会填，本任务先写 {} 占位
   - theme.extend.fontFamily: { pixel: ["Fusion Pixel", "monospace"], retro: ["Press Start 2P", "monospace"] }
   - theme.extend.boxShadow: { hard: "4px 4px 0 0 #1B1B1B" }
   - corePlugins: 关掉 preflight 的 border-radius 默认（在 styles/globals.css 用 @layer base 重置）

8. styles/globals.css:
   - @tailwind base / components / utilities
   - * { border-radius: 0 !important; }（像素风零圆角铁律）
   - body { background: #F4F2EB; color: #1B1B1B; font-family: "Fusion Pixel", monospace; image-rendering: pixelated; }

9. styles/pixel.css:
   - @font-face Fusion Pixel（占位 path：public/fonts/fusion-pixel-12px-monospaced-zh_hans.woff2；README 说明从 https://github.com/TakWolf/fusion-pixel-font 下载）
   - .pixel-render { image-rendering: pixelated; image-rendering: crisp-edges; }
   - .pixel-stroke-2 { border: 2px solid #1B1B1B; }
   - .pixel-shadow { box-shadow: 4px 4px 0 0 #1B1B1B; }

10. components.json（shadcn 配置）：
    {
      "$schema": "https://ui.shadcn.com/schema.json",
      "style": "default",
      "rsc": false,
      "tsx": true,
      "tailwind": {
        "config": "tailwind.config.ts",
        "css": "src/styles/globals.css",
        "baseColor": "neutral",
        "cssVariables": true
      },
      "aliases": {
        "components": "@/components",
        "utils": "@/lib/utils",
        "ui": "@/components/ui/8bit"
      }
    }

11. vite.config.ts:
    - plugins: [react()]
    - resolve.alias: { "@": path.resolve(__dirname, "./src") }
    - server.port: 5173
    - 不配置代理后端（任务 5 的 wsClient 直接连 ws://localhost:8000）

12. vitest.config.ts:
    - test.environment: "jsdom"
    - test.setupFiles: ["./tests/setup.ts"]
    - test.globals: true

13. tsconfig.json: strict, baseUrl="./", paths { "@/*": ["src/*"] }

14. README.md（≤ 80 行）:
    - 项目简介（v2 像素风 + 后端 WebSocket 接入）
    - 与 v1 关系（v1 在仓库根目录保留作纯 DOM 兜底）
    - 安装 / 启动命令
    - 数据源切换：VITE_DATA_SOURCE=mock|ws
    - 字体下载链接 + 落盘位置
    - 8bitcn-ui CLI 添加示例：pnpm dlx shadcn@latest add @8bitcn/button
    - 明确写"先用 mock 跑通；后端联调由任务 5 + 22 处理"

15. 最简测试：
    - tests/setup.ts: import "@testing-library/jest-dom"
    - tests/unit/app.smoke.test.tsx: 测 <App /> 渲染含 "YES, BOSS!" 字样

# 禁止事项
- 禁止启动后端（uvicorn / docker）。
- 禁止 pnpm install 实际执行；只产 package.json。
- 禁止填充任务 2–22 的具体代码（theme / store / protocol / 屏幕等）。
- 禁止删除仓库根 v1 文件（index.html / js/* / styles.css）。
- 禁止引入 Material-UI / Ant Design / Chakra 等非像素风 UI 库。
- 禁止给组件加圆角 / 渐变 / 阴影模糊。
- 禁止用任何会拉真实 LLM 请求的代码（v1 的 llm.js 与本项目无关）。
- 禁止在 main.tsx 写业务逻辑。

# 验收标准
1. frontend/ 目录树与提示词完全一致。
2. pnpm dev（声明在 README，不真启）需可启动 Vite。
3. pnpm test 跑 app.smoke.test.tsx 通过。
4. tsc --noEmit 零错误。
5. eslint 零错误。
6. body 背景 #F4F2EB，font-family Fusion Pixel。
7. 全局零圆角生效（即使 8bitcn-ui 后续添加，也被 globals.css * 重置覆盖）。
8. components.json 路径与目录约定一致。
9. 仓库根目录 v1 文件未被改动。
10. README 含字体下载说明 + 8bitcn-ui 添加示例。

# 输出
1. 计划摘要（5 行内）
2. 修改/新建文件清单
3. 关键文件内容（package.json / vite.config.ts / tailwind.config.ts / styles/globals.css / styles/pixel.css / App.tsx / main.tsx / README.md / components.json）
4. 验证命令（pnpm install / pnpm dev / pnpm test / pnpm lint / tsc --noEmit）
5. 声明"未启动后端、未连真实 LLM、v1 文件未改"
````

### 预期产物

- `frontend/` 完整目录树（约 30 个占位 + 上述需实现文件）
- 可启动的 Vite 项目骨架
- 1 条最小 smoke 测试
- `components.json` 让 8bitcn-ui CLI 找到 `src/components/ui/8bit`
- `README.md` 说明字体下载 + 数据源切换

### 验收标准

1. 目录树与提示词 1:1 对齐。
2. `pnpm test` smoke 通过；`tsc --noEmit` / `eslint` 零错误。
3. `body` 背景 `#F4F2EB` + Fusion Pixel 字体。
4. 全站零圆角（globals.css `* { border-radius: 0 !important }`）。
5. `components.json` 路径正确，让 `pnpm dlx shadcn@latest add @8bitcn/button` 能落到 `src/components/ui/8bit/`。
6. v1 文件未被改动。
7. README 含字体下载链接 + `VITE_DATA_SOURCE=mock|ws` 说明。
8. 没有任何代码调用真实 LLM。

### 禁止事项

- 禁止删除 v1 文件。
- 禁止启动后端。
- 禁止填充任务 2–22 的具体代码。
- 禁止圆角 / 渐变 / 模糊。
- 禁止 Material-UI / Ant Design / Chakra。
- 禁止 v1 风格的 LLM 直连。
- 禁止 `main.tsx` 写业务逻辑。

---

## 任务 2：像素设计 token + Tailwind theme + 字体加载

### 使用场景

把 §0.10 调色板 / §0.11 字体 / §0.2 视觉铁律落地为可在组件中复用的 token，并扩展 Tailwind theme。**所有后续任务都从这里取色 / 取字号 / 取间距**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现像素设计 token + Tailwind theme + 字体加载。

# 项目背景
v2 是像素风。所有组件不允许自定义颜色 / 字号 / 阴影，必须从 theme/palette.ts + theme/tokens.ts 取。本任务把 §0.10 调色板 / §0.11 字体 / §0.2 铁律编码为 ts 常量 + Tailwind theme 扩展。

# 共用规则（必须遵守）
1. 只做前端，不动后端。
2. 不启动后端服务。
3. 前端永远不调真实 LLM。
4. 业务逻辑在 store；组件只渲染。
5. 视觉严格遵守像素风（无圆角 / 无渐变 / 无模糊）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- TypeScript strict
- Tailwind CSS 3.4
- Fusion Pixel + Press Start 2P

# 本任务范围
- frontend/src/theme/palette.ts
- frontend/src/theme/tokens.ts
- frontend/src/theme/index.ts（统一导出）
- frontend/tailwind.config.ts（替换任务 1 占位）
- frontend/src/styles/pixel.css（追加完整 @font-face）
- frontend/src/styles/globals.css（追加 token 注入）
- frontend/public/fonts/README.md（说明从 https://github.com/TakWolf/fusion-pixel-font 下载哪个 woff2 + 校验 hash）
- frontend/tests/unit/theme.test.ts
- frontend/tests/component/pixel-token.smoke.tsx（最简快照）

# 必须实现的内容

## palette.ts
export const PALETTE = {
  bg: {
    canvas: "#F4F2EB",
    panel: "#FFFFFF",
    panelDim: "#ECE9DF",
    floor: "#DCD7C7",
  },
  ink: {
    primary: "#1B1B1B",
    secondary: "#4A4A4A",
    muted: "#8C8C8C",
  },
  brand: {
    blue: "#2E6FE6",
    green: "#3DAE5C",
    orange: "#F39A2B",
    red: "#D9412C",
  },
  alarm: {
    red: "#E63946",
    yellow: "#F5C518",
  },
  game: {
    expGold: "#FFD23F",
    legacyPurple: "#7B5BE6",
  },
  stroke: {
    ink: "#1B1B1B",
  },
} as const;
export type PaletteToken = typeof PALETTE;

## tokens.ts
export const SPACING = {
  // 4px / 8px / 16px 网格
  xs: 4, sm: 8, md: 12, base: 16, lg: 24, xl: 32, xxl: 48,
} as const;
export const RADIUS = {
  // 像素风默认 0；像素阶梯圆角用 step
  none: 0,
  step: 2,  // 1 像素阶梯
} as const;
export const FONT_SIZE = {
  xs: 10, sm: 12, base: 12, md: 14, lg: 16, xl: 20, xxl: 24, hero: 32,
} as const;
export const LINE_HEIGHT = {
  tight: 1.2, normal: 1.4, loose: 1.6,
} as const;
export const SHADOW = {
  hard: "4px 4px 0 0 #1B1B1B",
  hardSm: "2px 2px 0 0 #1B1B1B",
  hardLg: "6px 6px 0 0 #1B1B1B",
  innerHard: "inset 2px 2px 0 0 #1B1B1B",
} as const;
export const BORDER = {
  thin: 1,
  pixel: 2,   // 默认像素描边
  bold: 4,
} as const;
export const Z_INDEX = {
  hud: 100,
  mainBar: 100,
  dialog: 200,
  toast: 300,
  overlay: 400,
} as const;
export const ANIM = {
  typewriterMsPerChar: 30,
  numberRollMs: 400,
  pulseHz: 0.6,
  toastMs: 3000,
  cardEnterMs: 200,
} as const;

## index.ts
export * from "./palette";
export * from "./tokens";

## tailwind.config.ts（完整，替换任务 1 占位）
import type { Config } from "tailwindcss";
import { PALETTE, SPACING, FONT_SIZE, SHADOW } from "./src/theme";

export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  corePlugins: { preflight: true },
  theme: {
    extend: {
      colors: {
        canvas: PALETTE.bg.canvas,
        panel: PALETTE.bg.panel,
        "panel-dim": PALETTE.bg.panelDim,
        floor: PALETTE.bg.floor,
        "ink-1": PALETTE.ink.primary,
        "ink-2": PALETTE.ink.secondary,
        "ink-3": PALETTE.ink.muted,
        "pixel-blue": PALETTE.brand.blue,
        "pixel-green": PALETTE.brand.green,
        "pixel-orange": PALETTE.brand.orange,
        "pixel-red": PALETTE.brand.red,
        "alarm-red": PALETTE.alarm.red,
        "caution-yellow": PALETTE.alarm.yellow,
        "exp-gold": PALETTE.game.expGold,
        "legacy-purple": PALETTE.game.legacyPurple,
        "stroke-ink": PALETTE.stroke.ink,
      },
      fontFamily: {
        pixel: ['"Fusion Pixel"', "monospace"],
        retro: ['"Press Start 2P"', "monospace"],
      },
      fontSize: {
        "px-xs": [`${FONT_SIZE.xs}px`, "1.2"],
        "px-sm": [`${FONT_SIZE.sm}px`, "1.4"],
        "px-base": [`${FONT_SIZE.base}px`, "1.4"],
        "px-md": [`${FONT_SIZE.md}px`, "1.4"],
        "px-lg": [`${FONT_SIZE.lg}px`, "1.3"],
        "px-xl": [`${FONT_SIZE.xl}px`, "1.3"],
        "px-xxl": [`${FONT_SIZE.xxl}px`, "1.2"],
        "px-hero": [`${FONT_SIZE.hero}px`, "1.1"],
      },
      spacing: {
        "px-xs": `${SPACING.xs}px`,
        "px-sm": `${SPACING.sm}px`,
        "px-md": `${SPACING.md}px`,
        "px-base": `${SPACING.base}px`,
        "px-lg": `${SPACING.lg}px`,
        "px-xl": `${SPACING.xl}px`,
        "px-xxl": `${SPACING.xxl}px`,
      },
      boxShadow: {
        hard: SHADOW.hard,
        "hard-sm": SHADOW.hardSm,
        "hard-lg": SHADOW.hardLg,
        "inner-hard": SHADOW.innerHard,
      },
      borderRadius: { none: "0", step: "2px" },
      animation: {
        pulseAlarm: "pulseAlarm 1.67s ease-in-out infinite",
        typewriter: "typewriter 1s steps(40) forwards",
      },
      keyframes: {
        pulseAlarm: {
          "0%, 100%": { opacity: "1" },
          "50%": { opacity: "0.6" },
        },
        typewriter: {
          from: { width: "0" },
          to: { width: "100%" },
        },
      },
    },
  },
  plugins: [],
} satisfies Config;

## pixel.css（追加完整 @font-face）
@font-face {
  font-family: "Fusion Pixel";
  src: url("/fonts/fusion-pixel-12px-monospaced-zh_hans.woff2") format("woff2");
  font-display: swap;
  font-weight: 400;
}
/* English / digits — Press Start 2P 由 8bitcn-ui 加载，不重复 import */

/* 像素感工具类 */
.pixel-render { image-rendering: pixelated; image-rendering: crisp-edges; }
.pixel-stroke { border: 2px solid var(--stroke-ink, #1B1B1B); }
.pixel-shadow { box-shadow: 4px 4px 0 0 #1B1B1B; }
.pixel-shadow-sm { box-shadow: 2px 2px 0 0 #1B1B1B; }
.pixel-inset { box-shadow: inset 2px 2px 0 0 #1B1B1B; }

/* 严禁圆角的强制兜底 */
*, *::before, *::after { border-radius: 0 !important; }

## globals.css 追加
@layer base {
  :root {
    --color-canvas: #F4F2EB;
    --color-panel: #FFFFFF;
    --color-ink-1: #1B1B1B;
    --stroke-ink: #1B1B1B;
  }
  html, body, #root { height: 100%; }
  body {
    background: var(--color-canvas);
    color: var(--color-ink-1);
    font-family: "Fusion Pixel", monospace;
    font-size: 12px;
    line-height: 1.4;
    image-rendering: pixelated;
    -webkit-font-smoothing: none;
    -moz-osx-font-smoothing: grayscale;
  }
  /* 数字 / 拉丁字符使用 Press Start 2P */
  .font-retro, code, kbd, samp { font-family: "Press Start 2P", monospace; }
}

## fonts/README.md
- 字体下载源：https://github.com/TakWolf/fusion-pixel-font/releases
- 选择 fusion-pixel-12px-monospaced-zh_hans.woff2
- 放入 public/fonts/
- 校验：开发者工具 Network 选 Fonts → 文件大小 ≈ 250–400KB；MIME font/woff2
- 不提交字体二进制到仓库（.gitignore 已排除）；CI 拉取自 GitHub Release

## theme.test.ts
- PALETTE / SPACING / FONT_SIZE 字段冻结（typeof + readonly 类型断言）
- 调色板色值与 §0.10 一一对应
- SHADOW.hard 含 "0 0 #1B1B1B"
- ANIM.typewriterMsPerChar === 30

## pixel-token.smoke.tsx
- 渲染一个 <div className="bg-pixel-blue text-px-base font-pixel border-2 border-stroke-ink shadow-hard" />
- 测试 computed style：背景 rgb(46,111,230) / border 2px solid / box-shadow 含 "4px 4px"

# 禁止事项
- 禁止扩展调色板（≤ 32 色铁律）。
- 禁止给 borderRadius 加任何 > 2px 的值。
- 禁止 boxShadow 含 blur > 0px（必须硬阴影）。
- 禁止字号非 4 的倍数（10 / 12 / 16 / 20 / 24 / 32 允许，14 严禁）。
- 等等——FONT_SIZE.md=14；冲突。修正：去掉 md=14，改为 md=16；任何地方需要"次大字"用 md=16。
  → 重新声明 FONT_SIZE：xs=10, sm=12, base=12, md=16, lg=20, xl=24, xxl=32, hero=48
- 禁止 fontFamily 引入第 3 个字体。
- 禁止 globals.css 写组件级样式（应在组件 .tsx 内 className）。
- 禁止删除 * { border-radius: 0 !important } 这条兜底。
- 禁止把 PALETTE 改为 hex 渐变 / hsl 函数。

# 验收标准
1. theme.test.ts / pixel-token.smoke.tsx 通过；tsc --noEmit / eslint 零错误。
2. Tailwind class bg-pixel-blue / shadow-hard / font-pixel / text-px-base 全部可用。
3. 全站 border-radius 视觉 0（DOM 检查任意元素 border-radius=0px）。
4. Fusion Pixel 字体在 Network 加载（404 不致命，但要有 @font-face 声明）。
5. PALETTE 32 色冷藏；新增请走 RFC（README 标注）。
6. FONT_SIZE 全部 4 的倍数（10 例外允许，作为最小可读尺寸）。
7. SHADOW 全部硬阴影 0 blur。
8. theme/index.ts 可一行 import。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令（pnpm test theme / pnpm lint / tsc --noEmit）
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `theme/palette.ts` / `theme/tokens.ts` / `theme/index.ts`
- 完整 `tailwind.config.ts`
- `styles/pixel.css` 完整 @font-face + 工具类
- `public/fonts/README.md`
- 2 个测试

### 验收标准

1. 测试通过；tsc / eslint 零错误。
2. Tailwind class `bg-pixel-blue` / `shadow-hard` / `font-pixel` / `text-px-base` 可用。
3. 全站 border-radius 视觉 0。
4. `@font-face` 已声明。
5. PALETTE ≤ 32 色。
6. FONT_SIZE 4 的倍数（10 例外）。
7. SHADOW 全硬阴影 blur=0。
8. `theme/index.ts` 一行导出。

### 禁止事项

- 禁止扩调色板。
- 禁止 borderRadius > 2px。
- 禁止 boxShadow blur > 0。
- 禁止字号非 4 倍数（10 例外）。
- 禁止引入第 3 个字体。
- 禁止 globals.css 写组件级样式。
- 禁止删除全局 0 圆角兜底。

---

## 任务 3：AppShell + ScreenRouter + 屏幕枚举

### 使用场景

游戏不需要 URL 路由（不刷新 / 不分享链接），但需要在 11 个屏幕之间切换。本任务实现轻量的 ScreenRouter（枚举驱动 + history 栈），并搭出 AppShell 让所有屏幕共用 HUDBar + MainActionBar 槽位。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 AppShell + ScreenRouter + 屏幕枚举。

# 项目背景
11 个屏幕（Onboarding / CompanySelect / OfficeScene / GossipScene / StatsDashboard / DecisionScreen / PressScreen / SettlementScreen / OverviewScreen / DeathReportScreen / LegacyVaultScreen）。屏幕切换无需 URL；用枚举 + history 栈管理。
AppShell 是全局 Layout 容器：顶 HUDBar / 中 屏幕主体 / 底 MainActionBar。部分屏幕（Onboarding / DeathReport）可隐藏 MainActionBar。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端服务。
3. 前端永远不调真实 LLM。
4. 业务状态在 store；组件只渲染。
5. 视觉像素风（无圆角 / 无渐变）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TypeScript strict
- Zustand 4

# 本任务范围
- frontend/src/store/screenStore.ts
- frontend/src/components/layout/AppShell.tsx
- frontend/src/components/layout/HUDBar.tsx（占位骨架，任务 10 填充）
- frontend/src/components/layout/MainActionBar.tsx（占位骨架，任务 11 填充）
- frontend/src/App.tsx（替换任务 1 占位）
- frontend/src/components/screens/__placeholders.tsx（11 个最简占位组件，方便 router 跑通）
- frontend/tests/unit/screen-store.test.ts
- frontend/tests/component/app-shell.test.tsx

# 必须实现的类型

## screenStore.ts
import { create } from "zustand";

export type ScreenId =
  | "onboarding"
  | "company-select"
  | "office"
  | "gossip"
  | "stats-dashboard"
  | "decision"
  | "press"
  | "settlement"
  | "overview"
  | "death-report"
  | "legacy-vault";

export interface ScreenStackEntry {
  id: ScreenId;
  params?: Record<string, unknown>;
}

interface ScreenState {
  current: ScreenStackEntry;
  stack: ScreenStackEntry[];   // history（push/replace/back）
  hudVisible: boolean;
  mainBarVisible: boolean;
  push: (id: ScreenId, params?: Record<string, unknown>) => void;
  replace: (id: ScreenId, params?: Record<string, unknown>) => void;
  back: () => void;
  setChrome: (cfg: { hud?: boolean; mainBar?: boolean }) => void;
}

export const useScreenStore = create<ScreenState>((set, get) => ({
  current: { id: "onboarding" },
  stack: [],
  hudVisible: true,
  mainBarVisible: true,
  push: (id, params) => set((s) => ({
    current: { id, params },
    stack: [...s.stack, s.current],
  })),
  replace: (id, params) => set(() => ({ current: { id, params }, stack: [] })),
  back: () => set((s) => {
    if (s.stack.length === 0) return s;
    const prev = s.stack[s.stack.length - 1];
    return { current: prev, stack: s.stack.slice(0, -1) };
  }),
  setChrome: (cfg) => set(() => ({
    hudVisible: cfg.hud ?? get().hudVisible,
    mainBarVisible: cfg.mainBar ?? get().mainBarVisible,
  })),
}));

## AppShell.tsx
import React from "react";
import { useScreenStore } from "@/store/screenStore";
import { HUDBar } from "./HUDBar";
import { MainActionBar } from "./MainActionBar";

interface Props { children: React.ReactNode; }
export const AppShell: React.FC<Props> = ({ children }) => {
  const hud = useScreenStore((s) => s.hudVisible);
  const main = useScreenStore((s) => s.mainBarVisible);
  return (
    <div className="bg-canvas text-ink-1 font-pixel min-h-screen flex flex-col">
      {hud && <HUDBar />}
      <main className="flex-1 overflow-hidden relative">{children}</main>
      {main && <MainActionBar />}
    </div>
  );
};

## HUDBar.tsx / MainActionBar.tsx（本任务只占位）
- 64px / 72px 高
- 像素描边 + 硬阴影
- 仅占位文字 "HUD" / "MAIN BAR"
- 任务 10 / 11 填充具体内容

## App.tsx（ScreenRouter 真实实现）
import { useScreenStore } from "@/store/screenStore";
import { AppShell } from "@/components/layout/AppShell";
import * as Screens from "@/components/screens/__placeholders";

const REGISTRY: Record<ScreenId, React.FC<any>> = {
  "onboarding": Screens.OnboardingPlaceholder,
  "company-select": Screens.CompanySelectPlaceholder,
  // ... 11 个全注册
};

export default function App() {
  const cur = useScreenStore((s) => s.current);
  const Cmp = REGISTRY[cur.id];
  return (
    <AppShell>
      <Cmp {...cur.params} />
    </AppShell>
  );
}

## __placeholders.tsx
- 导出 11 个最简组件（each 渲染 `<div className="p-px-lg">[Screen: <id>]</div>`）
- 每个占位组件含 1 个按钮"跳下一屏"用 useScreenStore().push 测试跳转
- 这些占位会被任务 12–22 替换；本任务保留是为了 ScreenRouter 早期可跑通

## screen-store.test.ts
- push 后 stack 长度 +1，current.id 切换
- replace 清空 stack
- back 弹出最近一条
- back 在 stack 为空时不抛
- setChrome 仅修改 hudVisible / mainBarVisible 之一

## app-shell.test.tsx
- 默认渲染 HUDBar / MainActionBar
- setChrome({ hud: false }) 后 HUDBar 不渲染
- 屏幕切换：push("company-select") 后子组件出现 "company-select" 字样

# 禁止事项
- 禁止引入 react-router-dom（不需要 URL 路由）。
- 禁止在 AppShell 加业务逻辑。
- 禁止在 store 直接做 fetch / WebSocket 调用（那是任务 5 net 层的事）。
- 禁止把 11 个屏幕的实现塞进 __placeholders（仅占位，本任务严控范围）。
- 禁止允许 stack 无限增长——加 max 20 条断言（超过 console.warn）。
- 禁止把 HUD/MainBar 的具体 UI 在本任务实现（留给任务 10/11）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. AppShell + ScreenRouter + 11 占位可跑（pnpm dev 看到第一屏 onboarding + 跳转按钮）。
3. screenStore push/replace/back 行为正确。
4. setChrome 可隐藏 HUDBar / MainActionBar。
5. 没有引入 react-router-dom。
6. 11 个占位组件全部注册在 REGISTRY。
7. App.tsx 行数 ≤ 40。
8. tab 键可在占位按钮间切换（基础可达性）。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `store/screenStore.ts`
- `components/layout/AppShell.tsx` / `HUDBar.tsx`（占位）/ `MainActionBar.tsx`（占位）
- `App.tsx` 替换占位
- 11 个 `__placeholders` 屏幕
- 2 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. `pnpm dev` 可看 onboarding 占位 + 跳转按钮。
3. `screenStore` push/replace/back 行为正确。
4. `setChrome` 可隐藏 HUD / MainBar。
5. 未引入 `react-router-dom`。
6. 11 占位全部注册。
7. `App.tsx` 行数 ≤ 40。
8. tab 可达。

### 禁止事项

- 禁止 react-router-dom。
- 禁止 AppShell 写业务。
- 禁止 store 调 fetch / WebSocket。
- 禁止占位组件写真实 UI。
- 禁止 stack 无限增长。
- 禁止 HUD / MainBar 本任务填充。

---

## 任务 4：Zustand store + 持久化

### 使用场景

游戏全局状态：`session（公司/指标/季度/员工/历史）` + `meta（跨局元进度）` + `realtimeOps（实时操作 inflight）` + `outboundQueue（待消化的 outbound bundle）`。本任务建 store 骨架；具体字段与后端 `GameSession / GameSnapshot` 对齐。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 Zustand gameStore + localStorage 持久化。

# 项目背景
后端任务 7 定义了 GameSession 聚合根；任务 6 定义了 Outbound DTO（GameSnapshot / DecisionAck / GossipResult / PressAck / SettlementBundle / DeathReportBundle / Toast / Error）。
前端 store 把这些组装成可被 UI 直接消费的状态。本任务只建骨架 + 持久化 + selector；具体接入由任务 5 (net) 推送 action。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务状态全部在 store；组件只渲染。
5. 像素风（无渐变 / 无圆角）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- TypeScript strict
- Zustand 4 + zustand/middleware/persist

# 本任务范围
- frontend/src/store/gameStore.ts
- frontend/src/store/networkStore.ts
- frontend/src/store/selectors.ts
- frontend/src/store/__internal__/persist.ts（自定义 storage 序列化 + 版本号迁移）
- frontend/src/store/index.ts
- frontend/tests/unit/game-store.test.ts
- frontend/tests/unit/network-store.test.ts

# 必须实现的类型

## gameStore.ts
interface GameStoreState {
  // 协议层 DTO（与后端任务 6 完全对齐，字段 camelCase）
  snapshot: GameSnapshotDTO | null;
  history: HistoryEntryDTO[];
  pendingDecision: DecisionCardDTO | null;
  pendingSettlementBundle: SettlementBundleDTO | null;
  pendingDeathBundle: DeathReportBundleDTO | null;
  pressBundle: PressBundleDTO | null;
  toasts: ToastDTO[];
  // 实时操作 inflight 追踪（不阻塞 UI；用作 spinner / disabled）
  inflight: {
    createGame?: boolean;
    selectDecision?: boolean;
    collectGossip?: boolean;
    submitPress?: boolean;
    settleQuarter?: boolean;
  };
  // LLM 降级标记（后端 settle 完成后告知）
  llmDegraded: boolean;

  // actions（由任务 5 net 层调用；组件只读）
  ingestSnapshot: (s: GameSnapshotDTO) => void;
  ingestDecisionAck: (a: DecisionAckDTO) => void;
  ingestGossipResult: (r: GossipResultDTO) => void;
  ingestPressAck: (a: PressAckDTO) => void;
  ingestSettlementBundle: (b: SettlementBundleDTO) => void;
  ingestDeathBundle: (b: DeathReportBundleDTO) => void;
  pushToast: (t: ToastDTO) => void;
  dismissToast: (id: string) => void;
  setInflight: (key: keyof GameStoreState["inflight"], v: boolean) => void;
  setLLMDegraded: (v: boolean) => void;
  resetForNewRun: () => void;
}

注意：所有 ingest* 必须做最小校验（字段存在 / 不抛）；不做业务校验（业务在后端）。

## networkStore.ts
interface NetworkState {
  status: "idle" | "connecting" | "open" | "reconnecting" | "closed" | "error";
  lastError: string | null;
  latencyMs: number | null;
  dataSource: "mock" | "ws";
  setStatus: (s: NetworkState["status"]) => void;
  setError: (e: string | null) => void;
  setLatency: (ms: number | null) => void;
  setDataSource: (ds: NetworkState["dataSource"]) => void;
}

## persist.ts
- 自定义 storage 适配 localStorage
- key: "yb_v2_game_state"
- 序列化字段白名单：snapshot / history / llmDegraded / pendingDecision 等可恢复字段
- 不持久化：toasts / inflight（应在每次 reload 重置）
- 版本号 1；如发现旧 v1 (yb_llm_config_v1) 的 key 不动它（属于 v1 兼容）

## selectors.ts
// 组件不该直接读 store 复杂结构；用 selector
export const selectCurrentStats = (s: GameStoreState) => s.snapshot?.stats ?? null;
export const selectCurrentQuarter = (s: GameStoreState) => s.snapshot?.quarter ?? null;
export const selectDecisionCards = (s: GameStoreState) => s.snapshot?.quarter.decisionCards ?? [];
export const selectIsDead = (s: GameStoreState) => s.snapshot ? (s.snapshot.company as any).status === "dead" : false;
// ... 约 15 个 selector 覆盖所有 UI 读取场景

## index.ts
export * from "./gameStore";
export * from "./networkStore";
export * from "./selectors";

# 测试

## game-store.test.ts
- ingestSnapshot 后 selectCurrentStats / selectCurrentQuarter 返回正确
- ingestDecisionAck 后 inflight.selectDecision=false 且 stats 更新
- pushToast 后 toasts 长度 +1，id 自动生成
- dismissToast 移除指定 id
- resetForNewRun 清空所有 pending* / toasts / inflight 但保留 history（用作复仇进度计算）
  · 等等——history 是单局的；跨局应保留在 meta_progress，由后端推。修正：resetForNewRun 也清空 history
- 持久化往返：localStorage 模拟 set → 新实例 init → 状态恢复
- 持久化白名单：toasts / inflight 不在序列化结果中

## network-store.test.ts
- setStatus / setError / setLatency / setDataSource 各跑一次
- 默认 dataSource = "mock"（除非 VITE_DATA_SOURCE === "ws"）

# 禁止事项
- 禁止在 store 直接 fetch / new WebSocket（那是任务 5）。
- 禁止 ingest* 抛业务异常（必须容错；非法字段 console.warn 后跳过）。
- 禁止持久化 toasts / inflight（每次 reload 必须重置）。
- 禁止 selector 内含副作用。
- 禁止把 WARN 字段（hidden_secrets / is_truth / hidden_risks / internalEval）存入 store（即便后端漏发，前端也要在 ingest 时显式 strip）。
- 禁止把后端 DTO 与前端业务模型混在一起；store 字段就是 DTO（前端不二次建模）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. localStorage 持久化白名单生效。
3. ingest* 容错（非法字段不抛）。
4. selector 覆盖所有屏幕需要的字段。
5. WARN 字段 strip 测试通过（构造含 hidden_secrets 的 snapshot → ingest 后 store 中不存在）。
6. networkStore 默认 dataSource 跟环境变量。
7. resetForNewRun 行为正确。
8. store 不含 fetch / WebSocket。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `store/gameStore.ts` / `networkStore.ts` / `selectors.ts` / `persist.ts` / `index.ts`
- 2 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. localStorage 持久化白名单生效。
3. `ingest*` 容错。
4. selector 覆盖所有屏幕读取需求。
5. WARN 字段 strip 测试通过。
6. networkStore 默认 dataSource 跟环境变量。
7. `resetForNewRun` 行为正确。
8. store 不含 fetch / WebSocket。

### 禁止事项

- 禁止 store 直接 fetch / WebSocket。
- 禁止 ingest 抛业务异常。
- 禁止持久化 toasts / inflight。
- 禁止 selector 含副作用。
- 禁止 WARN 字段进 store。
- 禁止前端二次建模（store 字段即后端 DTO）。

---

## 任务 5：协议适配层（Envelope + Inbound + Outbound + wsClient + mockAdapter）

### 使用场景

后端任务 6 / 21 定义了 Envelope + Inbound + Outbound + WebSocket Gateway。本任务在前端实现 1:1 对齐的 TS 类型 + 一个 DataSource 接口 + 两个实现（mockAdapter / wsClient）+ 一个总线把入站 envelope 派发到 gameStore。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现协议适配层 —— Envelope + Inbound + Outbound + wsClient + mockAdapter。

# 项目背景
后端任务 6 / 21 已定义：
- Envelope { v, id, ts, direction, type, ack_for?, payload }
- Inbound（前端→后端）：CreateGame / SelectDecision / CollectGossip / SubmitPress / SettleQuarter / RequestSnapshot / Ping
- Outbound（后端→前端）：GameSnapshot / DecisionAck / GossipResult / PressAck / SettlementBundle / DeathReportBundle / Toast / ErrorOutbound
- WebSocket endpoint: /api/v1/ws/{player_id}
- 字段命名 camelCase（后端 by_alias=True）
- WARN 字段（hidden_secrets / is_truth / hidden_risks / internalEval）从协议层就被过滤，前端永远收不到

本任务实现 DataSource 抽象 + ws / mock 两个实现 + EnvelopeBus 派发到 gameStore。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端（mock 模式即可跑完）。
3. 前端永远不调真实 LLM（即便 dataSource=mock，也不要任何 OpenAI SDK）。
4. 业务状态在 store；组件只渲染。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- TypeScript strict
- 原生 WebSocket（不引入 socket.io）
- vitest

# 本任务范围
- frontend/src/protocol/envelope.ts
- frontend/src/protocol/inbound.ts
- frontend/src/protocol/outbound.ts
- frontend/src/protocol/types.ts（GameSnapshotDTO / StatsDTO / DecisionCardDTO 等 ~20 个 DTO）
- frontend/src/protocol/index.ts
- frontend/src/net/wsClient.ts
- frontend/src/net/mockAdapter.ts
- frontend/src/net/dataSource.ts（DataSource 接口 + 工厂）
- frontend/src/net/envelopeBus.ts（入站派发到 gameStore）
- frontend/src/net/index.ts
- frontend/tests/unit/protocol.test.ts
- frontend/tests/unit/mock-adapter.test.ts
- frontend/tests/unit/ws-client.test.ts（用 jsdom + mock-socket 或自写 mock）

# 必须实现的类型

## envelope.ts
export interface Envelope<T> {
  v: 1;
  id: string;
  ts: string;
  direction: "inbound" | "outbound";
  type: string;
  ackFor?: string;
  payload: T;
}
export const makeEnvelope = <T>(type: string, payload: T, ackFor?: string): Envelope<T> => ({
  v: 1, id: crypto.randomUUID(), ts: new Date().toISOString(),
  direction: "inbound", type, ackFor, payload,
});

## inbound.ts
export type InboundType =
  | "create_game" | "select_decision" | "collect_gossip"
  | "submit_press" | "settle_quarter" | "request_snapshot" | "ping";

export interface CreateGamePayload { playerId?: string; requestLegacies: boolean; }
export interface SelectDecisionPayload { sessionId: string; quarterNumber: number; cardId: string; }
export interface CollectGossipPayload { sessionId: string; quarterNumber: number; scene: "tearoom"|"elevator"|"meeting_room"|"workstation"|"rooftop"|"smoking_area"; }
export interface SubmitPressPayload { sessionId: string; quarterNumber: 3; pressType: "INAUGURATION"|"CRISIS"|"PRODUCT"|"FINANCIAL"|"ROADSHOW"|"LAYOFF_EXPLAIN"|"REGULATOR"|"COUNTER"; transcript: string; durationS?: number; }
export interface SettleQuarterPayload { sessionId: string; quarterNumber: number; }
export interface RequestSnapshotPayload { sessionId: string; }
export interface PingPayload { sentAt: string; }

## outbound.ts
export type OutboundType =
  | "game_snapshot" | "decision_ack" | "gossip_result"
  | "press_ack" | "settlement_bundle" | "death_report_bundle"
  | "toast" | "error" | "pong";

// 与后端任务 6 outbound 1:1 对齐，但 DTO 类型来自 types.ts
import type { GameSnapshotDTO, DecisionAckDTO, GossipResultDTO, PressAckDTO, SettlementBundleDTO, DeathReportBundleDTO, ToastDTO, ErrorOutboundDTO } from "./types";

export type OutboundPayload =
  | { type: "game_snapshot"; data: GameSnapshotDTO }
  | { type: "decision_ack"; data: DecisionAckDTO }
  | { type: "gossip_result"; data: GossipResultDTO }
  | { type: "press_ack"; data: PressAckDTO }
  | { type: "settlement_bundle"; data: SettlementBundleDTO }
  | { type: "death_report_bundle"; data: DeathReportBundleDTO }
  | { type: "toast"; data: ToastDTO }
  | { type: "error"; data: ErrorOutboundDTO };

## types.ts（约 20 个 DTO，与后端任务 6 outbound 严格对齐）
- StatsDTO { CASH: number; MORALE: number; BOARD: number; FACE: number; }
- CompanyDTO { id; name; business; absurdity; foundingMotto; deathCauses: DeathCauseDTO[]; foundedYear; }
- DeathCauseDTO { category; description; }
- EmployeeDTO { id; name; role; competence; loyalty; stress; personalityTag; faction; mood; attitudeToPlayer; }
  注意：禁止包含 hiddenSecrets / 任何 WARN 字段
- DecisionCardDTO { id; category; title; description; immediateEffect: Partial<StatsDTO>; flavor; longTermHint?; }
- GossipLeadDTO { id; quarter; scene; speakerId?; text; reliability: "RUMOR"|"LIKELY"|"CONFIRMED"; linkedEmployeeIds: string[]; apCost: number; }
  注意：禁止包含 isTruth
- QuarterDTO { number; phase: "BRIEFING"|"GOSSIP"|"DECISION"|"PRESS"|"SETTLEMENT"|"DONE"; briefing?: BriefingDTO; decisionCards: DecisionCardDTO[]; selectedDecisionId?; pressInput?: PressInputDTO; pressBundle?: PressBundleDTO; settlement?: SettlementDTO; apRemaining: number; }
- BriefingDTO { quarter; marketMood; headlineHint; }
  注意：禁止包含 hiddenRisks
- PressInputDTO { quarter; pressType; mustAnswerTopics: string[]; transcript; wordCount; flags: string[]; submittedAt: string; }
- PressEvaluationDTO { scores: Record<string, number>; memorableQuote; biggestFlaw; mediaAngle; statImpact: Partial<StatsDTO>; }
  注意：禁止包含 internalEval
- MediaHeadlineDTO { outlet; headline; tone; summary?; }
- PressBundleDTO { input: PressInputDTO; evaluation: PressEvaluationDTO; headlines: MediaHeadlineDTO[]; }
- BoardReactionDTO / EmployeeGossipDTO / RivalActionDTO
- SettlementDTO { quarter; quarterReport; boardReaction; employeeGossip; rivalAction; marketSignal; metricsDelta: Partial<StatsDTO>; }
- HistoryEntryDTO { quarter; decisionId; pressBundleId?; statsBefore: StatsDTO; statsAfter: StatsDTO; settlementSummary: string; }
- PromiseDTO { id; quarterMade; source; text; fulfilled: boolean | null; judgedAtQuarter?; parsed?: { metric; targetExpr; deadlineQuarter? }; }
- MetaSummaryDTO { playerId; totalRuns; unlockedLegacies: LegacyUnlockDTO[]; unlockedStyles: string[]; }
- LegacyUnlockDTO { type; labelZh; description; effectSummary; earnedAtRunId; earnedAtQuarter; }
- GameSnapshotDTO { sessionId; playerId; company; stats; quarter; history; metaSummary; promiseLog: PromiseDTO[]; status: "active"|"dead"|"won"; }
- DecisionAckDTO { sessionId; quarterNumber; cardId; immediateStats: StatsDTO; nextPhase; }
- GossipResultDTO { sessionId; quarterNumber; lead: GossipLeadDTO; apRemaining: number; }
- PressAckDTO { sessionId; quarterNumber; accepted: boolean; flags: string[]; replacedCount: number; }
- SettlementBundleDTO { sessionId; quarterNumber; settlement: SettlementDTO; pressBundle?: PressBundleDTO; newStats: StatsDTO; historyAdded: HistoryEntryDTO; death?: { reason; title }; llmDegraded: boolean; }
- DeathReportBundleDTO { sessionId; obituary; biggestMistakeDecisionId?; lastEmployee?: { name; quote }; headlines: string[]; legacyUnlocks: LegacyUnlockDTO[]; stylesUnlocked: string[]; }
- ToastDTO { id; level: "info"|"warn"|"error"; message; hint?; }
- ErrorOutboundDTO { code; message; retryable: boolean; }

## dataSource.ts
export interface DataSource {
  connect: (playerId: string, sessionId?: string) => Promise<void>;
  disconnect: () => void;
  send: <T>(envelope: Envelope<T>) => Promise<void>;
  onMessage: (cb: (env: Envelope<unknown>) => void) => void;
  status: "idle"|"connecting"|"open"|"reconnecting"|"closed"|"error";
}
export function createDataSource(mode: "mock"|"ws", config: { wsUrl?: string }): DataSource;
// 工厂按 mode 返回 wsClient 或 mockAdapter

## wsClient.ts
- 实现 DataSource
- new WebSocket(wsUrl + "/" + playerId + (sessionId ? "?session_id="+sessionId : ""))
- 心跳：30s 发 ping envelope；60s 无响应断开重连
- 重连：指数退避 0.5 / 1 / 2 / 5 / 10 秒（最多 5 次后放弃）
- onopen: status="open"，networkStore.setStatus
- onmessage: JSON.parse → envelopeBus.dispatch
- onclose / onerror: status 切换 + 重连逻辑
- send: WebSocket.send(JSON.stringify(env))
- 不调用 LLM
- 不持久化 raw envelope（debug 时可 console.debug）

## mockAdapter.ts
- 实现 DataSource，使用 setTimeout 模拟延迟（50–200ms）
- 入站 envelope 派发：根据 type 分发到本地 mock fixtures
  · "create_game" → 立即响应 game_snapshot envelope（fixture 默认首局公司 + 12 员工）
  · "select_decision" → 立即响应 decision_ack
  · "collect_gossip" → 立即响应 gossip_result
  · "submit_press" → 立即响应 press_ack
  · "settle_quarter" → 延迟 1.5s 响应 settlement_bundle（可配 fixture：happy / die-q1 / press-fail）
  · "request_snapshot" → 立即响应 game_snapshot
  · "ping" → 立即响应 pong
- fixture 存在 src/mock/fixtures/（任务 22 完善）
- 本任务先实现 createGame + selectDecision 两个 mock 响应；其余 stub 占位
- 暴露 setScenario("happy"|"die-q1"|"press-fail") 切换不同剧本

## envelopeBus.ts
import { useGameStore } from "@/store/gameStore";
import { useNetworkStore } from "@/store/networkStore";

export function dispatch(env: Envelope<unknown>) {
  switch (env.type) {
    case "game_snapshot": useGameStore.getState().ingestSnapshot(env.payload as GameSnapshotDTO); break;
    case "decision_ack": useGameStore.getState().ingestDecisionAck(env.payload as DecisionAckDTO); break;
    case "gossip_result": useGameStore.getState().ingestGossipResult(env.payload as GossipResultDTO); break;
    case "press_ack": useGameStore.getState().ingestPressAck(env.payload as PressAckDTO); break;
    case "settlement_bundle":
      const b = env.payload as SettlementBundleDTO;
      useGameStore.getState().ingestSettlementBundle(b);
      useGameStore.getState().setLLMDegraded(b.llmDegraded);
      break;
    case "death_report_bundle": useGameStore.getState().ingestDeathBundle(env.payload as DeathReportBundleDTO); break;
    case "toast": useGameStore.getState().pushToast(env.payload as ToastDTO); break;
    case "error":
      const e = env.payload as ErrorOutboundDTO;
      useGameStore.getState().pushToast({ id: crypto.randomUUID(), level: "error", message: e.message });
      useNetworkStore.getState().setError(e.code);
      break;
    case "pong": break;
    default: console.warn("[envelopeBus] unknown type", env.type);
  }
}

# 测试

## protocol.test.ts
- makeEnvelope 生成 v=1 / id 形如 uuid / ts ISO
- 所有 Outbound DTO 类型可被 satisfies 验证（构造完整 fixture）
- WARN 字段编译时被禁止（type-only test）：
  · const bad: EmployeeDTO = { ...validFields, hiddenSecrets: ["x"] }; // 期望 ts-expect-error 报错
  · 写 3 条 ts-expect-error 注释，跑 tsc 时如果不报错则失败

## mock-adapter.test.ts
- createDataSource("mock", {}) 返回 mockAdapter
- connect → status="open"
- send create_game → onMessage 收到 game_snapshot envelope（ackFor === 原 id）
- send select_decision → 收 decision_ack
- 切换 scenario("die-q1") 后 settle_quarter 响应 death_report_bundle
- 不调用任何 fetch / WebSocket / LLM SDK

## ws-client.test.ts
- 用 mock-socket 替换 WebSocket
- connect → onopen 触发 → status="open"
- send envelope → mock-server 收到正确 JSON
- mock-server 推 game_snapshot → envelopeBus.dispatch 调用
- 心跳：30s tick 时发送 ping
- 断线：onclose → status="closed" → 0.5s 后 status="reconnecting"
- 5 次重连失败 → status="error" + 不再重试

# 禁止事项
- 禁止引入 socket.io / Pusher 等。
- 禁止本任务真发起到 ws://localhost:8000（用 mock-socket）。
- 禁止 envelope 字段不是 camelCase。
- 禁止 outbound DTO 含 WARN 字段（TS 类型必须禁止）。
- 禁止 dispatch 内做业务逻辑（只调 store ingest）。
- 禁止 wsClient 内调 LLM SDK。
- 禁止把 fixture 数据写进 wsClient（fixture 只属于 mockAdapter / mock/fixtures）。
- 禁止协议层引入 React（纯 TS，便于将来 SSR / Node 复用）。

# 验收标准
1. 所有测试全过；tsc / eslint 零错误。
2. Envelope / Inbound / Outbound DTO 与后端任务 6 字段名 1:1 对齐。
3. WARN 字段在 TS 类型层禁止（ts-expect-error 验证）。
4. mockAdapter 不真连网；wsClient 用 mock-socket 通过。
5. dispatch 正确路由 8 种 outbound type。
6. envelopeBus 不含业务逻辑。
7. dataSource.ts 工厂按 mode 切换。
8. 心跳 / 重连 / 退避算法正确。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码 + ~20 个 DTO
4. 验证命令
5. 声明"未启动后端、未发起真实网络请求、未调 LLM"
````

### 预期产物

- `protocol/envelope.ts` / `inbound.ts` / `outbound.ts` / `types.ts`（~20 DTO）
- `net/wsClient.ts` / `mockAdapter.ts` / `dataSource.ts` / `envelopeBus.ts`
- 3 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. Envelope / DTO 与后端任务 6 字段名 1:1 对齐。
3. WARN 字段 TS 类型禁止（`ts-expect-error` 验证）。
4. mockAdapter 不真连网；wsClient 用 `mock-socket` 通过。
5. dispatch 路由 8 种 outbound type。
6. envelopeBus 不含业务逻辑。
7. 工厂按 mode 切换。
8. 心跳 / 重连 / 退避算法正确。

### 禁止事项

- 禁止 socket.io / Pusher。
- 禁止真连 `ws://localhost:8000`。
- 禁止 envelope 非 camelCase。
- 禁止 DTO 含 WARN 字段。
- 禁止 dispatch 做业务。
- 禁止 wsClient 调 LLM SDK。
- 禁止 fixture 写进 wsClient。
- 禁止协议层引入 React。

---

## 任务 6：PixelButton + PixelCard + PixelDialog 基础组件

### 使用场景

3 个最基础的像素风原子组件。所有屏幕都用到。**与 8bitcn-ui 的 Button / Card / Dialog 整合**（如可用）；若 8bitcn-ui 默认样式不够像素感，在外面再包一层加 `pixel-stroke` / `pixel-shadow`。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 3 个基础像素组件：PixelButton + PixelCard + PixelDialog。

# 项目背景
所有屏幕的"按钮 / 卡片 / 弹窗"都基于这 3 个原子。8bitcn-ui 提供了 Button / Card / Dialog 的像素化版本（已自带 retro 字体 + 像素描边），但默认偏 8-bit 英文风格；我们需要在外面套一层让中文字体（Fusion Pixel）也能像素感对齐 + 颜色按 §0.10 调色板匹配。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store；组件只渲染。
5. 像素风（无圆角 / 无渐变 / 无模糊 / image-rendering: pixelated）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict
- Tailwind + 8bitcn-ui（先尝试用，若不合适降级手写）
- clsx

# 本任务范围
- frontend/src/components/pixel/PixelButton.tsx
- frontend/src/components/pixel/PixelCard.tsx
- frontend/src/components/pixel/PixelDialog.tsx
- frontend/src/components/pixel/index.ts
- frontend/scripts/add-8bit-components.md（说明用 shadcn CLI 添加哪些；本任务不实际执行）
- frontend/tests/component/pixel-button.test.tsx
- frontend/tests/component/pixel-card.test.tsx
- frontend/tests/component/pixel-dialog.test.tsx

# 必须实现的内容

## PixelButton.tsx
type Variant = "blue" | "green" | "orange" | "red" | "ghost" | "danger";
type Size = "sm" | "md" | "lg";

interface PixelButtonProps {
  variant?: Variant;           // 默认 "blue"
  size?: Size;                  // 默认 "md"
  icon?: React.ReactNode;       // 左侧像素图标（24×24）
  disabled?: boolean;
  pressed?: boolean;            // 按下态（offset 阴影 + 反向位移）
  fullWidth?: boolean;
  loading?: boolean;            // 显示 spinner（pixelarticons "loader" 旋转）
  children: React.ReactNode;
  onClick?: () => void;
  // 键盘可达性：默认 type="button"；按下空格 / Enter 触发
  ariaLabel?: string;
  hotkey?: string;              // 显示在右下角的快捷键提示，如 "1"
}

视觉规则（依据原型图）：
- 高度：sm=32 / md=44 / lg=56（必须 4 的倍数）
- 内边距：px-px-md（12px）+ py-px-sm
- 边框：2px stroke-ink；阴影 hard (4px offset)
- 默认态：背景 = variant 主色（pixel-blue 等）+ 白字
- hover：上移 -2px + 阴影偏移到 6px
- active / pressed：下移 2px + 阴影变 0px
- disabled：bg-panel-dim + 灰字 + 无阴影 + 鼠标禁用
- 字体：font-pixel（中文用）；hotkey 数字用 font-retro
- 视觉范例对齐：原型图底部 4 个主按钮（公司管理蓝 / 员工沟通绿 / 项目推进橙 / 财务决策红）

实现要点：
- 先尝试 import { Button } from "@/components/ui/8bit"，包一层 PixelButton 应用颜色 + hotkey
- 若 8bitcn-ui 不可用（components.json 错或 CLI 未运行），降级用纯 Tailwind 实现
- 测试可用 vitest 模拟，不依赖真实 8bit 组件
- forwardRef 暴露按钮 DOM

## PixelCard.tsx
type CardKind = "default" | "metric" | "decision" | "risk" | "info" | "event";

interface PixelCardProps {
  kind?: CardKind;
  title?: string;                 // 顶部标题条（彩色）
  titleColor?: Variant;           // 标题条色，default kind 时透明
  badge?: React.ReactNode;        // 右上角小徽章
  footer?: React.ReactNode;
  selected?: boolean;             // 决策卡选中态：金边 + 高亮
  dimmed?: boolean;               // 决策卡未选态：grayscale 60%
  onClick?: () => void;
  className?: string;
  children: React.ReactNode;
}

视觉规则：
- 背景：bg-panel（白）
- 边框：2px stroke-ink
- 阴影：hard (4px)
- 标题条：高 32px / 顶部 / 内边距 px-px-md / 颜色对应 variant
- selected：外加 2px exp-gold 描边 + 6px 硬阴影 + check icon 右上
- dimmed：filter grayscale(0.6) opacity 0.7

## PixelDialog.tsx
interface PixelDialogProps {
  open: boolean;
  onOpenChange: (v: boolean) => void;
  title: string;
  description?: string;
  size?: "sm" | "md" | "lg";
  primaryAction?: { label: string; onClick: () => void; variant?: Variant };
  secondaryAction?: { label: string; onClick: () => void };
  children?: React.ReactNode;
}

视觉规则：
- 遮罩：bg-ink-1 alpha 0.6（无模糊）
- 对话框：bg-panel + 2px stroke + 4px hard shadow
- 标题条：彩色 + 居中
- 关闭按钮：右上 X 图标（pixelarticons "close-box"）
- ESC 关闭 / 点遮罩关闭（默认）
- 焦点陷阱：tab 在对话框内循环

实现：先用 8bitcn-ui 的 Dialog 组件包一层；若不能用降级用 React 原生 + portal

## index.ts
统一 export 3 个组件 + 类型

## add-8bit-components.md
说明在终端跑：
- pnpm dlx shadcn@latest add @8bitcn/button
- pnpm dlx shadcn@latest add @8bitcn/card
- pnpm dlx shadcn@latest add @8bitcn/dialog
- 产物落在 src/components/ui/8bit/{button,card,dialog}.tsx
- 注意：本任务不实际执行 CLI；只在文档说明
- 若执行失败，PixelButton/Card/Dialog 自动走降级路径

## pixel-button.test.tsx
- 6 个 variant 各渲染快照
- click 触发 onClick
- disabled 时 click 不触发
- hotkey="1" 显示在右下角
- aria-label 正确
- pressed 状态 box-shadow 偏移变 0

## pixel-card.test.tsx
- 5 个 kind 各渲染快照
- selected 时 outline 金色
- dimmed 时 grayscale 生效
- title + badge + footer 渲染

## pixel-dialog.test.tsx
- open=false 不渲染
- open=true 渲染 + 焦点进入
- ESC 关闭
- primaryAction click 触发
- 焦点陷阱（tab 循环）

# 禁止事项
- 禁止圆角 > 2px。
- 禁止阴影 blur > 0。
- 禁止按钮 hover 动画 > 100ms / 用 ease-out cubic。
- 禁止 Dialog 用模糊遮罩。
- 禁止把业务文案硬编码进组件（label 一律 props）。
- 禁止组件内调 fetch / WebSocket / LLM。
- 禁止重复实现 8bitcn-ui 已有的功能（先尝试用；不可用再手写）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 3 个组件 6 + 5 + 2 个 variant 全部渲染正确。
3. 与 8bitcn-ui 整合：components.json 配置正确时 import 不报错；不可用时降级正常。
4. 视觉零圆角 / 零渐变 / 零模糊。
5. 键盘可达性（tab / Enter / Space / ESC）。
6. add-8bit-components.md 含正确 CLI 命令。
7. forwardRef 暴露 DOM。
8. 单文件 ≤ 300 行。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码（含 add-8bit-components.md）
4. 验证命令
5. 声明"未启动后端、未调 LLM、未执行 shadcn CLI"
````

### 预期产物

- `pixel/PixelButton.tsx` / `PixelCard.tsx` / `PixelDialog.tsx`
- `pixel/index.ts`
- `scripts/add-8bit-components.md`
- 3 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 3 组件多个 variant 渲染正确。
3. 与 8bitcn-ui 整合；降级路径可用。
4. 视觉零圆角 / 零渐变 / 零模糊。
5. 键盘可达性。
6. CLI 命令文档正确。
7. forwardRef 暴露 DOM。
8. 单文件 ≤ 300 行。

### 禁止事项

- 禁止圆角 > 2px。
- 禁止阴影 blur > 0。
- 禁止 hover 动画 > 100ms。
- 禁止 Dialog 模糊遮罩。
- 禁止业务文案硬编码。
- 禁止组件调 fetch / LLM。
- 禁止重复实现 8bitcn-ui 已有功能。

---

## 任务 7：PixelIcon wrapper（pixelarticons + 像素感缩放）

### 使用场景

整站统一图标入口。pixelarticons 自带 React 组件（800 图标，24×24，currentColor），但需要：(1) 强制 24 的倍数缩放避免锯齿；(2) 兜底图标库（缺失时降级用 emoji）；(3) 与 8bitcn-ui 提供的 4 个游戏化条（health-bar 等）解耦。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 PixelIcon 统一图标 wrapper。

# 项目背景
- pixelarticons：800 个免费图标，24×24 网格，currentColor。React 用法：
  import { Heart } from "pixelarticons/react/Heart";
  推荐尺寸 24 / 48 / 72 / 96（24 的倍数保持像素锐利）。
- 8bitcn-ui 提供 4 个游戏化条（health-bar / mana-bar / xp-bar / enemy-health-display）独立使用。
- 我们需要一个 PixelIcon 组件统一管理图标使用、命名、尺寸缩放、兜底。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风（无圆角 / 无渐变 / image-rendering: pixelated）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict
- pixelarticons npm 包

# 本任务范围
- frontend/src/components/pixel/PixelIcon.tsx
- frontend/src/components/pixel/iconCatalog.ts（项目用到的图标全索引）
- frontend/src/components/pixel/index.ts（追加导出）
- frontend/tests/component/pixel-icon.test.tsx

# 必须实现的内容

## iconCatalog.ts
- 列出整个项目用到的图标白名单（约 30 个），每个映射到 pixelarticons 的 React 组件名
- 用 dynamic import 实现 tree-shake：
  export const ICON_MAP = {
    "money": () => import("pixelarticons/react/Coin"),
    "morale": () => import("pixelarticons/react/Mood"),  // 笑脸
    "board": () => import("pixelarticons/react/Shield"),
    "face": () => import("pixelarticons/react/Sunglasses"),
    "calendar": () => import("pixelarticons/react/Calendar"),
    "users": () => import("pixelarticons/react/Users"),
    "fire": () => import("pixelarticons/react/Fire"),
    "trophy": () => import("pixelarticons/react/TrophyDuotone"),
    "alarm": () => import("pixelarticons/react/Alert"),
    "trending-up": () => import("pixelarticons/react/TrendingUp"),
    "trending-down": () => import("pixelarticons/react/TrendingDown"),
    "check": () => import("pixelarticons/react/Check"),
    "close-box": () => import("pixelarticons/react/CloseBox"),
    "loader": () => import("pixelarticons/react/Loader"),
    "message": () => import("pixelarticons/react/Message"),
    "briefcase": () => import("pixelarticons/react/Briefcase"),
    "edit": () => import("pixelarticons/react/Edit"),
    "search": () => import("pixelarticons/react/Search"),
    "heart": () => import("pixelarticons/react/Heart"),
    "rip": () => import("pixelarticons/react/Tombstone"),       // 死亡报告
    "news": () => import("pixelarticons/react/Article"),
    "speech": () => import("pixelarticons/react/Message"),
    "settings": () => import("pixelarticons/react/SlidersBox"),
    "save": () => import("pixelarticons/react/Save"),
    "menu": () => import("pixelarticons/react/Menu"),
    "back": () => import("pixelarticons/react/ChevronLeft"),
    "forward": () => import("pixelarticons/react/ChevronRight"),
    "lock": () => import("pixelarticons/react/Lock"),
    "unlock": () => import("pixelarticons/react/LockOpen"),
    "scissors": () => import("pixelarticons/react/Scissors"),  // 裁员
    "handshake": () => import("pixelarticons/react/Handshake"),  // 融资
    "megaphone": () => import("pixelarticons/react/Volume3"),  // 隐瞒坏消息
  } as const;
  export type IconName = keyof typeof ICON_MAP;

注：若 pixelarticons 实际无某图标名，用最接近的等价品（README 给的图标网格可索引）。

## PixelIcon.tsx
interface PixelIconProps {
  name: IconName;
  size?: 16 | 24 | 32 | 48 | 64 | 72 | 96;  // 严格限制
  color?: string;                              // 默认继承 currentColor
  className?: string;
  ariaLabel?: string;                          // a11y
}

实现：
- React.lazy + Suspense 加载具体图标组件（用 ICON_MAP[name]()）
- Suspense fallback：渲染一个同尺寸的 bg-panel-dim 占位方块
- 通过 inline style 设 width/height（必须 24 的倍数；16 例外用 16×16 像素图，禁止 14/18 等）
- image-rendering: pixelated 默认强制
- ariaLabel 缺省时 = name；提供时覆盖
- color 通过 style.color 传递（currentColor 继承）
- React.memo 包装，避免重渲染

兜底：
- 若 ICON_MAP[name] 不存在或加载失败 → 渲染 emoji 兜底（catalog 表自带 emoji fallback）
- catalog 增加 EMOJI_FALLBACK: Record<IconName, string>
- catalog 缺图标抛 dev warning + 默认 "❓"

## index.ts（追加）
export * from "./PixelIcon";
export type { IconName } from "./iconCatalog";

## pixel-icon.test.tsx
- size=24 时元素 width=24 / height=24
- size=14（TS 编译期已禁；运行期传入字符串测试）抛 dev warning
- name="money" 渲染（用 await screen.findBy... 处理 lazy）
- name="non-existent" 渲染 emoji fallback
- ariaLabel 缺省时 = name
- image-rendering: pixelated 在 computed style

# 禁止事项
- 禁止全量 import "pixelarticons/react"（破坏 tree-shake，体积爆炸）。
- 禁止 size 非 24 的倍数（除 16）。
- 禁止 PixelIcon 内调 fetch / 远程加载 SVG。
- 禁止用 inline SVG 字符串（必须走 pixelarticons React 组件）。
- 禁止把 ICON_MAP 散在多个文件（必须在 iconCatalog 集中维护）。
- 禁止 emoji fallback 在生产环境频繁出现（dev warning 提醒补 catalog）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. tree-shake 验证：bundle 分析（pnpm build 后看 dist）只含 catalog 使用的图标
3. ariaLabel / size 限制生效。
4. fallback 路径正常。
5. ICON_MAP ≥ 30 条。
6. image-rendering: pixelated 默认强制。
7. React.memo 包装。
8. Suspense fallback 是同尺寸方块。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令（pnpm test + pnpm build 后 ls dist/assets 看图标分布）
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `pixel/PixelIcon.tsx` / `iconCatalog.ts`
- 1 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. tree-shake 验证 bundle 仅含 catalog 中图标。
3. ariaLabel / size 限制生效。
4. fallback 路径正常。
5. `ICON_MAP` ≥ 30 条。
6. `image-rendering: pixelated` 默认强制。
7. `React.memo` 包装。
8. Suspense fallback 同尺寸方块。

### 禁止事项

- 禁止全量 `import "pixelarticons/react"`。
- 禁止 size 非 24 倍数（16 例外）。
- 禁止 PixelIcon 内 fetch。
- 禁止 inline SVG 字符串。
- 禁止 ICON_MAP 散落。
- 禁止生产频繁 emoji fallback。

---

## 任务 8：PixelSpeechBubble + PixelPortrait（对话气泡 + 立绘）

### 使用场景

原型图 Image 1/2/4/6/7/9 大量使用**角色立绘 + 对话气泡**组合（CEO / 员工 / 同事三类）。本任务实现两个核心组件：

- `PixelPortrait`：左下角的大头像立绘（240×320 像素图），支持表情切换
- `PixelSpeechBubble`：右上方的对话气泡，含说话人标签 + 内容 + 信任度小条

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 PixelSpeechBubble + PixelPortrait 立绘对话组件。

# 项目背景
原型图大量出现"左侧角色立绘 + 右侧对话气泡"组合（开场任命书页 / 茶水间 / 决策页 / 死亡报告等）。
立绘风格：半身像，240×320 像素分辨率，色调统一（参考原型图 Image 1/2/4 的男 CEO 与女员工立绘）。
对话气泡：带"说话人标签条 + 文本区 + 信任度 / 标签"。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风（无圆角 / 无渐变 / image-rendering: pixelated）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict
- Tailwind
- 打字机动画用 setInterval（不引动画库）

# 本任务范围
- frontend/src/components/pixel/PixelPortrait.tsx
- frontend/src/components/pixel/PixelSpeechBubble.tsx
- frontend/src/components/pixel/portraitCatalog.ts（立绘资源索引）
- frontend/src/components/pixel/index.ts（追加导出）
- frontend/src/anim/typewriter.ts
- frontend/public/sprites/portraits/README.md（说明立绘资源命名规范 + 来源 / 占位）
- frontend/tests/component/pixel-portrait.test.tsx
- frontend/tests/component/pixel-speech-bubble.test.tsx
- frontend/tests/unit/typewriter.test.ts

# 必须实现的内容

## portraitCatalog.ts
export type PortraitId =
  | "ceo_male_01"        // 主角男 CEO（原型 Image 1/2）
  | "ceo_male_02"        // 备选男 CEO（原型 Image 9）
  | "ceo_female_01"      // 女 CEO（备选）
  | "employee_zhang_chi" // 张弛（原型 Image 3 压力 85%）
  | "employee_lin_xiaoman" // 林小满（原型 Image 4 市场部专员）
  | "board_chairman"     // 王董 / 林董 / 周董
  | "rival_ceo"          // 竞品 CEO
  | "advisor";           // CFO / 顾问

export type PortraitExpression = "neutral" | "smile" | "tired" | "angry" | "confident" | "anxious" | "rip";

export interface PortraitMeta {
  id: PortraitId;
  baseSrc: string;       // /sprites/portraits/{id}/neutral.png
  expressions: PortraitExpression[];
  // 每个 expression 对应一张 240×320 sprite；缺失则回退 neutral
}

export const PORTRAIT_CATALOG: Record<PortraitId, PortraitMeta>;

注：本任务不真做美术；只建立 catalog + 占位文件路径，README 写明命名规范。

## PixelPortrait.tsx
interface PixelPortraitProps {
  id: PortraitId;
  expression?: PortraitExpression;   // 默认 "neutral"
  size?: "sm" | "md" | "lg" | "hero"; // 96 / 144 / 192 / 320
  position?: "left" | "right" | "inline";  // 屏幕停靠
  bounce?: boolean;                  // 说话时上下浮动 4px 周期 1s
  className?: string;
}

实现：
- <img src=catalog 决定路径 width/height 整数倍 image-rendering: pixelated
- expression 缺失文件时 console.warn + 回退 neutral
- position="left" 时 absolute bottom-0 left-0；right 时 right-0
- bounce 时 animate-bounce-pixel（自定义 keyframe，整数像素位移：translateY(0/-4px) 切换）
- 无圆角；可在四边像素描边可选 prop framed

## PixelSpeechBubble.tsx
interface PixelSpeechBubbleProps {
  speaker?: { name: string; role?: string };  // "林小满 | 市场部专员"
  text: string;
  typewriter?: boolean;                        // 是否启用打字机
  typewriterSpeedMs?: number;                  // 默认 30
  trust?: { value: number; label: string };    // 底部信任度小条 "信任度 62 友好"
  tone?: "friendly" | "neutral" | "hostile" | "thinking" | "rip";
  arrow?: "left" | "right" | "down" | "none";  // 指向立绘的小三角
  collapsible?: boolean;                       // 右上角箭头折叠
  onComplete?: () => void;                     // 打字机完成时回调
  className?: string;
}

视觉规则（依据原型图 Image 4）：
- 容器：bg-panel + 2px stroke-ink + 4px hard shadow + 内边距 px-px-md
- 顶部说话人条：bg-ink-1 + 白字 + 角色名 + "|" + 岗位（如 "林小满 | 市场部专员"）
- 文本区：font-pixel + ink-1 + line-height 1.4
- arrow：用 CSS 画小三角（border 技巧），指向立绘
- trust 条：左下角 + 笑脸 icon + 数字 + 标签（绿色"友好" / 灰"中立" / 橙"敌意"）
- tone 控制底色微调（friendly 默认 / hostile 极淡红底）
- collapsible 右上角 ChevronDown 图标可点击折叠

打字机：
- 字符按 typewriterSpeedMs 30ms/字 逐个 append
- 用户按任意键 → 立刻全显
- 打字过程中显示光标 _ 闪烁（300ms 周期）

## anim/typewriter.ts
export function useTypewriter(
  text: string,
  enabled: boolean,
  speedMs = 30,
): { display: string; isDone: boolean; skip: () => void; reset: () => void; };
- 用 useEffect + setInterval
- enabled=false 时 display = text
- 卸载时清 interval
- speedMs ≤ 5 时直接全显

## sprites/portraits/README.md
- 命名规范：sprites/portraits/<id>/<expression>.png（240×320）
- 调色板限 32 色 + 透明
- 不提交 binary 到仓库；CI 拉取或占位 placeholder.png（10×10 灰色透明）
- 美术输出工具推荐：Aseprite

## 测试
- pixel-portrait.test.tsx:
  · 默认 size=md → width=144
  · expression 缺失文件时回退 neutral（mock <img> onerror）
  · bounce=true 时 className 含 animate-bounce-pixel
- pixel-speech-bubble.test.tsx:
  · text 渲染
  · speaker 渲染顶部条
  · trust 渲染底部条
  · typewriter=true 时初始 display=""，1.5s 后 display=text（fakeTimers）
  · 按任意键触发 skip → display=text
  · onComplete 在打字完成时调用
- typewriter.test.ts:
  · 默认 30ms/字
  · enabled=false 直接全显
  · skip 立刻 isDone=true
  · 卸载清 interval（用 useEffect cleanup spy）

# 禁止事项
- 禁止立绘用 SVG 路径（必须 raster PNG / WebP，integer scale only）。
- 禁止打字机用 CSS animation typing（必须 React state，便于 skip）。
- 禁止气泡圆角 > 0 / 阴影 blur > 0。
- 禁止 trust 字段值超过 0–100 范围（运行期 console.warn）。
- 禁止在组件内 fetch 远程立绘 URL（资源本地）。
- 禁止 bounce 周期 < 500ms / 位移 > 8px（破坏像素稳定感）。

# 验收标准
1. 所有测试全过；tsc / eslint 零错误。
2. 立绘 image-rendering: pixelated；尺寸整数倍。
3. 打字机 30ms/字；skip 工作；onComplete 触发。
4. 气泡 4 种 tone / 4 种 arrow 视觉正确。
5. 角色 catalog ≥ 8 条；命名规范文档完整。
6. trust 条颜色按数字桶映射（≥70 绿 / 30–70 灰 / <30 橙）。
7. typewriter useEffect 卸载清 interval。
8. 单文件 ≤ 300 行。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码 + sprites README
4. 验证命令
5. 声明"未启动后端、未调 LLM、未下载真实立绘资源"
````

### 预期产物

- `pixel/PixelPortrait.tsx` / `PixelSpeechBubble.tsx` / `portraitCatalog.ts`
- `anim/typewriter.ts`
- `public/sprites/portraits/README.md`
- 3 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 立绘 pixelated + 整数倍。
3. 打字机 30ms/字；skip / onComplete OK。
4. 气泡 4 tone / 4 arrow 视觉正确。
5. catalog ≥ 8 条。
6. trust 颜色按桶映射。
7. typewriter cleanup 正确。
8. 单文件 ≤ 300 行。

### 禁止事项

- 禁止立绘 SVG。
- 禁止打字机用 CSS animation typing。
- 禁止圆角 / 阴影 blur。
- 禁止 trust 超范围。
- 禁止远程 fetch 立绘。
- 禁止 bounce 周期 < 500ms / 位移 > 8px。

---

## 任务 9：PixelMetricCard + PixelLineChart / PixelBarChart（像素图表）

### 使用场景

原型图 Image 5（六大核心数据）/ Image 7（趋势图）需要像素风的指标卡 + 趋势图。**禁止用 Chart.js / Recharts**（它们渲染会破坏像素感），手写 canvas 离散像素点 + 整数坐标。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 PixelMetricCard + 像素风趋势图。

# 项目背景
原型图：
- Image 5 六大核心数据：6 个指标卡，每卡含"标题条 + 数字 + 状态文字 + 小趋势 sparkline + delta 标"
- Image 7 公司经营总览：本周趋势折线图（4–6 条线，整数像素点）
- Image 12 季度结算：4 指标对比卡（上季 / 本季 + 状态色）

禁止用 Chart.js / Recharts 等抗锯齿库，必须 canvas 自绘整数坐标点 + 1px 像素线段。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风（image-rendering: pixelated 强制）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict
- 原生 canvas 2d
- 不引图表库

# 本任务范围
- frontend/src/components/pixel/PixelMetricCard.tsx
- frontend/src/components/pixel/PixelLineChart.tsx
- frontend/src/components/pixel/PixelBarChart.tsx
- frontend/src/components/pixel/PixelSparkline.tsx
- frontend/src/components/pixel/index.ts（追加导出）
- frontend/src/anim/numberRoll.ts
- frontend/tests/component/pixel-metric-card.test.tsx
- frontend/tests/component/pixel-line-chart.test.tsx
- frontend/tests/component/pixel-bar-chart.test.tsx
- frontend/tests/unit/number-roll.test.ts

# 必须实现的内容

## PixelMetricCard.tsx（对齐原型 Image 5 / Image 12）
interface PixelMetricCardProps {
  metricKey: "CASH" | "MORALE" | "BOARD" | "FACE" | "SALES" | "MKT";  // v1 只用前 4
  label: string;                  // "现金流"
  iconName: IconName;             // "money" / "morale" / ...
  value: number;                  // 当前数值
  statusText: string;             // "健康" / "稳定" / "中立" / "危险"
  delta?: { value: number; unit?: string };  // "+2,500 /小时"
  sparkline?: number[];           // 近 7 期数据，画 60×16 小图
  variant: "blue" | "green" | "orange" | "red" | "purple" | "gold";  // 标题条色
  size?: "compact" | "default" | "hero";  // 不同屏幕用不同尺寸
  onClick?: () => void;
}

视觉（依据 Image 5 卡片设计）：
- 卡片：bg-panel + 2px stroke + 4px hard
- 标题条：上方 32px 高，左右文字居中
- 主体：emoji-like icon 左 + 大数字 (font-retro xxl 24px) + 状态小字
- 底部：sparkline + delta 文字（绿正/红负 + 箭头 icon）
- 数字滚动动画：使用 useNumberRoll hook，从旧值到新值 400ms 阶梯（整数步进，不要 ease）

## PixelLineChart.tsx
interface PixelLineChartProps {
  width: number;             // 必须 4 的倍数
  height: number;            // 必须 4 的倍数
  series: { id: string; label: string; color: string; values: number[] }[];
  xLabels: string[];         // ["周一","周二",...]
  yMin?: number;             // 默认 0
  yMax?: number;             // 默认 100
  yTicks?: number[];         // 默认 [0, 25, 50, 75, 100]
  showGrid?: boolean;
  showLegend?: boolean;
}

绘制规则：
- canvas 2d
- context.imageSmoothingEnabled = false
- 坐标四舍五入到整数像素
- 折线用 Bresenham 像素步进（自写函数）
- 每个数据点画 3×3 像素方块（按 color）
- 网格线 1px 半透明灰（用 alpha = 1，颜色 #DDD，非 alpha 透明）
- 图例右上角小色块 + 标签
- 数据更新时重新整张绘制（不做插帧动画）

## PixelBarChart.tsx
- 类似 LineChart 但条形
- 每条 bar 宽度 = (width - 2*padding) / N，整数像素
- bar 顶部 1px 描边深色，制造像素硬边
- 数值标签在 bar 顶上方

## PixelSparkline.tsx
- 简化版 LineChart，60×16 默认尺寸
- 仅 1 条线 + 颜色 + 无坐标轴
- 用于 PixelMetricCard 底部

## anim/numberRoll.ts
export function useNumberRoll(target: number, durationMs = 400): number;
- 用 requestAnimationFrame
- 阶梯模式：duration / Math.max(1, |target - current|) 时间间隔
- 数值取整（无小数动画）
- 卸载时 cancelAnimationFrame

## 测试
- pixel-metric-card.test.tsx:
  · 6 个 variant 渲染正确
  · delta > 0 显示绿色 + up arrow
  · delta < 0 显示红色 + down arrow
  · sparkline 渲染 (canvas 存在)
  · 数字从旧值动画到新值
- pixel-line-chart.test.tsx:
  · canvas 存在 width/height 整数倍
  · imageSmoothingEnabled=false
  · 数据点画了 3×3 方块（mock canvas spy）
  · 图例渲染
- pixel-bar-chart.test.tsx:
  · 类似 line chart
- number-roll.test.ts:
  · target=100 从 0 开始，400ms 后 = 100
  · 卸载 cancel rAF（spy）

# 禁止事项
- 禁止用 Chart.js / Recharts / Victory / d3-shape。
- 禁止 canvas 启用抗锯齿。
- 禁止坐标小数（必须 Math.round）。
- 禁止 sparkline 含坐标轴 / 图例。
- 禁止数字动画用 CSS transition（必须 React state + rAF）。
- 禁止 bar 之间圆角 / 渐变。
- 禁止 v1 阶段渲染 SALES / MKT 卡（除非 env.NEXT_PUBLIC_METRIC_V2==="on"）。

# 验收标准
1. 所有测试全过；tsc / eslint 零错误。
2. canvas imageSmoothingEnabled=false。
3. 坐标整数化。
4. 数字滚动动画无小数。
5. 6 variant 颜色与 §0.10 调色板一致。
6. v1 SALES/MKT 卡默认不渲染。
7. 单文件 ≤ 300 行。
8. canvas 缩放在 200% 浏览器下仍像素锐利。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `pixel/PixelMetricCard.tsx` / `PixelLineChart.tsx` / `PixelBarChart.tsx` / `PixelSparkline.tsx`
- `anim/numberRoll.ts`
- 4 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. canvas 抗锯齿关闭。
3. 坐标整数化。
4. 数字动画无小数。
5. 6 variant 颜色与调色板一致。
6. v1 SALES / MKT 默认不渲染。
7. 单文件 ≤ 300 行。
8. 200% 缩放下仍像素锐利。

### 禁止事项

- 禁止 Chart.js / Recharts / Victory / d3-shape。
- 禁止 canvas 抗锯齿。
- 禁止坐标小数。
- 禁止 sparkline 含坐标轴。
- 禁止数字动画用 CSS transition。
- 禁止 bar 圆角 / 渐变。
- 禁止 v1 渲染 SALES / MKT。

---

## 任务 10：HUDBar（顶部 - Logo + 日期 + 4–6 指标）

### 使用场景

原型图所有屏幕顶部都有 HUDBar。本任务实现完整可交互版本（替换任务 3 的占位）。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 HUDBar 顶部信息条（替换任务 3 占位）。

# 项目背景
HUDBar 出现在所有屏幕顶部（Onboarding / DeathReport 可隐藏，由任务 3 setChrome 控制）。
内容：左侧 LOGO（"YES, BOSS!"）+ 日期卡片 + 4 个核心指标 + 右侧可选员工人数 / 设置入口。
原型图 Image 1/2/3/4/5/8/12 顶部都展示了这条 HUDBar。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store；HUDBar 只从 selector 读不改 store。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict
- Zustand selector

# 本任务范围
- frontend/src/components/layout/HUDBar.tsx（替换占位）
- frontend/src/components/layout/hudParts/LogoBadge.tsx
- frontend/src/components/layout/hudParts/DateCard.tsx
- frontend/src/components/layout/hudParts/HUDMetric.tsx
- frontend/src/components/layout/hudParts/index.ts
- frontend/tests/component/hud-bar.test.tsx
- frontend/tests/component/hud-metric.test.tsx

# 必须实现的内容

## LogoBadge.tsx
- 文本 "YES,BOSS!" 用 Press Start 2P 24px
- 黄底斜体效果：用 transform: skewX(-8deg) + bg-exp-gold + 2px stroke
- 右上角 ✨ pixelarticon "sparkles"（若存在；不存在用 emoji 兜底）
- 高度 48px 适配 HUDBar 64px 总高

## DateCard.tsx
interface Props { day: number; time: string; weekday: string; date: string; }
- 视觉：bg-panel + 2px stroke + 4px hard + 内含日历 icon + "第N天 HH:MM" + "周X YYYY/MM/DD"
- 紧凑 280×56

## HUDMetric.tsx
interface Props {
  icon: IconName;
  label: string;        // "现金流"
  value: string;        // "¥100,000"
  delta?: string;       // "+2,500"
  status?: string;      // "健康" / "稳定" / "中立"
  unit?: string;        // "/小时"
  variant: "blue"|"green"|"orange"|"red"|"purple"|"gold";
  dimmed?: boolean;     // v1 时 SALES/MKT 灰显
}
- 行内式：icon + label 上面 + 大数字下面 + delta 小字
- 宽度自适应；不超 220px
- 危机色（value 等触发死亡阈值时）整体闪烁 alarm-red

## HUDBar.tsx
- 总高 64px / bg-panel / 2px stroke 底部
- flex row 排列：[LogoBadge] [DateCard] [4–6 HUDMetric] [可选：员工人数 / Settings]
- 从 useGameStore + useScreenStore selector 取数据：
  · LogoBadge：静态
  · DateCard：snapshot?.quarter.number 推算 "第N天 HH:MM"；时间用 mock = 09:15；date = "2025/05/26"（mock 固定）
  · HUDMetric × 4：CASH/MORALE/BOARD/FACE
  · HUDMetric × 2 灰显：SALES/MKT（v1 时 dimmed=true）
  · 员工人数：snapshot.company.employeeCount（mock 5/20）
- 响应式：1280 宽完整展示；< 1280 隐藏 SALES/MKT；< 1024 隐藏 status 文字
- 设置入口：右上角 settings icon，点击 push 到 stats-dashboard 屏幕（或弹一个 PixelDialog 占位）

# 数据来源
- 必须用 selector：useGameStore(selectCurrentStats) 等
- 如果 snapshot=null（启动初态）→ HUDBar 显示占位（"-/-"），不崩

## 测试
- hud-bar.test.tsx:
  · 默认渲染 LogoBadge / DateCard / 4 个 HUDMetric
  · v1 时 SALES/MKT dimmed=true
  · snapshot=null 时显示占位不崩
  · 响应式：宽度 800 时隐藏 SALES/MKT（设 window innerWidth + resize）
- hud-metric.test.tsx:
  · variant 6 种颜色
  · dimmed 时 opacity 0.5 + bg-panel-dim
  · delta > 0 绿 / < 0 红
  · 危机值（CASH ≤ 5）闪烁 alarm-red

# 禁止事项
- 禁止 HUDBar 写业务逻辑（store mutation 不能在这里发起）。
- 禁止 HUDBar 显示后端未发送的字段。
- 禁止 v1 阶段 SALES/MKT 真填值（必须显示"v2"占位或 dimmed）。
- 禁止 LogoBadge 圆角。
- 禁止 emoji 直接当 icon（用 PixelIcon）。
- 禁止 HUDBar 高度 > 64px。
- 禁止用 useEffect 周期性更新时间（mock 固定即可；真接入由 DateCard 后续任务扩展）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. HUDBar 高度 = 64px。
3. 渲染 4 指标 + 2 灰显（v1）。
4. 响应式正确（1280 / 1024 / 800 宽度）。
5. 危机值闪烁。
6. snapshot=null 不崩。
7. 完全从 selector 读取，不写 store。
8. 视觉对齐原型 Image 1/3/5 顶部 HUD。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `layout/HUDBar.tsx` + 3 个 hudParts/* 子组件
- 2 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 高度 = 64px。
3. 4 指标 + 2 灰显。
4. 响应式正确。
5. 危机值闪烁。
6. snapshot=null 不崩。
7. 只读 selector。
8. 视觉对齐原型 Image 1/3/5 顶部。

### 禁止事项

- 禁止写业务。
- 禁止显示后端未发字段。
- 禁止 v1 真填 SALES/MKT。
- 禁止 LogoBadge 圆角。
- 禁止 emoji 当 icon。
- 禁止高度 > 64px。
- 禁止 useEffect 周期更新时间。

---

## 任务 11：MainActionBar（底部 - 4 大彩色按钮 + 上下文徽标）

### 使用场景

原型图 Image 2/3/4/5/7/8 底部固定显示 4 大按钮：**公司管理（蓝） / 员工沟通（绿） / 项目推进（橙） / 财务决策（红）**。本任务实现可交互版本（替换任务 3 占位）。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 MainActionBar 底部 4 大主操作按钮。

# 项目背景
4 大按钮固定在底部所有正式屏幕（Onboarding / DeathReport 可隐藏）。
- 公司管理（蓝 pixel-blue）：跳 OverviewScreen 或 StatsDashboard
- 员工沟通（绿 pixel-green）：跳 OfficeScene 探索
- 项目推进（橙 pixel-orange）：v1 暂跳 DecisionScreen 入口
- 财务决策（红 pixel-red）：v1 暂跳 DecisionScreen（财务卡分类）

每个按钮支持 :hover 浮起 + :active 按下；带 hotkey 数字 1/2/3/4；带"红点徽标"提示新事件（如未读消息 / 待办）。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store；MainActionBar 只 read selector + 触发 screenStore push。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict

# 本任务范围
- frontend/src/components/layout/MainActionBar.tsx（替换占位）
- frontend/src/components/layout/MainActionButton.tsx
- frontend/src/components/layout/contextHint/ContextHint.tsx
- frontend/tests/component/main-action-bar.test.tsx
- frontend/tests/component/main-action-button.test.tsx

# 必须实现的内容

## MainActionButton.tsx
interface Props {
  variant: "blue"|"green"|"orange"|"red";
  icon: IconName;          // "briefcase"|"users"|"trending-up"|"money"
  label: string;           // "公司管理"
  hotkey: "1"|"2"|"3"|"4";
  badgeCount?: number;     // 右上红点数字（>9 显示 "9+"）
  disabled?: boolean;      // 死亡 / 未开始时禁用
  onClick: () => void;
}

视觉（依据 Image 3/4/5/7/8/12 底部按钮）：
- 高 56px / 宽自适应 / flex 1
- bg = variant 主色 / 白字
- 2px stroke + 4px hard shadow
- icon (24×24 白) + label (px-md 16px) 水平排列
- hotkey 显示在右下角，font-retro 10px
- badgeCount > 0 时右上 14×14 alarm-red 方块 + 白字
- hover：translateY(-2px) + shadow 偏移 6px
- active：translateY(2px) + shadow 0
- disabled：bg-panel-dim + 灰字 + 无 shadow

## MainActionBar.tsx
- 总高 72px / bg-canvas + 顶部 2px stroke / padding x px-px-base
- flex row gap px-md
- 左侧可选 ContextHint（一句话提示 "记住：短期能活下去..."；原型 Image 8 / Image 5 底部都展示）
- 4 个 MainActionButton flex-1 平铺
- 监听 1/2/3/4 数字键触发对应按钮（仅当焦点不在 input/textarea 内）

## ContextHint.tsx
interface Props { speaker?: PortraitId; text: string; }
- 左侧 32×32 小头像（PixelPortrait size=sm）
- 右侧文字气泡（极简版 PixelSpeechBubble）
- 宽度自适应 max 360px

## 数据
- 4 按钮的 onClick：
  · 公司管理 → screenStore.push("overview")
  · 员工沟通 → screenStore.push("office")
  · 项目推进 → push("decision") （v1 提示 "v2 即将开放"）—— 暂时 push 但加 toast 提示
  · 财务决策 → push("decision")
- badgeCount 来自 selector：未读消息 / 待办（v1 mock 全 0；可在 fixture 注入）
- disabled 条件：snapshot.status !== "active"
- ContextHint.text：从 selector 取当前阶段提示语（v1 mock 一组固定字串）

## 测试
- main-action-button.test.tsx:
  · 4 variant 渲染
  · click 触发 onClick
  · disabled 时 click 不触发
  · hotkey 显示
  · badgeCount > 9 显示 "9+"
- main-action-bar.test.tsx:
  · 默认渲染 4 按钮
  · 按 "1" 触发第 1 按钮
  · 焦点在 textarea 时按 "1" 不触发（避免误操作）
  · snapshot.status="dead" 时所有按钮 disabled

# 禁止事项
- 禁止按钮内写业务逻辑（必须 onClick → screenStore action）。
- 禁止 ContextHint 文字硬编码（必须从 selector 取，本任务用 mock 固定字串占位但带 TODO 标签）。
- 禁止 hotkey 在 input/textarea 焦点时触发。
- 禁止 badgeCount 显示 0（必须 hidden）。
- 禁止圆角 / 渐变 / 阴影 blur。
- 禁止 4 按钮位置可拖拽 / 可调换。
- 禁止把 6 大入口扩成更多（v1 死守 4 大）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 4 按钮颜色对齐 §0.10 调色板（blue/green/orange/red）。
3. hotkey 数字键触发；input 焦点时不触发。
4. badgeCount 显示规则正确。
5. disabled 全套样式正确。
6. ContextHint 渲染左侧头像 + 右侧文字。
7. 单文件 ≤ 300 行。
8. 视觉对齐原型 Image 3/4/5/7/8/12 底部。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `layout/MainActionBar.tsx` + `MainActionButton.tsx` + `ContextHint.tsx`
- 2 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 4 按钮色对齐调色板。
3. hotkey 数字触发；input 焦点不触发。
4. badge 规则正确。
5. disabled 全套样式。
6. ContextHint 左头像 + 右文字。
7. 单文件 ≤ 300 行。
8. 视觉对齐原型底部。

### 禁止事项

- 禁止按钮内写业务。
- 禁止 ContextHint 硬编码。
- 禁止 hotkey 在 input 焦点触发。
- 禁止 badgeCount 显示 0。
- 禁止圆角 / 渐变 / 阴影 blur。
- 禁止位置可拖拽。
- 禁止扩成更多入口（守 4）。

---

## 任务 12：OnboardingScreen（开场任命书）

### 使用场景

原型 Image 1 / Image 2 —— 玩家首次打开游戏看到的"空降上任 / 欢迎，新任 CEO"页面。**全屏立绘 + 任命书 + 入职流程列表 + "开始任职"按钮**。可隐藏 MainActionBar。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 OnboardingScreen 开场任命书页。

# 项目背景
原型图（Image 1 / Image 2）：
- 左侧 / 中部：CEO 全身立绘（240×320 hero size）
- 中部右侧：任命卡片"空降上任 / 欢迎，新任 CEO"标题 + 几张状态卡（公司现金流告急 / 员工正在观望 / 今天是你上任第一天）
- 右侧：入职流程进度（董事会任命 → 到岗报到 → 进入办公室）或当前压力清单
- 底部：[开始任职] 主按钮 + [查看背景] [设置] 次按钮

视觉关键：办公室白色背景照片虚化（v2 用纯色 + 像素办公室占位 sprite 替代真实照片）。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风（禁止照片背景；用 sprite 拼贴）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict
- 复用任务 6/8 组件

# 本任务范围
- frontend/src/components/screens/OnboardingScreen.tsx
- frontend/src/components/screens/onboarding/IntroCard.tsx
- frontend/src/components/screens/onboarding/StatusList.tsx
- frontend/src/components/screens/onboarding/JourneySteps.tsx
- frontend/public/sprites/scenes/lobby_bg.png（占位 README）
- frontend/tests/component/onboarding-screen.test.tsx

# 必须实现的内容

## OnboardingScreen.tsx
- useEffect 进入时 setChrome({ hud: true, mainBar: false })
- 离开时 setChrome 恢复
- 布局：
  · 顶部 HUDBar（保留，与 §0.8 一致）
  · 主区域 grid 12 列：
    - col-span-5：PixelPortrait id="ceo_male_01" expression="confident" size="hero" position="left"
    - col-span-5：IntroCard（标题 + StatusList）
    - col-span-2：JourneySteps
  · 主按钮居中 PixelButton variant="blue" hotkey="Enter" label="开始任职"
- "开始任职"点击：
  · 触发 dataSource.send(makeEnvelope("create_game", { requestLegacies: true }))
  · 不等待响应，立即 screenStore.replace("company-select")
  · 后端 game_snapshot 到达时，ingestSnapshot 自动写入 store；CompanySelectScreen 监听 selectCurrentSnapshot 触发后续

## IntroCard.tsx
- 内含 PixelCard：
  · 标题 "空降上任" / "欢迎，新任 CEO" 32px font-pixel
  · 副标题 "董事会把最后一张牌交给了你" 16px ink-2
  · 内嵌 3 张迷你状态卡（StatusList）

## StatusList.tsx
items: { icon: IconName; title: string; subtitle?: string; tone: "info"|"warn"|"alert" }[]
- 渲染 3–4 条
- 每条左 icon + 右文字
- tone="alert" 时左侧加 alarm-red 竖条

## JourneySteps.tsx
- 垂直时间线 3 步
- 当前步：filled circle + pixel-blue
- 已完成：filled gray + check
- 未来步：empty circle

## 数据
- 文案 v1 直接硬编码 mock（标题 / 副标题 / 3 条状态）；后续可走 cms 但本任务不做
- 入职流程文本：["董事会任命","到岗报到","进入办公室"]（mock）

## 测试
- onboarding-screen.test.tsx:
  · 默认渲染 3 卡片 + 立绘 + 按钮
  · 进入时 mainBar 隐藏（setChrome called）
  · 离开时 setChrome 恢复（unmount cleanup）
  · 点 "开始任职" → screenStore.replace("company-select") 被调
  · dataSource.send 被调 1 次 with type="create_game"
  · 键盘 Enter 等价点击

# 禁止事项
- 禁止用真实办公室照片做背景（v2 必须像素 sprite）。
- 禁止 OnboardingScreen 写网络重试 / 错误处理（统一在网络层）。
- 禁止把"开始任职"按钮 disable 等待后端响应（必须立即跳转，乐观式）。
- 禁止文案硬编码到组件内部样式（必须可被 prop 覆盖以便后期 cms）。
- 禁止立绘旁加渐变 / 高斯模糊。
- 禁止在本任务实现 [查看背景] [设置] 弹窗（占位按钮，console.log 即可，TODO 标注任务 22 完善）。
- 禁止隐藏 HUDBar（只隐藏 MainActionBar）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. setChrome 进出生命周期正确。
3. 开始任职按钮 send + 立即跳转。
4. 视觉对齐 Image 1 / Image 2（立绘 + 任命卡 + 入职步骤）。
5. 立绘 image-rendering: pixelated。
6. 文案可 prop 覆盖。
7. 单文件 ≤ 300 行。
8. 1024 / 1280 / 1920 宽度均可读。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/OnboardingScreen.tsx` + `onboarding/{IntroCard,StatusList,JourneySteps}.tsx`
- `sprites/scenes/lobby_bg` 占位 README
- 1 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. setChrome 进出生命周期正确。
3. 开始任职：send + 立即跳转。
4. 视觉对齐 Image 1/2。
5. 立绘 pixelated。
6. 文案可 prop 覆盖。
7. 单文件 ≤ 300 行。
8. 多宽度可读。

### 禁止事项

- 禁止用真实照片做背景。
- 禁止写网络重试。
- 禁止按钮等待后端 disable。
- 禁止文案硬编码到组件样式。
- 禁止立绘旁渐变 / 模糊。
- 禁止本任务实现 [查看背景] [设置] 弹窗。
- 禁止隐藏 HUDBar。

---

## 任务 13：CompanySelectScreen（空降 CEO 3 选 1 + 复仇进度）

### 使用场景

原型 Image 9 / Image 11 —— "空降 CEO" 3 公司选择页。**3 张公司卡（灵眸科技 / 源语互动 / 钛深智能）+ 右侧复仇进度（总声望 / 累计持股 / 已解锁内容 / 里程碑）**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 CompanySelectScreen 空降 CEO 选择页。

# 项目背景
原型 Image 9 / 11：
- 标题"空降 CEO"+ 副标题"打工人复仇：从空降 CEO 到大股东"
- 中部 3 张公司卡：图标 + 名字 + 业务 tag + 简介 + "当前个人持股"
- 右侧"复仇进度"面板：声望 / 累计持股 / 已解锁内容 + 里程碑列表（30 / 10% / 50 / 25% / 100 / 50% / 200 等）
- 底部：[开始这一局] 主按钮 + [查看规则] [读取进度] 次按钮 + 左下角 PixelSpeechBubble "董事会把这家烂公司丢给了你..."

v1 数据：mock 3 家公司 + meta_progress（声望 / 持股 / 已解锁数）；后续从后端 GameSnapshot.metaSummary 取真实数据。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict
- 复用 PixelCard / PixelButton / PixelPortrait / PixelSpeechBubble

# 本任务范围
- frontend/src/components/screens/CompanySelectScreen.tsx
- frontend/src/components/screens/companySelect/CompanyChoiceCard.tsx
- frontend/src/components/screens/companySelect/RevengeProgressPanel.tsx
- frontend/src/components/screens/companySelect/MilestoneList.tsx
- frontend/tests/component/company-select-screen.test.tsx

# 必须实现的内容

## CompanyChoiceCard.tsx
interface CompanyChoice {
  templateId: string;
  name: string;
  business: string;
  tag: "AI"|"游戏研发"|"工业科技"|"消费硬件";
  iconName: IconName;
  brandColor: "blue"|"green"|"purple"|"orange";
  description: string;        // ≤ 60 字
  currentStakePercent: number; // 玩家当前持股
}

interface Props {
  choice: CompanyChoice;
  selected: boolean;
  onSelect: () => void;
}

视觉（依据 Image 9 / 11）：
- PixelCard kind="info" + 顶部 brandColor 标题条 + 居中 公司大图标 64×64
- 中部：name 20px + tag 12px + description 12px ink-2
- 底部：分隔线 + "当前个人持股" + 大数字 (font-retro xxl) brandColor
- selected 时 outline + check icon
- hover：浮起 -4px

## RevengeProgressPanel.tsx
- PixelCard kind="default" + 标题 "复仇进度" (target icon)
- 3 行大数据：总声望 / 累计持股 % / 已解锁内容 X / Y
- 底部"里程碑"小标题 + MilestoneList

## MilestoneList.tsx
items: { value: number | string; label: string; achieved: boolean }[]
- 滚动列表 6–10 条
- 每条 left: icon (star/coin/trophy) + value + label + right: 完成态绿 check / 未完成灰 lock
- 已完成可点击查看（v1 暂不弹窗）

## CompanySelectScreen.tsx
- useEffect setChrome({ hud: true, mainBar: false })
- 主布局 grid 12 列：
  · col-span-2：PixelPortrait id="ceo_male_01" expression="tired" size="hero"（疲倦版立绘，对齐 Image 11 略憔悴）
  · col-span-7：3 张 CompanyChoiceCard 横排
  · col-span-3：RevengeProgressPanel
- 底部居中：PixelButton "开始这一局" + 次级 PixelButton "查看规则" / "读取进度"
- 左下：PixelSpeechBubble "董事会把这家烂公司丢给了你。先活下来，再把他们全收拾了。"
- 选中 1 家后 "开始这一局" 才 enabled
- 点击 "开始这一局" → dataSource.send(create_game with chosen template)
  · 后端响应 game_snapshot → 自动 ingest
  · 立即 screenStore.replace("office")

## 数据
- 3 家公司 mock（写死在本组件 fixtures；最终从后端 GameSnapshot.companyTemplates 拉，但 v1 写死）：
  · C-01 灵眸科技 / AI / blue / "计算机视觉芯片..." / 12.5%
  · C-02 源语互动 / 游戏研发 / green / "独立游戏研发..." / 5.1%
  · C-03 钛深智能 / 工业科技 / purple / "工业 AI 解决方案..." / 0%
- meta_progress mock：声望 1350 / 累计持股 18% / 已解锁 3/10
- 里程碑：30 / 50 / 100 / 200 声望 + 10% / 25% / 50% 持股 + 终极目标"成为最大股东"

## 测试
- company-select-screen.test.tsx:
  · 3 张公司卡渲染
  · 默认无选中 → 开始按钮 disabled
  · 点击第 1 张 → outline + 开始按钮 enabled
  · 切换选中：第 2 张被点 → 第 1 张取消 outline
  · 点击开始 → dataSource.send 调用 + screenStore.replace("office")
  · MilestoneList achieved=true 显示 check / false 显示 lock
  · setChrome 进入隐藏 mainBar / 离开恢复

# 禁止事项
- 禁止超过 3 家公司展示（v1 死守 3）。
- 禁止 brandColor 用调色板之外的颜色。
- 禁止"开始这一局"按钮在无选中时仍可点（必须 disabled）。
- 禁止里程碑用渐变进度条（必须像素离散）。
- 禁止 RevengeProgressPanel 在 meta=null 时崩（默认 0 占位）。
- 禁止"查看规则" / "读取进度" 本任务实现（占位 console.log + toast "敬请期待"）。
- 禁止公司图标用真实 logo 图片（必须 PixelIcon）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 3 家公司卡 + 复仇进度对齐 Image 9/11。
3. 选中 / 取消 / 切换交互正确。
4. send + 跳转正确。
5. meta=null 不崩。
6. 立绘 pixelated。
7. 单文件 ≤ 300 行。
8. 多宽度可读。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/CompanySelectScreen.tsx` + 3 个子组件
- 1 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 3 公司卡 + 复仇进度对齐 Image 9/11。
3. 选中 / 切换交互正确。
4. send + 跳转正确。
5. meta=null 不崩。
6. 立绘 pixelated。
7. 单文件 ≤ 300 行。
8. 多宽度可读。

### 禁止事项

- 禁止超过 3 家。
- 禁止 brandColor 出调色板。
- 禁止无选中可点开始。
- 禁止里程碑渐变进度条。
- 禁止 meta=null 时崩。
- 禁止本任务实现 [查看规则] / [读取进度]。
- 禁止公司图标用真实 logo。

---

## 任务 14：OfficeScene（办公室俯视主场景）

### 使用场景

原型 Image 3 —— 像素瓦片办公室俯视图。**主场景：CEO 工位 + 员工工位 + 茶水间区域 + 走廊 + 多个可点击图块触发子场景**。这是整个游戏"真实感"的核心承载屏。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 OfficeScene 办公室俯视主场景。

# 项目背景
原型 Image 3：
- 整屏俯视像素办公室（瓦片地图，类 Stardew Valley 内饰）
- 中部 CEO 工位（带桌椅 + 电脑 + 思考气泡 "📈" 上升图标）
- 周围 4 个员工工位（带椅子 + 不同方向 + 头顶可显示 ⚠ / 💢 / 💧 等情绪 icon）
- 左侧茶水间区域（咖啡机 + 杯子 + 冰箱）
- 左下饮水机 + 植物
- 右下角员工"张弛"立绘 + speech bubble "Boss 测试环境又挂了..."
- 右侧 panel：今日待办（与市场部开会 / 审批员工加班 / 查看 Q2 项目进度报告） + 消息 (2 条红点)
- 底部 MainActionBar

可交互区域：点击场景图块（茶水间 → GossipScene "tearoom"；会议室 → 会议；员工头像 → 弹个人信息）

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风（瓦片必须 16×16 或 32×32 网格 + 整数缩放 + image-rendering: pixelated）。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict
- 不用 Phaser / PixiJS（保留体积；用纯 div + CSS background-image 拼瓦片）
- 也可用 canvas 离屏绘制（v1 视情况选 div 拼贴）

# 本任务范围
- frontend/src/components/screens/OfficeScene.tsx
- frontend/src/components/screens/office/OfficeMap.tsx
- frontend/src/components/screens/office/OfficeHotspot.tsx
- frontend/src/components/screens/office/TodoPanel.tsx
- frontend/src/components/screens/office/MessagePanel.tsx
- frontend/src/components/screens/office/EmployeeAvatar.tsx
- frontend/public/sprites/tiles/README.md
- frontend/tests/component/office-scene.test.tsx
- frontend/tests/component/office-map.test.tsx

# 必须实现的内容

## OfficeMap.tsx
- 整屏俯视，逻辑网格 40×20 瓦片（每瓦片 32px）→ 总 1280×640
- 用 div 拼贴：地板背景 div + 各家具 sprite layer
- 多个 layer：floor / furniture / characters / hotspot
- 瓦片资源 sprite sheet：sprites/tiles/office_set.png（占位 README）
- 用 background-image + background-position offset 实现瓦片复用
- 不引第三方 tilemap 库

## OfficeHotspot.tsx
interface Hotspot {
  id: string;
  x: number; y: number;          // 瓦片坐标
  width: number; height: number;
  label: string;
  iconName?: IconName;
  destination: "tearoom"|"elevator"|"meeting_room"|"workstation"|"rooftop"|"smoking_area"|"office_main";
  // 鼠标悬停时高亮 + 提示"点击进入..."
}

实现：
- hover 时显示一个半透明白色 outline + tooltip
- click → dispatch screenStore.push("gossip", { scene: destination }) 或其他屏
- ESC 取消 hover

## EmployeeAvatar.tsx
interface Props {
  id: string;
  name: string;
  x: number; y: number;
  mood: "anxious"|"angry"|"tired"|"hopeful"|"numb"|"excited"|"in_love"|"neutral";
  highlighted?: boolean;
}

- 32×48 角色 sprite（4 方向，仅显示当前方向）
- mood 不为 neutral 时头顶显示 16×16 mood icon（⚠/🔥/💧/💗 等）
- click 触发回调（v1 弹个简版 PixelDialog 占位"员工 <name> 详细信息：v2 即将开放"）

## TodoPanel.tsx
items: { time: string; text: string; done: boolean }[]
- PixelCard kind="default" + 标题 "今日待办" + check icon
- 列表渲染；done 显示删除线
- v1 mock 3 条

## MessagePanel.tsx
items: { from: string; text: string; time: string; unread: boolean }[]
- PixelCard kind="default" + 标题 "消息" + 红点数字
- 列表渲染，unread 加红点
- 底部 "查看更多消息" 按钮
- v1 mock 2 条未读

## OfficeScene.tsx
- 进入时 setChrome({ hud: true, mainBar: true })
- 布局：
  · 左侧主区 col-span-9：OfficeMap + 一系列 OfficeHotspot
  · 右侧 col-span-3：TodoPanel + MessagePanel + 可选 GossipPreview（最近偷听到的 1 条）
  · 右下：浮动 EmployeeAvatar 选中时 + PixelSpeechBubble（如原型张弛压力 85%）
- Hotspots：
  · 茶水间区域 → gossip(scene=tearoom)
  · 电梯口 → gossip(elevator)
  · 会议室门口 → gossip(meeting_room)
  · 工位区 → gossip(workstation)
  · 走廊角落 → 占位 toast
- 自动选中"张弛"模拟原型显示（mock）

## sprites/tiles/README.md
- 命名规范：sprites/tiles/office_set.png（512×512 sprite sheet，包含地板/桌椅/咖啡机/植物/电脑等）
- 单瓦片 32×32
- 提供 office_atlas.json 描述每个 sprite 在 sheet 中的 x/y/w/h
- 美术输出：Aseprite + 自动导出 atlas

## 测试
- office-scene.test.tsx:
  · 渲染地图 + 至少 4 个 Hotspot + TodoPanel + MessagePanel
  · 点击茶水间 → screenStore.push("gossip",{scene:"tearoom"})
  · 点击员工头像 → 弹 PixelDialog 占位
  · TodoPanel 渲染 mock 3 条
  · MessagePanel 渲染未读红点
- office-map.test.tsx:
  · OfficeMap 整屏宽度 1280
  · 瓦片缩放 image-rendering: pixelated

# 禁止事项
- 禁止用 PixiJS / Phaser（v1 保持轻量）。
- 禁止瓦片非 32×32（保持网格统一）。
- 禁止 sprite background-size 非整数倍。
- 禁止 hotspot 鼠标圆形（必须方形 outline）。
- 禁止员工头顶 mood icon 大于 16×16。
- 禁止 TodoPanel / MessagePanel 写远程请求（mock 数据）。
- 禁止允许玩家拖动地图 / 缩放（v1 固定）。
- 禁止 EmployeeAvatar 显示真实姓名以外的内部 id。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 3（瓦片俯视 + Hotspot + 右侧 panel）。
3. 4+ Hotspot 可点击。
4. 员工 mood icon 16×16 显示。
5. 瓦片 pixelated 缩放。
6. 单文件 ≤ 300 行。
7. 跳转茶水间正确。
8. 1280×640 内完整展示。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码 + sprites README
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/OfficeScene.tsx` + 5 个 office/* 子组件
- `sprites/tiles/README.md`
- 2 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 3。
3. 4+ Hotspot 可点击。
4. 员工 mood icon 16×16。
5. 瓦片 pixelated。
6. 单文件 ≤ 300 行。
7. 跳转 gossip 正确。
8. 1280×640 内完整展示。

### 禁止事项

- 禁止 PixiJS / Phaser。
- 禁止瓦片非 32×32。
- 禁止 background-size 非整数倍。
- 禁止 hotspot 圆形。
- 禁止 mood icon > 16×16。
- 禁止 panel 发远程请求。
- 禁止拖动 / 缩放地图。
- 禁止 EmployeeAvatar 显示 id。

---

## 任务 15：GossipScene（茶水间对话 + AP 消耗 + 信任度）

### 使用场景

原型 Image 4 —— 茶水间像素场景。**左侧大头像 + 中部 2 个 NPC 像素角色坐在桌边 + 对话气泡 "听说产品部那边又改需求了..." + 右侧 4 个行动点按钮（记下来 / 追问细节 / 安抚情绪 / 离开）**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 GossipScene 茶水间对话场景。

# 项目背景
原型 Image 4：
- 顶部场景标签 "茶水间 TEA ROOM"
- 中央：2 个像素 NPC 坐在圆桌边（程序员男 + 市场部女）；女 NPC 头顶 "..." 气泡
- 左下：大头像立绘 "林小满"（带 trust 标签 "信任度 62 友好"）
- 右侧 panel：行动点 AP 3/3 + 4 按钮（记下来蓝 / 追问细节绿 / 安抚情绪橙 / 离开灰）
- 中下：PixelSpeechBubble "林小满 | 市场部专员" + 内容
- 底部 MainActionBar

交互：
- 进入：发 collect_gossip → 后端返回 GossipLead → 渲染到 SpeechBubble
- 点击行动按钮：每个消耗 AP 1
  · 记下来 → 保存到 store gossipNotes
  · 追问细节 → 发 collect_gossip 再来一条
  · 安抚情绪 → 提高 NPC 信任度（v1 mock + 1）
  · 离开 → screenStore.back() 回 OfficeScene

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store；GossipScene 只 send + 读 selector。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict

# 本任务范围
- frontend/src/components/screens/GossipScene.tsx
- frontend/src/components/screens/gossip/SceneStage.tsx
- frontend/src/components/screens/gossip/ActionPanel.tsx
- frontend/src/components/screens/gossip/TrustBar.tsx
- frontend/src/components/screens/gossip/GossipNotesDrawer.tsx
- frontend/tests/component/gossip-scene.test.tsx

# 必须实现的内容

## SceneStage.tsx
- 像素背景：sprites/scenes/tearoom_bg.png（占位）
- 2 个 NPC 角色 sprite（侧坐姿态）+ 头顶气泡 sprite
- 顶部场景标签卡（左上）"📍 茶水间 TEA ROOM"
- 中部圆桌 sprite + 3 个杯子 + 1 个小植物
- 右侧饮水机 + 左侧咖啡机
- 整体 1280×640 内填满

## TrustBar.tsx
interface Props { value: number; label?: string; size?: "sm"|"md" }
- 笑脸 icon + 数字 + 文本（"友好" 绿 / "中立" 灰 / "敌意" 橙 / "崩坏" 红）
- value ∈ [0,100]
- 颜色按桶：>=70 绿 / 30-70 灰 / 10-29 橙 / <10 红
- 显示在大立绘下方

## ActionPanel.tsx
interface Action {
  id: "note"|"probe"|"comfort"|"leave";
  label: string;
  iconName: IconName;
  variant: "blue"|"green"|"orange"|"red"|"ghost";
  apCost: number;
  disabled?: boolean;
  onClick: () => void;
}

- PixelCard 标题 "行动点 AP X/Y"
- 4 个垂直 PixelButton size=lg
- AP 用完时除"离开"外全 disabled

## GossipNotesDrawer.tsx
- 右下角抽屉，点击展开
- 显示已"记下"的 gossip 列表
- v1 mock 数据 + 当前局新记录

## GossipScene.tsx
- 进入时 setChrome({ hud: true, mainBar: true })
- 进入时立即触发首条 gossip：
  · dataSource.send(makeEnvelope("collect_gossip", { sessionId, quarterNumber, scene: params.scene ?? "tearoom" }))
  · 收到 gossip_result → store ingestGossipResult → 渲染 SpeechBubble
- 4 按钮 onClick：
  · note：本地 push 到 gossipNotes（不发后端）
  · probe：send collect_gossip 再来一条
  · comfort：本地 trust 数值 +1（v1 不影响后端，TODO 任务 22）
  · leave：screenStore.back()
- AP 从 store.snapshot.quarter.apRemaining 取；扣减按 lead.apCost
- 显示 PortraitId="employee_lin_xiaoman"（mock）
- 当 ap=0 且未离开 → toast 提示 "行动点用完，去做别的吧"

# 测试
- gossip-scene.test.tsx:
  · 进入时 send collect_gossip 调用 1 次
  · 渲染立绘 + SpeechBubble + ActionPanel
  · AP 显示正确
  · 点 "追问细节" → send collect_gossip 调用 +1 次
  · 点 "记下来" → notes 长度 +1
  · 点 "离开" → screenStore.back 调用
  · AP=0 时 disabled 正确（仅"离开"可点）

# 禁止事项
- 禁止 GossipScene 调 LLM 生成文本（gossip 来自后端，前端只渲染）。
- 禁止 trust 数值跨 [0,100]。
- 禁止 NPC 头像超过 32×32 sprite。
- 禁止"安抚情绪"按钮在 v1 真的影响后端（只本地 +1，TODO 标注）。
- 禁止离开时不调 screenStore.back（必须显式回 OfficeScene）。
- 禁止 NotesDrawer 与 backend 同步（v1 本地 only）。
- 禁止显示 GossipLead.isTruth（永不存在）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 4。
3. 进入 send 1 次 collect_gossip。
4. 4 按钮交互正确。
5. AP 扣减正确。
6. trust 颜色桶映射正确。
7. 单文件 ≤ 300 行。
8. 不显示 isTruth。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/GossipScene.tsx` + 4 个 gossip/* 子组件
- 1 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 4。
3. 进入 send 1 次 collect_gossip。
4. 4 按钮交互正确。
5. AP 扣减正确。
6. trust 颜色桶正确。
7. 单文件 ≤ 300 行。
8. 不显示 isTruth。

### 禁止事项

- 禁止 GossipScene 调 LLM。
- 禁止 trust 跨范围。
- 禁止 NPC 头像 > 32×32。
- 禁止"安抚情绪" v1 影响后端。
- 禁止漏调 screenStore.back。
- 禁止 NotesDrawer 同步后端。
- 禁止显示 isTruth。

---

## 任务 16：StatsDashboard（六大核心数据 + 风险预警）

### 使用场景

原型 Image 5 —— **"六大核心数据"** 仪表板（6 张大指标卡 + 趋势 sparkline + 风险预警列表 + 左下角顾问立绘语录 + "查看详细分析" 按钮）。v1 只填 4 指标，另 2 张 dim。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 StatsDashboard 六大核心数据屏。

# 项目背景
原型 Image 5：
- 顶部标题"六大核心数据"+ 装饰星点
- 中央 2×3 网格 6 张 PixelMetricCard：现金流/士气/董事会信任/员工人数/市场热度/公司体面
- 右侧 panel "风险预警 🚨" + 3 张风险卡（现金流承压 / 董事会耐心下降 / 项目延期扩大）+ 各色风险标 + "查看全部风险"
- 左下角顾问立绘 + PixelSpeechBubble "老板，数据比表面看起来更危险..."
- 中下"查看详细分析"主按钮（icon line-chart）
- 底部 ContextHint + MainActionBar

v1 4 指标真填，员工人数 / 市场热度 / 公司体面 灰显（NEXT_PUBLIC_METRIC_V2 控制）。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store；只 read selector。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict

# 本任务范围
- frontend/src/components/screens/StatsDashboard.tsx
- frontend/src/components/screens/stats/MetricGrid.tsx
- frontend/src/components/screens/stats/RiskPanel.tsx
- frontend/src/components/screens/stats/RiskCard.tsx
- frontend/tests/component/stats-dashboard.test.tsx

# 必须实现的内容

## MetricGrid.tsx
- 2×3 grid gap px-md
- 6 PixelMetricCard，order 严格：CASH / MORALE / BOARD / SALES / MKT / FACE（按原型）
- v1 阶段 SALES / MKT dimmed=true（除非 env on）
- size="hero" 让卡片足够大（约 280×180）
- sparkline 7 期数据 mock（前 4 期 history + 当前）

## RiskCard.tsx
interface Risk {
  id: string;
  iconName: IconName;
  title: string;
  description: string;       // ≤ 50 字
  level: "low"|"mid"|"high";
}

- PixelCard
- 顶部 icon + title
- 中部 description 12px ink-2
- 底部 RiskLevelBadge：低风险绿 / 中风险橙 / 高风险红
- hover 上移

## RiskPanel.tsx
- 标题 "风险预警" + alarm icon
- 列表 ≤ 5 张 RiskCard
- 底部 "查看全部风险" → 跳全屏风险页（v1 暂 push 同屏 + toast）

## StatsDashboard.tsx
- 进入 setChrome({ hud: true, mainBar: true })
- 主区域 grid 12 列：
  · col-span-9：MetricGrid + 中下 PixelButton "查看详细分析" + 左下 PixelPortrait id="advisor" + SpeechBubble "老板..."
  · col-span-3：RiskPanel
- 计算 Risk 列表（v1 mock 规则）：
  · CASH < 30 → 高风险 "现金流承压"
  · BOARD < 50 → 中风险 "董事会耐心下降"
  · 任何 CARD.fulfilled === false → 中风险 "承诺翻车"
  · v1 至少出 3 条占位

# 测试
- stats-dashboard.test.tsx:
  · 渲染 6 卡（v1：4 真填 + 2 dim）
  · 风险卡按规则生成
  · CASH < 30 时高风险卡出现
  · 立绘 + 顾问 SpeechBubble 渲染
  · 主按钮存在 + onClick 跳到 overview

# 禁止事项
- 禁止 6 卡顺序与原型不一致。
- 禁止 v1 时 SALES/MKT 真填值。
- 禁止风险 description > 50 字。
- 禁止风险卡数量 > 5（截断 + "查看全部"）。
- 禁止顾问 SpeechBubble 文案硬编码（从 selector 取 hint，本任务可 fallback 静态字串）。
- 禁止单卡 sparkline 显示坐标轴。
- 禁止 RiskCard hover 圆角。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 5。
3. v1 4 真填 + 2 dim。
4. 风险卡规则正确。
5. 主按钮跳 overview。
6. 顾问立绘 + 气泡渲染。
7. 单文件 ≤ 300 行。
8. 1280 内完整。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/StatsDashboard.tsx` + 3 个 stats/* 子组件
- 1 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 5。
3. v1 4 真填 + 2 dim。
4. 风险卡规则正确。
5. 主按钮跳 overview。
6. 顾问立绘 + 气泡渲染。
7. 单文件 ≤ 300 行。
8. 1280 内完整。

### 禁止事项

- 禁止 6 卡顺序错。
- 禁止 v1 SALES/MKT 真填。
- 禁止风险 description > 50 字。
- 禁止 > 5 风险卡。
- 禁止顾问文案硬编码。
- 禁止 sparkline 含坐标轴。
- 禁止 RiskCard 圆角。

---

## 任务 17：DecisionScreen + PromiseLedger（决策卡 + 画饼总账）

### 使用场景

原型 Image 8 / Image 10 —— **季度决策页**：左中 CEO 立绘 + 3 张决策卡（融资续命蓝 / 小幅裁员黄 / 隐瞒坏消息红）+ 上方"预计即时变化"小卡 + 右侧"画饼总账"（你说过的话 4/4，含已兑现/进行中/已失败）。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 DecisionScreen 季度决策页 + PromiseLedger 画饼总账。

# 项目背景
原型 Image 8 / 10：
- 顶部装饰：办公室像素瓦片 + CEO 坐桌前 + SpeechBubble "Q1 开局不利，是时候做出艰难决定了..." + 右上"专注/创新/结果"标签
- 中部蓝标题条 "Q1 季度决策" + 副标题 "每个季度只能选择一项决策，选定后无法更改"
- 3 张并排 PixelCard kind="decision"：
  · 融资续命（蓝 icon: handshake）："引入新一轮投资，缓解现金流压力" + 效果预览 ¥+40,000 / 士气-10 / 信任-10 / 体面-10
  · 小幅裁员（黄 icon: scissors）："优化人员结构，提升短期效率" + 效果 +15K / -20 / -5 / -5
  · 隐瞒坏消息（红 icon: megaphone selected）："对外保持乐观，延后披露负面情况" + +5K / -5 / -15 / -20
- 右上小浮卡 "预计即时变化"（即选中卡的 immediateEffect）
- 右侧 panel "画饼总账"：
  · 当前画饼评级 B+ 大字 + 预期收益 +¥12,000/季度
  · "你说过的话 (4/4)" 列表：4 条承诺（Q1 不裁员 ✓ 已兑现 / Q1 Q3 GMV 翻倍 ⏳ 进行中 / Q2 季度内盈利 ✗ 已失败 / Q2 年底市值破 10 亿 ⏳ 进行中）
  · 底部"查看更多历史记录"
- 底部 ContextHint "记住：短期能活下去，才有未来的赢家" + MainActionBar

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict

# 本任务范围
- frontend/src/components/screens/DecisionScreen.tsx
- frontend/src/components/screens/decision/DecisionCardGroup.tsx
- frontend/src/components/screens/decision/EffectPreview.tsx
- frontend/src/components/screens/decision/PromiseLedger.tsx
- frontend/src/components/screens/decision/PromiseRow.tsx
- frontend/tests/component/decision-screen.test.tsx
- frontend/tests/component/promise-ledger.test.tsx

# 必须实现的内容

## DecisionCardGroup.tsx
interface Props {
  cards: DecisionCardDTO[];  // 3 张
  selectedId?: string;
  onSelect: (id: string) => void;
}

- 水平 3 卡，gap px-lg
- 每张：PixelCard kind="decision" + 顶部彩色标题条（按 card.category 决定色）
- 卡片内容：
  · 大 icon (48×48) + 标题 (16px)
  · description (12px ink-2)
  · 分隔线 "效果预览:"
  · 4 行指标 delta（icon + 名称 + 数值 + 颜色）：现金流 / 士气 / 董事会信任 / 公司体面
- selected：金边 + check icon 右上 + 其他 2 张 dimmed
- 键盘 1/2/3 数字键选 / Enter 确认（与 MainActionBar 共享 hotkey 时需要避免冲突；用 input 焦点检查）

## EffectPreview.tsx
- 右上浮动小卡 (240×120) 显示当前 selected 的 immediateEffect 4 项
- 与 DecisionCardGroup 共享数据
- selected=null 时不渲染

## PromiseRow.tsx
interface Props { promise: PromiseDTO; }
- 一行：
  · 左：季度 badge "Q1" (font-retro)
  · 中：状态 icon (check 已兑现 / clock 进行中 / x 已失败) + 内容文字
  · 右：状态文字（绿/橙/红）
- 进度数字（"预计剩余 7 周"）作为副文字

## PromiseLedger.tsx
- PixelCard kind="default" + 标题 "画饼总账"
- 顶部：当前画饼评级 (B+ 等) + 预期收益（绿色数字）
- 中部：标题 "你说过的话 (4/4)" + 4 行 PromiseRow
- 底部："查看更多历史记录 →" 链接 (v1 toast 占位)

## DecisionScreen.tsx
- setChrome({ hud: true, mainBar: true })
- 主区 grid 12：
  · col-span-9：顶部 SpeechBubble + 中部 DecisionCardGroup + 右上 EffectPreview
  · col-span-3：PromiseLedger
- 数据：
  · cards 从 selector selectDecisionCards 取（v1 没接后端时 mock 3 张写死）
  · promiseLog 从 selector selectPromiseLog 取
- selectedId 本地 state（不入 store）；确认才入 store
- 选中后 "确认决策" 按钮：
  · disabled if !selectedId
  · click → dataSource.send(select_decision)
  · 立即 screenStore.replace 到下一阶段（PressScreen if Q3 else SettlementScreen）
  · 等待 decision_ack 进 store；UI 已切走

# 测试
- decision-screen.test.tsx:
  · 渲染 3 决策卡
  · 默认无选中 → "确认" disabled
  · 点第 2 张 → 选中 + EffectPreview 渲染该卡 immediateEffect
  · 键盘 "2" 选第 2 张
  · 确认 → send select_decision 调用 + screenStore.replace
  · Q3 时跳 press；其他季跳 settlement
- promise-ledger.test.tsx:
  · 渲染评级 + 收益
  · 4 条 PromiseRow 状态 icon 正确
  · fulfilled=null → clock；true → check；false → x

# 禁止事项
- 禁止 3 卡数量变化（v1 死守 3）。
- 禁止 EffectPreview 实时模拟全季度（只显 immediate 4 项）。
- 禁止 PromiseRow 显示 promise.parsed 内部字段（只显示 text + status）。
- 禁止 hotkey 在 input/textarea 触发。
- 禁止已选定后还可改（v1：选定后锁定，仅可"重新选择"前提点击；本任务暂不实现重选；selected→确认→入下一屏）。
- 禁止"确认决策"按钮在无选中时仍可点。
- 禁止 PromiseLedger 显示 fulfilled=null 数量在评级计算（v1 评级用 mock B+）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 8/10。
3. 选中切换正确。
4. EffectPreview 跟随选中。
5. PromiseRow 三态 icon 正确。
6. 确认跳转正确（Q3 / 其他）。
7. 单文件 ≤ 300 行。
8. 键盘 1/2/3/Enter 可达。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/DecisionScreen.tsx` + 4 个 decision/* 子组件
- 2 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 8/10。
3. 选中切换正确。
4. EffectPreview 跟随选中。
5. PromiseRow 三态正确。
6. 跳转正确（Q3 / 其他）。
7. 单文件 ≤ 300 行。
8. 键盘可达。

### 禁止事项

- 禁止 3 卡数量变化。
- 禁止 EffectPreview 模拟全季。
- 禁止 PromiseRow 显示 parsed 字段。
- 禁止 hotkey 在 input 触发。
- 禁止选定后可改。
- 禁止确认按钮无选中可点。
- 禁止 fulfilled=null 进评级。

---

## 任务 18：PressScreen（发布会输入 + 必答主题 + 字数校验）

### 使用场景

DESIGN §8.2 + §15.3 + 后端任务 13 —— Q3 发布会输入屏。**摄像头占位 + 必答 3 主题 + textarea (30–300 字) + 实时字数 + 必答关键词命中提示 + 在场记者列表 + [发言完毕] [跳过]**。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 PressScreen 发布会输入屏（v1 文字模式）。

# 项目背景
- DESIGN §15.3：发布会 UI（摄像头占位 + 必答问题 + 在场记者 + 字数统计 + 实时关键词命中）
- 后端任务 13：PressInputService 校验 transcript 30–600 字、做 brand_blacklist + profanity 清洗、返回 PressAck (accepted/flags/replacedCount)
- 评估在异步结算阶段（任务 19）做，本屏不调 LLM；点击 [发言完毕] 后跳到 Settlement 等异步推送

视觉参考 DESIGN §15.3 的 ASCII 框；像素化：摄像头位置用一个空像素方框占位 + "想象你正对着摄像头"小字。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict

# 本任务范围
- frontend/src/components/screens/PressScreen.tsx
- frontend/src/components/screens/press/CameraPlaceholder.tsx
- frontend/src/components/screens/press/MustAnswerPanel.tsx
- frontend/src/components/screens/press/PressTranscriptInput.tsx
- frontend/src/components/screens/press/ReporterList.tsx
- frontend/tests/component/press-screen.test.tsx

# 必须实现的内容

## CameraPlaceholder.tsx
- 像素描边方框 320×240
- 内部居中："📷 想象你正对着摄像头" 16px ink-2
- 角标 "REC" 红字闪烁 + 时长 mock "00:47"
- v1 不真接摄像头（DESIGN §22 提示玩家可文字模式）

## MustAnswerPanel.tsx
items: string[]  // 3 条
- 列表 ①②③ + 内容
- 玩家输入时高亮命中（用 PRESS_KEYWORDS 表，参见 DESIGN §6 +）
- 高亮：✅ 已命中绿 / ⬜ 未命中灰

## PressTranscriptInput.tsx
interface Props {
  value: string;
  onChange: (v: string) => void;
  minWords: number;       // 30
  maxWords: number;       // 300（后端实际 600，但 v1 文字模式 300 更友好）
}

- textarea 6 行
- bg-panel + 2px stroke + 4px hard
- 实时字数 + 颜色：< min 灰 / [min,max] 绿 / 接近 max 黄 / > max 红
- placeholder "新官上任要说什么？"
- ESC 不清空
- 命中真公司名提示（"已替换 N 处敏感内容"——v1 在前端做简版 brand 提醒；真正替换在后端）

## ReporterList.tsx
items: { outlet: string; tone: "neutral"|"friendly"|"hostile"|"mocking" }[]
- 列表 3–5 个媒体
- 每个：tone 色点 + outlet 名 + tone 标签
- mock 4 家（财经主流 / 互联网八卦 / 行业垂类 / 自媒体）

## PressScreen.tsx
- setChrome({ hud: true, mainBar: false })  // 发布会专注模式隐藏底部
- 主区 grid 12：
  · col-span-7：CameraPlaceholder + PressTranscriptInput
  · col-span-5：MustAnswerPanel + ReporterList
  · 底部居中：PixelButton "发言完毕" + 次按钮 "跳过 (扣 FACE -10)" + "改用文字模式" (v1 默认就是文字，placeholder 灰显)
- 提交：
  · 字数 < 30 → button disabled + 提示 "再说点"
  · 字数 ≥ 30 → enabled
  · click → setInflight("submitPress", true) → dataSource.send(submit_press)
  · 不等待 press_ack；UI 切到 SettlementScreen 显示 "AI 正在结算..."
  · press_ack 到达 → store ingest；若 accepted=false → toast 红字 + 留在 PressScreen
- ESC 触发跳过 + 二次确认 dialog
- 离开时 setChrome 恢复

# 测试
- press-screen.test.tsx:
  · 渲染 4 个子组件
  · 字数 < 30 → 按钮 disabled
  · 输入 30 字 → enabled
  · click 发言完毕 → send submit_press
  · ESC → 弹出二次确认 dialog
  · MustAnswer 命中关键词 → 高亮变绿

# 禁止事项
- 禁止 v1 调用 getUserMedia（摄像头占位即可，TODO 标注任务 22）。
- 禁止前端发起 LLM 评估（评估在后端任务 19）。
- 禁止 textarea 超 300 字仍可提交（必须 disabled）。
- 禁止 "跳过" 不弹二次确认。
- 禁止 reporter tone 出 4 选项之外。
- 禁止提交后 UI 卡在 PressScreen 等响应（必须立即跳 Settlement）。
- 禁止前端做真正的 brand 替换（提示即可，替换归后端任务 13）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 字数门槛严格。
3. 提交后立即跳 Settlement，不阻塞。
4. ESC 二次确认。
5. 关键词命中高亮正确。
6. MustAnswer 3 条 + Reporter 3–5 家。
7. setChrome 进出生命周期正确。
8. 单文件 ≤ 300 行。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/PressScreen.tsx` + 4 个 press/* 子组件
- 1 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 字数门槛严格。
3. 提交后立即跳 Settlement。
4. ESC 二次确认。
5. 关键词高亮正确。
6. MustAnswer 3 + Reporter 3–5。
7. setChrome 生命周期正确。
8. 单文件 ≤ 300 行。

### 禁止事项

- 禁止 v1 调用 getUserMedia。
- 禁止前端 LLM 评估。
- 禁止 > 300 字仍可提交。
- 禁止跳过无二次确认。
- 禁止 reporter tone 越界。
- 禁止 UI 阻塞等响应。
- 禁止前端真做 brand 替换。

---

## 任务 19：SettlementScreen（季度结算 - 指标对比 + 承诺结果 + 董事评论）

### 使用场景

原型 Image 12 —— **"Q2 季度结算"** 屏：左侧大立绘 + 中部 4 指标对比卡（上季→本季）+ "本季承诺结果"列表 + 媒体头条 + 右侧"董事评论"+ "员工一句话"+ 底部 [进入下一季度] / [查看详细评分]。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 SettlementScreen 季度结算屏。

# 项目背景
原型 Image 12：
- 左侧大立绘（CEO 疲惫表情 + 保温杯）
- 顶部标题 "Q2 季度结算" + 副标题 "这一季，你又画成了几个饼？"
- 中部 4 指标对比卡：现金流（上季 ¥75,000 → 本季 ¥87,500 "表现良好" 绿）/ 士气（80 → 68 "士气下滑" 浅红）/ 董事会信任（60 → 55 "信任下降" 浅红）/ 公司体面（45 → 35 "体面受损" 浅红）
- 下部"本季承诺结果"列表：4 条承诺（已兑现绿 +6 亿 / 进行中橙 70% / 已兑现绿 — / 已翻车红 -8 人）
- "媒体头条"卡：单条媒体引语 + 来源
- 右侧"董事评论"3 条 PixelPortrait + 引语（王董 / 李董 / 陈董）
- 右下"员工一句话" 1 条引语
- 底部居中：[进入下一季度] 主按钮 + [查看详细评分] 次按钮

数据来自后端 SettlementBundle（任务 19 后端任务）：
- settlement.quarterReport
- settlement.boardReaction
- settlement.employeeGossip
- settlement.metricsDelta
- pressBundle?.headlines[0] 用作媒体头条
- newStats 与 stats_before 做对比

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict

# 本任务范围
- frontend/src/components/screens/SettlementScreen.tsx
- frontend/src/components/screens/settlement/MetricDiffCard.tsx
- frontend/src/components/screens/settlement/PromiseResultList.tsx
- frontend/src/components/screens/settlement/MediaQuoteCard.tsx
- frontend/src/components/screens/settlement/BoardCommentCard.tsx
- frontend/src/components/screens/settlement/SkeletonLoading.tsx
- frontend/tests/component/settlement-screen.test.tsx

# 必须实现的内容

## MetricDiffCard.tsx
interface Props {
  metricKey: "CASH"|"MORALE"|"BOARD"|"FACE";
  label: string;
  iconName: IconName;
  before: number;
  after: number;
  statusText: string;  // "表现良好" / "士气下滑"
  statusColor: "green"|"yellow"|"orange"|"red";
}
- PixelCard
- 标题条彩色 + label
- 中部大笑脸/钱袋等 icon
- 上季数字（删除线效果或上方小字）
- ↓ 箭头
- 本季数字（大字 + 颜色按 delta > 0 绿 / < 0 红）
- 底部状态文字 + 状态色 chip

## PromiseResultList.tsx
items: { quarter: string; status: "fulfilled"|"in_progress"|"failed"; text: string; expected: string; result: string; delta?: string }[]
- 表格风：Q badge / 状态 icon / 标题 / 预期 / 结果 / delta
- 颜色：fulfilled 绿 / in_progress 橙 / failed 红
- 4 条

## MediaQuoteCard.tsx
- PixelCard + 标题 "媒体头条" (newspaper icon)
- 引语用 ""..."" + 来源 "—— XX 财经日报"
- 字体 14px italic（用 Press Start 2P 看不出 italic，用 Fusion Pixel + 引号包装）

## BoardCommentCard.tsx
items: { name: string; portraitId: PortraitId; comment: string }[]
- 3 行
- 每行：小立绘 (size sm 48×64) + 名字（董长 / 财务总监 / 战略顾问） + 引语

## SkeletonLoading.tsx
- 当 store.pendingSettlementBundle === null 且 inflight.settleQuarter === true 时显示
- 4 个 MetricDiffCard 骨架（bg-panel-dim 闪烁）+ "AI 正在结算..." 文本
- llmDegraded=true 时额外显示 "AI 失联，已用兜底文案" 灰标

## SettlementScreen.tsx
- setChrome({ hud: true, mainBar: false })  // 结算专注隐藏底部
- 监听 store.pendingSettlementBundle 变化：
  · null → 显示 SkeletonLoading
  · 非 null → 显示完整结算
- 主区 grid 12：
  · col-span-2：PixelPortrait id="ceo_male_02" expression="tired" size="hero"
  · col-span-7：标题 + MetricDiffCard × 4 + PromiseResultList + MediaQuoteCard
  · col-span-3：BoardCommentCard × 3 + "员工一句话" + 1 条 SpeechBubble 小版
- 底部居中：[进入下一季度] click → screenStore.replace("office") + advance quarter
- 死亡分支：bundle.death 非空 → 不显示"进入下一季度"，改为 [查看死亡报告] click → screenStore.replace("death-report")
- 离开时 setChrome 恢复

# 测试
- settlement-screen.test.tsx:
  · pendingSettlementBundle=null → SkeletonLoading 渲染
  · ingest 完整 bundle → 4 MetricDiffCard 渲染 + delta 颜色正确
  · 死亡 bundle.death 非空 → 按钮变 "查看死亡报告"
  · llmDegraded=true → "AI 失联" 灰标显示
  · BoardCommentCard 3 条
  · 进入下一季 → screenStore.replace("office")

# 禁止事项
- 禁止 SkeletonLoading 卡死（如果 30s 后仍 pending → 显示 "结算异常，请刷新" toast）。
- 禁止 MetricDiffCard 显示 v1 不支持的指标。
- 禁止 PromiseResultList 显示 promise.parsed 内部。
- 禁止 BoardComment 引语字数 > 30。
- 禁止前端写死结算文案（必须从 store 取；为空时显示 - / 占位）。
- 禁止"进入下一季度"按钮在 pending 时仍可点。
- 禁止把 internalEval 显示（不会收到，但万一收到必须忽略）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 12。
3. SkeletonLoading 与 pending 状态同步。
4. 死亡 / 通关分支跳转正确。
5. llmDegraded 灰标显示。
6. PromiseResultList 4 条状态色正确。
7. 单文件 ≤ 300 行。
8. setChrome 生命周期正确。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/SettlementScreen.tsx` + 5 个 settlement/* 子组件
- 1 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 12。
3. Skeleton 与 pending 同步。
4. 死亡 / 通关分支正确。
5. llmDegraded 灰标。
6. PromiseResultList 状态色正确。
7. 单文件 ≤ 300 行。
8. setChrome 生命周期正确。

### 禁止事项

- 禁止 Skeleton 卡死无 fallback。
- 禁止 v1 不支持指标。
- 禁止显示 promise.parsed 内部。
- 禁止 BoardComment > 30 字。
- 禁止文案硬编码。
- 禁止 pending 时按钮可点。
- 禁止显示 internalEval。

---

## 任务 20：OverviewScreen（经营总览 - 趋势图 + 部门状态 + 建议动作）

### 使用场景

原型 Image 7 —— **"公司经营总览"** 屏：左侧大立绘 + 中部"本周趋势" 折线图（6 条线，4 指标 + 员工人数 + 体面）+ 6 小指标卡 + 3 部门状态卡（产品部 / 市场部 / 技术部）+ 右侧 "本周重点事项" + "建议动作" 3 条。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 OverviewScreen 经营总览屏。

# 项目背景
原型 Image 7：
- 左侧立绘 "公司经营总览 - 掌控六大核心指标，带领公司走向盈利与荣耀"
- 中部上方：折线图（6 条线，对应 6 指标，每条颜色 + 图例）+ 顶右"查看详细报告"按钮
- 中下：6 小指标卡（缩小版 PixelMetricCard，size=compact）
- 中底：3 部门状态卡（产品部 / 市场部 / 技术部，各含 mini icon + 状态色 + 一句话点评 + "建议动作" 链接）
- 右上：本周重点事项 list（董事会信任持续下滑 / 市场热度偏低 / 员工申请加班 / Q2 项目进度 62%）
- 右下：建议动作 3 条（稳住现金流 / 处理董事会关系 / 拉高市场热度）

数据：
- 趋势图：history 中过去 7 期的 stats（v1 mock 7 周数据）
- 部门状态 v1 mock 3 条
- 建议动作 v1 mock 3 条
- 重点事项 v1 mock 4 条

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict

# 本任务范围
- frontend/src/components/screens/OverviewScreen.tsx
- frontend/src/components/screens/overview/TrendChartPanel.tsx
- frontend/src/components/screens/overview/CompactMetricRow.tsx
- frontend/src/components/screens/overview/DepartmentCard.tsx
- frontend/src/components/screens/overview/HighlightsPanel.tsx
- frontend/src/components/screens/overview/SuggestedActions.tsx
- frontend/tests/component/overview-screen.test.tsx

# 必须实现的内容

## TrendChartPanel.tsx
- PixelCard 标题 "本周趋势" + 右上 "查看详细报告" 按钮
- 左侧图例（6 行 icon + 名称）
- 右侧 PixelLineChart 6 条 series
- v1 阶段 SALES/MKT 灰显（虚线 or dim color）
- 占位 mock 数据 7 天

## CompactMetricRow.tsx
- 6 PixelMetricCard size="compact" 水平排列
- 数据从 selector 取

## DepartmentCard.tsx
interface Props {
  name: string;
  status: "healthy"|"tired"|"stable"|"alert";
  comment: string;       // ≤ 20 字
  suggestion: string;    // ≤ 20 字
  iconName: IconName;
}
- 上半部分：左 icon + 部门名 + 状态 chip
- 下半部分：背景 bg-panel-dim + "建议动作:" + 文本 + 右 → 箭头

## HighlightsPanel.tsx
items: { iconName: IconName; title: string; subtitle: string }[]
- PixelCard 标题 "本周重点事项"
- 4 行

## SuggestedActions.tsx
items: { color: "green"|"blue"|"red"; title: string; subtitle: string; onClick: () => void }[]
- 3 个全宽 PixelButton 风格条
- 不同色按重要度
- 点击触发 v1 mock toast / 跳屏

## OverviewScreen.tsx
- setChrome({ hud: true, mainBar: true })
- 主区 grid 12：
  · col-span-3：PixelPortrait id="ceo_male_01" expression="confident" size="hero" + 标题 "公司经营总览"
  · col-span-6：TrendChartPanel + CompactMetricRow + 3 DepartmentCard 横排
  · col-span-3：HighlightsPanel + SuggestedActions
- 左下：SpeechBubble "这周最危险的是董事会信任下滑..."

# 测试
- overview-screen.test.tsx:
  · 渲染 6 条折线 series
  · 6 CompactMetricRow 渲染
  · 3 DepartmentCard
  · 4 Highlights
  · 3 SuggestedActions
  · v1 时 SALES/MKT 系列 dim
  · 立绘 + SpeechBubble 渲染

# 禁止事项
- 禁止 TrendChartPanel 引图表库。
- 禁止 DepartmentCard suggestion 字数 > 20。
- 禁止 HighlightsPanel 超过 5 条（截断 + "更多"按钮）。
- 禁止 SuggestedActions > 3 条。
- 禁止部门状态色出 4 选项之外（healthy 绿 / tired 红淡 / stable 灰 / alert 红）。
- 禁止整页超过 1280×640 出现纵向滚动（必须固定布局）。
- 禁止 v1 时 SALES/MKT 折线显示真实数据（mock 占位即可，必须 dim）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 7。
3. 6 折线 + 6 卡 + 3 部门 + 4 重点 + 3 建议。
4. v1 SALES/MKT dim。
5. 立绘 + SpeechBubble 渲染。
6. 单文件 ≤ 300 行。
7. 1280×640 内固定布局。
8. 部门状态色严格。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/OverviewScreen.tsx` + 5 个 overview/* 子组件
- 1 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 7。
3. 6 折线 + 6 卡 + 3 部门 + 4 重点 + 3 建议。
4. v1 SALES/MKT dim。
5. 立绘 + 气泡渲染。
6. 单文件 ≤ 300 行。
7. 固定布局。
8. 部门色严格。

### 禁止事项

- 禁止图表库。
- 禁止部门建议 > 20 字。
- 禁止 Highlights > 5。
- 禁止 Suggested > 3。
- 禁止部门色越界。
- 禁止纵向滚动。
- 禁止 v1 SALES/MKT 真数据。

---

## 任务 21：DeathReportScreen + LegacyVaultScreen（死亡报告 + 遗产解锁）

### 使用场景

原型 Image 6 —— **"公司死亡报告"** 屏：左侧立绘 + 中部墓碑 + 直接死因 / 最大失误决策 / 媒体头条 / 最后留言 + 右侧核心指标（最终结果） + 解锁遗产 3 张 + [带着遗产再来一轮] [返回主菜单]。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 实现 DeathReportScreen 死亡报告 + LegacyVaultScreen 遗产仓库。

# 项目背景
原型 Image 6：
- 顶部 HUDBar 现金流 -¥358,700 红 + 士气 12 崩溃 + 董事会信任 0 归零（数据极糟，表明已死）
- 左侧大立绘 CEO 神情消沉 + SpeechBubble "这家公司...就当我从未来梦里醒来了。"
- 中部：装饰花纹 + 标题 "公司死亡报告" + 副标题 "存活季度: 4 | 死因: 董事会罢免 CEO"
- 中部白卡：墓碑 RIP + 4 行：
  · 直接死因（icon + 文字）
  · 最大失误决策（icon + 文字）
  · 媒体头条（icon + 引语 + 来源）
  · 最后留言（icon + 引语 + 署名）
- 右侧：核心指标（最终结果）—— 6 行带 ↓ 箭头
- 右下：解锁遗产 3 卡（媒体应对经验 +1 / 危机判断 +1 / 失败教训手册 已入库）
- 底部：[带着遗产再来一轮] 主按钮 + [返回主菜单] 次按钮 + dimmed MainActionBar
- 中下 SpeechBubble "老板，接下来做什么？"

数据来源：DeathReportBundle 后端任务 6 / 20。

LegacyVaultScreen：用户点 "带着遗产再来一轮" 时进入，跳到 CompanySelectScreen 之前的中间过渡屏（v1 简化：直接跳 CompanySelectScreen 并展示 "已应用遗产 +X" toast；本任务可选实现一个 Vault 全屏页展示所有 unlocked_legacies + ManagementStyle 列表）。

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- React 18 + TS strict

# 本任务范围
- frontend/src/components/screens/DeathReportScreen.tsx
- frontend/src/components/screens/LegacyVaultScreen.tsx
- frontend/src/components/screens/death/ObituaryCard.tsx
- frontend/src/components/screens/death/FinalStatsList.tsx
- frontend/src/components/screens/death/LegacyUnlockCard.tsx
- frontend/tests/component/death-report-screen.test.tsx
- frontend/tests/component/legacy-vault-screen.test.tsx

# 必须实现的内容

## ObituaryCard.tsx
- PixelCard kind="default" + 顶部装饰花纹（用 pixelarticons "leaf" * 4 拼装左右）
- 标题 "公司死亡报告" 大字 + 副标题 "存活季度: X | 死因: XX"
- 中央 RIP 墓碑 icon (size=72)
- 4 行内容（icon + 标题 + 内容）：
  · 直接死因
  · 最大失误决策
  · 媒体头条（含来源）
  · 最后留言（含署名）

## FinalStatsList.tsx
- PixelCard kind="default" 标题 "核心指标 (最终结果)"
- 6 行 metric：icon + 名称 + 数值 / 100 + ↓ 箭头
- 数值红字（表明都跌）

## LegacyUnlockCard.tsx
interface Props { legacy: LegacyUnlockDTO; isNew?: boolean }
- 小卡 ≤ 120×140
- 顶部 icon（按 LegacyType: megaphone / scale / file 等）
- 中部 label_zh + "+1"
- 底部 "已入库" 小字
- isNew=true 时金边 + sparkle 装饰

## DeathReportScreen.tsx
- setChrome({ hud: true, mainBar: false })  // 死亡场景隐藏 mainBar；可显示 dimmed 版（参考 Image 6 底部 mainBar 灰显示）
- 改进：本任务实现 MainActionBar 接受 dimmed prop 全局禁用所有按钮（已在任务 11 支持）
- 主区 grid 12：
  · col-span-3：立绘 + SpeechBubble
  · col-span-6：ObituaryCard
  · col-span-3：FinalStatsList + 3 LegacyUnlockCard 横排
- 底部居中：[带着遗产再来一轮] + [返回主菜单]
- "带着遗产再来一轮" click：
  · resetForNewRun() 清空 store
  · screenStore.replace("legacy-vault") 或直接 "company-select"
  · toast "已应用 N 条遗产" (mock)
- "返回主菜单" click：
  · screenStore.replace("onboarding")
  · resetForNewRun()

## LegacyVaultScreen.tsx (v1 简化版)
- 全屏展示 meta.unlockedLegacies + unlockedStyles
- 网格 4 列 LegacyUnlockCard
- 底部 [继续到公司选择] 按钮
- 进入：setChrome({ hud: true, mainBar: false })

# 测试
- death-report-screen.test.tsx:
  · 渲染 ObituaryCard + FinalStatsList + 3 LegacyUnlockCard
  · "带着遗产再来一轮" click → screenStore.replace + resetForNewRun
  · "返回主菜单" click → screenStore.replace("onboarding")
  · isNew=true 显示金边
  · setChrome 进入 mainBar=false
- legacy-vault-screen.test.tsx:
  · 渲染所有 unlockedLegacies
  · "继续" 跳 company-select

# 禁止事项
- 禁止死亡报告显示 "胜利" 文案（won 走 SettlementScreen 完结，不走 DeathReport）。
- 禁止 ObituaryCard 字数 > 500（必须 from 后端 bundle.obituary 截断）。
- 禁止 LegacyUnlockCard 显示后端未发的 type。
- 禁止 "带着遗产再来一轮" 不调 resetForNewRun（必须清旧 session）。
- 禁止 "返回主菜单" 跳到 office（避免死循环）。
- 禁止保留旧 session 中的 promise_log / scheduled_events（必须由 resetForNewRun 清除）。
- 禁止 FinalStatsList 显示 v1 不支持的指标（v1 4 项；mock 可附 SALES/MKT 占位 dim）。

# 验收标准
1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 6。
3. 4 行内容 + 6 final stats + 3 legacy 渲染。
4. 跳转正确。
5. resetForNewRun 触发。
6. setChrome 生命周期正确。
7. 单文件 ≤ 300 行。
8. dimmed MainBar 支持。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码
4. 验证命令
5. 声明"未启动后端、未调 LLM"
````

### 预期产物

- `screens/DeathReportScreen.tsx` + `LegacyVaultScreen.tsx` + 3 个 death/* 子组件
- 2 个测试

### 验收标准

1. 测试全过；tsc / eslint 零错误。
2. 视觉对齐 Image 6。
3. 4 行内容 + 6 final stats + 3 legacy。
4. 跳转正确。
5. `resetForNewRun` 触发。
6. setChrome 生命周期正确。
7. 单文件 ≤ 300 行。
8. dimmed MainBar 支持。

### 禁止事项

- 禁止显示"胜利"文案。
- 禁止 obituary > 500 字。
- 禁止显示后端未发 LegacyType。
- 禁止"带遗产"不清旧 session。
- 禁止"返回主菜单"跳 office。
- 禁止保留旧 promise / scheduled。
- 禁止 FinalStats 显示 v1 不支持指标。

---

## 任务 22：mock fixtures + 关键 e2e + FRONTEND_BACKEND_INTEGRATION.md

### 使用场景

收口任务：把前 21 条任务零散的 mock 数据 / 测试组织成稳定的 CI 链；写一份对后端的集成说明文档。

### 可直接复制给 AI 编程工具的完整提示词

````text
你是前端工程师。要为《YES, BOSS!》v2 做"测试套件收口 + mock 全套 + 集成文档"。

# 项目背景
前 21 条任务每条都有自己的测试 + mock；本任务集中整理：
- src/mock/fixtures/ 全套 4 季度数据（happy / die-q1 / press-fail 三剧本）
- tests/e2e/ 用 playwright 跑 3 个关键 happy path
- docs/FRONTEND_BACKEND_INTEGRATION.md 对前端集成做完整说明

# 共用规则
1. 只做前端，不动后端。
2. 不启动后端（mock 模式跑通即可）。
3. 前端永远不调真实 LLM。
4. 业务在 store。
5. 像素风。
6. 单文件 > 300 行就拆。
7. 输出顺序：计划 → 文件清单 → 验证命令。

# 技术栈
- vitest（已配）
- @playwright/test（仅 e2e）

# 本任务范围
- frontend/src/mock/fixtures/{company_template_set_v1.ts, employees_set_v1.ts, decision_cards_v1.ts, gossip_leads_v1.ts, press_bundle_v1.ts, settlement_q1.ts, settlement_q3_press.ts, death_q4_oust.ts}
- frontend/src/mock/scenarios/{happy.ts, die-q1.ts, press-fail.ts}
- frontend/src/mock/mockAdapter.ts（扩展任务 5 占位实现，让 4 季度跑通）
- frontend/tests/architecture/test_no_warn_fields.test.ts（关键断言）
- frontend/tests/architecture/test_no_llm_call.test.ts（关键断言）
- frontend/tests/e2e/full-loop-happy.spec.ts
- frontend/tests/e2e/full-loop-die-q1.spec.ts
- frontend/tests/e2e/press-fallback.spec.ts
- frontend/docs/FRONTEND_BACKEND_INTEGRATION.md
- frontend/.gitignore（确保 playwright artifacts 排除）

# 必须实现的内容

## fixtures/* （每个文件 export 一个常量 + 类型）
- company_template_set_v1.ts: 3 家公司（灵眸 / 源语 / 钛深）符合后端 CompanyTemplate 字段
- employees_set_v1.ts: 12 名员工（与 DESIGN §9.3 对齐）含 mood / personality_tag
- decision_cards_v1.ts: 12 张决策卡 + 每季抽 3 张的预排
- gossip_leads_v1.ts: 6 场景 × 4 条线索模板（reliability 分布）
- press_bundle_v1.ts: 一份完整 PressBundle（含 9 维 scores + 3 媒体 headlines）
- settlement_q1.ts / settlement_q3_press.ts: 不同情况下的 SettlementBundle
- death_q4_oust.ts: 完整 DeathReportBundle（含 3 条 legacyUnlocks）

## scenarios/* （每个 export 一组事件流）
- happy.ts: 4 季度顺利通关 → won
- die-q1.ts: Q1 选错卡 + CASH 触底 → BANKRUPTCY DeathReport
- press-fail.ts: Q3 发布会 transcript < 30 → rejected + flag

每个 scenario 是 dict<InboundType, OutboundEnvelope>，mockAdapter 根据当前 scenario 和 inbound type 返回对应 outbound。

## mockAdapter 扩展
- setScenario(name) 切换
- 维持本地 GameSession state（mock 内可变）
- 接收 inbound → 推动 state → 推送 outbound envelope
- create_game → 推 game_snapshot（基于 scenario 选公司）
- select_decision → 应用 immediate + 推 decision_ack
- collect_gossip → 推 gossip_result（从 fixtures 抽）
- submit_press → 推 press_ack（press-fail scenario 时 accepted=false）
- settle_quarter → 延迟 1500ms 推 settlement_bundle（按 scenario 决定 happy/die）
- die scenario：当 stats.CASH ≤ 0 → settlement_bundle 含 death 字段 + 1.5s 后再推 death_report_bundle

## architecture tests
- test_no_warn_fields.test.ts: 跑完整 happy scenario，遍历所有 outbound envelope payload 序列化，确认无 hiddenSecrets / isTruth / hiddenRisks / internalEval / hidden_secrets / is_truth / hidden_risks / internal_eval 字段
- test_no_llm_call.test.ts: spy fetch / WebSocket / 全局 import；happy scenario 完整跑完 LLM 相关 spy 调用 0 次

## e2e tests（playwright）
- full-loop-happy.spec.ts:
  · 打开 / → onboarding 出现
  · 点 "开始任职" → CompanySelect 出现
  · 选第 1 家公司 → 点 "开始这一局" → OfficeScene 出现
  · 进茶水间 → 收 1 条 gossip → 离开
  · 回到 OfficeScene → 点 "公司管理" → OverviewScreen
  · 跳决策（手动 push）→ 选第 2 张 → 确认 → SettlementScreen
  · 等 SettlementBundle 推送（mock 1.5s）→ 4 MetricDiffCard 显示
  · 跳下一季 → 重复直到 Q4
  · Q3 触发 PressScreen → 输入 80 字 → 提交 → 等结算
  · Q4 顺利通关 → won
- full-loop-die-q1.spec.ts:
  · scenario = die-q1
  · Q1 决策应用后 settle → death bundle
  · DeathReport 出现 + 3 legacy 卡
  · 点 "带遗产再来一轮" → CompanySelect 出现
- press-fallback.spec.ts:
  · scenario = press-fail
  · Q3 提交 25 字 → 按钮 disabled
  · 输入 30 字 → 提交 → 收 press_ack accepted=false → 留在 PressScreen + toast 红字

## FRONTEND_BACKEND_INTEGRATION.md
约 400 行：

1. **协议总览**：v=1；Envelope 字段；camelCase；JSON over WS / REST
2. **数据源切换**：VITE_DATA_SOURCE=mock|ws；mockAdapter 三种 scenario 切换；wsClient 连 ws://localhost:8000/api/v1/ws/{player_id}
3. **消息序列图**：每个屏幕对应的 inbound + 异步推送 outbound 序列
4. **实时期 vs 异步期**：
   · 前端永远不调 LLM
   · 实时期 inbound 立即 ack；UI 不阻塞
   · 异步期 settle_quarter 收 PressAck → SkeletonLoading → 1.5s 后 SettlementBundle 异步推送
5. **WARN 字段清单**：前端 TS 类型已禁止；架构测确保运行时也无
6. **降级路径**：
   · WebSocket 断线 → 自动重连 5 次后放弃 → toast 红条
   · LLM degraded → SettlementBundle.llmDegraded=true → UI 标 "AI 失联"
   · transcript rejected → 留在 PressScreen + toast
7. **本地联调步骤**：
   · 启 mock: VITE_DATA_SOURCE=mock pnpm dev
   · 启 ws: VITE_DATA_SOURCE=ws VITE_WS_URL=ws://localhost:8000 pnpm dev
   · 后端启动方法：见 backend/docs/BACKEND_FRONTEND_INTEGRATION.md
8. **键盘快捷键**：1/2/3/4 主操作；1/2/3 决策卡；Enter 确认；ESC 关闭对话框 / 跳过
9. **响应式**：1280 基准；1024 / 1366 / 1920 测试覆盖
10. **测试命令**：
    · 单元：pnpm test
    · e2e：pnpm e2e
    · 类型：tsc --noEmit
    · lint：pnpm lint
11. **不启动后端的明确声明**：v1 阶段允许仅 mock 模式跑；ws 模式联调由后端任务 21 实现后启用

# 禁止事项
- 禁止 e2e 跑真实后端（必须 mock scenario）。
- 禁止 e2e 跑 LLM 调用（架构断言确保 0 次）。
- 禁止 fixtures 含 WARN 字段（即便后端漏发，前端测试也要确保渲染层不显示）。
- 禁止 scenarios 互相耦合（每个独立可重置）。
- 禁止 FRONTEND_BACKEND_INTEGRATION.md 暴露内部实现细节（保持接口层面）。
- 禁止 playwright 跑超 30s 单个测试。
- 禁止 mockAdapter 把 fixtures 数据直接 mutate（必须 deepClone 后变更，保护 fixture 不变）。

# 验收标准
1. 所有单元 + 架构 + e2e 测试全过；tsc / eslint 零错误。
2. 3 个 e2e scenarios 全通。
3. WARN 字段架构断言通过（运行时 0 出现）。
4. 0 LLM 调用断言通过。
5. fixtures 全套覆盖 4 季度 + 死亡 + 发布会失败。
6. FRONTEND_BACKEND_INTEGRATION.md ≥ 11 章节。
7. mockAdapter 切 scenario 不互相污染。
8. e2e 跑完总时长 ≤ 90s。

# 输出
1. 计划摘要
2. 文件清单
3. 完整代码 + fixtures + FRONTEND_BACKEND_INTEGRATION.md
4. 验证命令（pnpm test / pnpm e2e / tsc --noEmit / pnpm lint）
5. 声明"未启动后端、未发起真实网络请求、未调 LLM"
````

### 预期产物

- 8 个 `mock/fixtures/*.ts` + 3 个 `mock/scenarios/*.ts`
- 扩展后的 `mockAdapter.ts`
- 2 个架构断言 + 3 个 e2e
- `docs/FRONTEND_BACKEND_INTEGRATION.md`

### 验收标准

1. 单元 + 架构 + e2e 全过；tsc / eslint 零错误。
2. 3 e2e scenarios 全通。
3. WARN 字段架构断言通过。
4. 0 LLM 调用断言通过。
5. fixtures 全覆盖 4 季度 + 死亡 + 发布会失败。
6. `FRONTEND_BACKEND_INTEGRATION.md` ≥ 11 章节。
7. mockAdapter 切 scenario 不污染。
8. e2e 总时长 ≤ 90s。

### 禁止事项

- 禁止 e2e 跑真实后端。
- 禁止 e2e 跑 LLM。
- 禁止 fixtures 含 WARN。
- 禁止 scenarios 耦合。
- 禁止文档暴露内部细节。
- 禁止 e2e 单测 > 30s。
- 禁止 mockAdapter 直接 mutate fixtures。

---

## 收尾

### v1 → v2 视觉迁移备忘

| 维度 | v1（保留为兜底） | v2（本提示词库实现） |
|---|---|---|
| 形式 | 纯 DOM + 双击 index.html | Vite + React + WebSocket |
| 数据源 | 浏览器直连 LLM | 后端 ws + mock 双源 |
| 视觉 | 终端深夜美学（黑底金字） | 8-bit 像素风（白底彩按钮） |
| 决策卡 | 文本卡 | PixelCard + 立绘 + 效果预览 |
| 发布会 | textarea + AI 兜底文案 | PixelPortrait + 摄像头占位 + textarea + 异步结算 |
| 死亡报告 | 报纸版式 | 像素墓碑 + 立绘 + 遗产卡 |

### 屏幕跳转图（前端导航）

```
[onboarding] → [company-select] → [office] ⟷ [gossip]
                                     ↓
                              [stats-dashboard] / [overview]
                                     ↓
                              [decision]
                                     ↓
              (Q3) → [press] → (Settlement 跳过)
                                     ↓
                              [settlement] → [office] (q+1) → ... 直到 Q4
                                     ↓
                  death: → [death-report] → [legacy-vault] → [company-select] (新一局)
                  won: → [settlement won 版] → [legacy-vault] → [onboarding]
```

### 一局完整 LLM 调用预算（与后端对齐）

| 阶段 | 来源 | 次数 |
|---|---|---|
| 实时期（前端任何操作） | — | **0**（铁律） |
| 异步结算 Q1–Q4 Director | 后端任务 19 | 4 |
| Q3 发布会 PressEval | 后端任务 19 | 1 |
| 死亡 / 通关 DeathReport | 后端任务 20 | 1 |
| **合计** | | **6**（与后端预算一致） |

### 项目通用快速验证命令

```
cd frontend
pnpm install
pnpm dev                          # 启动 mock 模式
VITE_DATA_SOURCE=ws pnpm dev      # 联调后端模式
pnpm test                         # 单元 + 组件 + 架构
pnpm e2e                          # 关键 3 个 happy path
tsc --noEmit
pnpm lint
```

### 与后端、AI Agent 工程师、策划的接口表

| 你给 | 给谁 | 形态 |
|---|---|---|
| inbound envelope schema | 后端 | `protocol/inbound.ts` |
| 协议字段命名约定 | 后端 | camelCase；与后端任务 6 outbound 对齐 |
| 屏幕设计细节 | 美术 | `screens/*` + sprites README |
| 必答关键词命中规则 | AI Agent 工程师 | `PressScreen` 的 PRESS_KEYWORDS 表 |
| 数字字段范围 | 策划 | 各 DTO 类型注释 |

| 你消费 | 来自谁 | 形态 |
|---|---|---|
| outbound envelope schema | 后端 | `protocol/outbound.ts` 1:1 对齐 |
| 决策卡 / 公司 / 员工模板 | 策划（经后端） | mock 时本地 fixtures；ws 时后端 content registry |
| 立绘 sprite + 瓦片 | 美术 | `public/sprites/*` |
| 像素图标 | pixelarticons | `iconCatalog.ts` 白名单 |
| 字体 | 开源 | Fusion Pixel + Press Start 2P |

> **设计师注**：v2 像素风的精髓是"克制"——颜色、字号、间距、动画都极致整数化，看起来糙、玩起来扎实。任何想"加点圆角让它柔和"的冲动都要忍住——这不是 UI 工艺品，这是一台老虎机：你要让玩家相信每次决策都是骰子，每次发布会都是真盘。

> **文档结束** · 任何前端变更请同步 `FRONTEND_BACKEND_INTEGRATION.md` 与本提示词库。
